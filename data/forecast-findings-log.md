# Forecast findings log — daily-ops-guardian

Append-only. date | item | signal | evidence (live SQL, run date) | direction. Log only — forecast edits via weekly/monthly proposals (V6).

## 2026-07-03 (first guardian run; forecast version 369f5cb1 published 2026-06-08, 25d old 🟡)

| item | signal | evidence (14d window) | direction |
|---|---|---|---|
| FG-MAT-30G | committed > forecast | committed 300 vs forecast 177.3 | forecast under-estimates matcha B2B demand; raise |
| GTCC-MUZ-SMAR-1L | committed > forecast ×2.4 | committed 14 vs forecast 5.9 | Muza spicy margarita under-forecast |
| ADD-MUZ-HER-1L | committed ≈ forecast, zero stock | committed 4, forecast 5.9, on-hand 0 | confirm Muza line status (parked) — real orders exist |
| FG-CON-1L | forecast demand w/o supply plan | forecast 175 /14d, on-hand 105, planned 0 | not a forecast miss — planning gap; handed to Thursday ritual |
| FG-SAN-WHI-750ML | forecast w/o production | forecast 40.9, on-hand 12, planned 0 | check if sangria forecast still realistic (summer) |

Data-quality flags (not forecast): negative on-hand — FG-SAN-RED-3850ML −1, FG-SAN-BAB-RED/WHI-750ML −6/−6, ADD-GAR-ORA-DRY −16, EXCLUDED-NONSTOCK −346. rebuild_verifier=0 ∴ ledger-consistent negatives = unrecorded production/receipt events.

## 2026-07-05 (second guardian run; manual, morning trigger produced no evidence — see run notes below; forecast version 369f5cb1 published 2026-06-08, unchanged, now 27d old 🟡)

| item | signal | evidence (14d window) | direction |
|---|---|---|---|
| FG-DET-STR-500ML | no forecast coverage at all | committed backlog 1,250 units / 7 open orders (oldest 2026-06-08, 27d) vs forecast_14d = 0 (item absent from `fn_forecast_daily_demand` output entirely) | confirm this B2B/Elita-Ofek line is intentionally forecast-exempt (custom account), or add forecast coverage |
| FG-DES-1L | committed ≈ forecast, zero firmed batch found | committed 51 (3 orders, oldest 2026-06-24, 11d) vs forecast 60.45; no `production_plan` row (item- or base-level) in next 14d | needs a Thursday-ritual production slot, or confirm this item is intentionally parked like the Muza line |
| ADD-MUZ-PNMM-1L | committed persists, oldest unfulfilled order now 42 days | committed 25 (5 orders, oldest 2026-05-24) vs forecast 83.64; still no firmed batch (Muza line parked per 2026-07-03 finding) | Muza line status decision is more urgent now — oldest unfulfilled order has aged from unknown to 42 days |
| FG-ENE-1L / FG-ENE-500ML | committed backlog keeps growing against a known, deliberate no-batch decision | committed 27+24=51 units (10 open orders) vs forecast 157.7+72.7; base deprioritized vs CON-REG per Tom's 2026-07-04 delegated tradeoff (plan-production-14d) | re-confirm the ENE vs CON tradeoff at the next Thursday ritual now that backlog is still accumulating |

Data-quality / engine-contract flag (not forecast): `fn_compute_daily_fg_projection`'s `incoming_supply_qty` nets only `purchase_order_lines` receipts, not firmed `production_plan` output — so its raw FG shortfall read this run (41/79 active items, ~₪22,994 material margin "at risk" over 14d) is a *zero-further-production* baseline, not netted against this week's already-firmed, Tom-approved 2026-07-04 batches. Cross-checked manually against `production_plan`: most flagged items already have a scheduled batch inside the window. Recommend the engine eventually nets firmed production as "incoming supply" the same way it nets PO receipts — flagged here for awareness, not actioned (out of guardian's write scope).

Run note: today's 06:30 IDT scheduled trigger fired (per trigger logs) but produced no completion evidence (no commit here, presumably no email) — leading hypothesis is the fresh/headless trigger session lacked live Supabase access. This run is a manual make-up executed from an interactive session with confirmed live Supabase + Gmail access, to isolate "skill bug" from "trigger infrastructure gap."

## 2026-07-06 (manual run, Tom asked explicitly for a forecast-improvement suggestion; forecast version 369f5cb1 published 2026-06-08, now 28d old 🟡 — first run past the 14d staleness threshold)

Stage 1b two-window persistence test (day −28..−15 vs −14..0) run read-only against `fn_forecast_daily_demand` + `stock_ledger` FG_OUT_PICK, per plan-production-14d §1b. 18 items passed the >15%/>15% same-direction filter.

**Routine correction candidates (not stock/backlog-censored, factor inside [0.5, 2.0] clamp — proposed to Tom in chat this run, none written without approval, per V6):**

| item | actual_b (14d rate) | forecast_b | dev | proposed_factor |
|---|---|---|---|---|
| FG-REV-500ML | 7.57/day | 3.92/day | +93.0% | 1.47 |
| FG-FRE-500ML | 7.36/day | 5.95/day | +23.6% | 1.12 |
| FG-DES-1L | 4.14/day | 3.50/day | +18.4% | 1.09 |
| FG-DET-500ML | 11.21/day | 20.84/day | −46.2% | 0.77 |
| ADD-GAR-ROSE-DRY | 0.29/day | 0.52/day | −45.0% | 0.77 |

**Structural (factor outside clamp — flagged for Tom's judgment, NOT a routine proposal):**

| item | dev | implied factor |
|---|---|---|
| FG-SAN-WHI-1L | +560.0% | 3.80 |
| FG-MAT-18G | +294.6% | 2.47 |

**Stock/backlog-censored (real signal exists, cannot be trusted yet — low actuals reflect empty shelf or unpicked backlog, not low demand):** FG-FRE-500ML-NS, FG-MAT-500G, GTCC-MUZ-APPZ-1L, FG-NM-3850ML, FG-FRE-1L, FG-NM-1L, FG-DET-1L, FG-REV-1L, FG-NAM-1L, FG-DET-1L-NS, FG-CAL-1L.

**Separate finding — forecast staleness itself:** published version `369f5cb1` is now 28 days old, past the 14d Stage-0 threshold for the first time since this skill started tracking (was 25d on 2026-07-03, 27d on 2026-07-05). Horizon (`2026-06-01` + 9 weeks = through `2026-08-03`) still covers the planning window, so this is a freshness/quality flag, not a coverage gap — but the correction-factor drift above (5 routine + 2 structural candidates, mostly this run) is consistent with a forecast that is due for a full republish rather than one-off factor patches. Recommend: republish the forecast at the next monthly cycle (guardian is 2026-07 first-of-month proposal per §Monthly) rather than accumulating more per-item factors.

**RM/PKG gaps found in Stage 2 (latest planning run 2026-06-23, 13d stale — read-back only, not re-run):** `RAW-LEMON-JUICE` (11.36 on hand) and `RAW-MERLOT-GRAPES` (1.73 on hand) both feed multiple Muza batches marked `ready_if_purchase_executes` in that run but have **no line in the current open purchase session** (`2026-07-04`) — a real procurement gap, not yet actioned (handed to procurement-planning skill scope, not written here). `PKG-LABEL-MUZ-JASM-1L` has `needs_purchase_missing_supplier` (zero supplier mapping) blocking the entire Muza Jasmine line at every recommended batch size — needs a new supplier before it can even enter a purchase session. `ADD-MUZ-BZSM-1L` still `blocked_missing_bom` (no active BOM version) — same as prior runs, unresolved.

## 2026-07-09 (third guardian run; manual, Thursday — plan-production-14d ritual day; forecast version 369f5cb1 published 2026-06-08, unchanged, now 31d old 🟡; rebuild_verifier=0, last planning run 2026-06-23)

| item | signal | evidence (14d window) | direction |
|---|---|---|---|
| FG-DET-STR-500ML | improving — not a new alarm | committed backlog now 850 units / 5 open orders (oldest 2026-06-09, 30d), down from 1,250/7 on 2026-07-05; 500L batch firmed for 2026-07-14 (Tom-approved 2026-07-04 note references the tranche plan) | system already handling this correctly via the tranche plan; keep scheduling next tranche, no new action |
| ADD-MUZ-PNMM-1L | Muza line status decision now overdue 46 days | committed 132 units / 3 orders, oldest 2026-05-24 (46d); still zero production_plan row; 6 more Muza SKUs (FG-MUZ-JAS-200ML, ADD-MUZ-PRPL-1L, ADD-MUZ-TRIL-1L, FG-MUZ-NEG-200ML, ADD-MUZ-JASM-1L, FG-MUZ-HER-200ML, FG-MUZ-QUE-200ML) share the same zero-plan pattern, committed gaps 24–144 units each | this has recurred in every guardian run since 2026-07-03; escalating to a direct ask in today's report rather than another silent log line |
| FG-FRE-1L (base FRE-REG) | new — base fully unscheduled | on-hand 26, committed backlog 97/13 orders, gap 71 units, ~₪1,764/day material margin at risk; base_bom_head_id BOM-BASE-FRE-REG has zero `production_plan` rows (planned or draft) anywhere in the 14d window, while sibling bases (DET-REG, DET-STR, NAM-REG, FRE-NS, CAL-REG) all have at least a draft or planned batch | flagged for today's Thursday ritual — highest-margin unscheduled gap found this run |
| FG-MAT-30G / FG-MAT-500G | persists — still zero repack batch | FG-MAT-30G: 0 on-hand vs 300 committed (1 order, since 2026-06-29, 10d); FG-MAT-500G: 3 on-hand vs 8 committed, ~₪2,822/day material margin at risk | same gap as 2026-07-03 finding, still unaddressed 6 days later; needs a matcha-repack slot in today's ritual |
| RM/PKG procurement-approval backlog (not forecast, engine-contract adjacent) | 4 draft purchase-session POs (~₪14,223 total, session opened 2026-07-04) have sat `proposed`/unapproved since their `order_by_date` (2026-06-24–2026-06-27, 12–15d overdue): PKG-CAP-PLASTIC-28+PKG-BOTTLE-500ML (session_po ec4db7d9, ₪6,940.73), RAW-APPLE-DRY (ba71c749, ₪2,316.00), RAW-LYCHEE-PUREE (21965b83, ₪4,574.20), PKG-LABEL-CON-500ML (731e0f79, ₪392.04). PKG-CAP-PLASTIC-28 and PKG-BOTTLE-500ML block the firmed 2026-07-12 DET-REG batch in 3 days | approval, not sourcing, is the bottleneck — surfaced as today's #1 action, not a new draft (drafts already exist) |
| PKG-BOTTLE-1L | overdue receipt, separate from the approval backlog above | PO-2026-00216: 32,999 units ordered 2026-05-13, `expected_receive_date` 2026-07-05 (4d past due), still `OPEN`/not received; blocks the firmed 2026-07-12 DET-REG batch | chase the supplier directly — quantity is not the issue, the shipment is |

Data-quality flags (not forecast): negative on-hand — EXCLUDED-NONSTOCK −354 (accounting bucket, not a real FG), FG-CAL-1L −136 (has a firmed 500L batch 2026-07-20; likely unposted production/receipt event), FG-MAT-100G −10, GT-MAT-KIT −8, FG-NM-3850ML −7, FG-SAN-BAB-RED/WHI-750ML −6/−6, ADD-UBE-500G −5, GTCC-MUZ-APPZ-1L −3, AP-DRI-PIN-1KG −1, FG-SAN-RED-3850ML −1. rebuild_verifier=0 ∴ ledger-consistent negatives = unrecorded production/receipt events, same pattern as 2026-07-03.

Double-order-trap (Stage 0 gate, not forecast): 15 open PO lines across 9 POs (PO-2026-00256/257/258/259/260/261/263/264, ordered 2026-07-03–2026-07-06) still have no `expected_receive_date`. Two of them (RAW-LIME-PUREE 80 units on PO-2026-00258, PKG-LABEL-NAM-500ML 1,000 units on PO-2026-00260) would fully cover this run's Stage-2 shortages on those components once a supplier ETA is confirmed — flagged in today's report as a quick win, not a new order.

## 2026-07-17 (Tom asked "למה זה לא עובד?" — root-cause + manual run; forecast version 369f5cb1 published 2026-06-08, still unchanged, now 39d old 🟡)

**Root cause of the 12-day silence (2026-07-05 → 2026-07-17), found this run:** there was never a recurring cron trigger for this skill. `list_triggers` shows the account's entire trigger store is one-shot `send_later` PR-babysitting check-ins (re-arming hourly) for unrelated PRs — none targets `daily-ops-guardian`. The 2026-07-05 hypothesis ("fresh/headless session lacked live Supabase access") was the wrong diagnosis: a live check this run (`rebuild_verifier()`) succeeded fine from a fresh-context session. Separately, the Make delivery pipe (scenario `6439326`) is healthy and fired successfully on 07-06, 07-09, 07-10 and 07-15 (real 27–38KB payloads, HTTP 200) — meaning the skill *did* run and email Tom on at least those four days via manual/ad-hoc fires, but none of those runs committed a findings-log entry, so this log under-represents actual run history. Fixed this run: real cron trigger created (`30 3 * * *` UTC, fresh session per fire, see repo commit). Process gap also flagged: findings-log commit should not be optional on days the report sends — folding into the Stage 4/5 checklist going forward.

| item | signal | evidence (14d window) | direction |
|---|---|---|---|
| PKG-BOTTLE-1L | engine-contract mask hides a real PKG shortage | PO-2026-00216 (32,999u) is 11 days overdue (expected 2026-07-05); projection still counts it as arriving today, netting the component to "healthy". Real position: 816 on-hand vs 4,957 firmed 14d demand = **-4,141 deficit**, first-blocked 2026-07-19. No draft session line exists for it yet (the generator that produced session `00802e4a` on 07-16 was fooled by the same mask). | not a forecast miss — data-quality/engine-contract gap (same class as the 07-05 `incoming_supply_qty` flag). Chase the supplier or correct the PO status before the engine can see this; flagged as today's #1 procurement action regardless. |
| ADD-MUZ-PNMM-1L | committed backlog keeps aging, Muza line status still undecided | 72 units unpicked (2 orders), oldest now **54 days** (was 42d on 07-05, 25-ish implied on 07-03) — +12 days aged with zero progress since the last guardian run that flagged it | third consecutive flagged run with no movement; this is no longer a forecast question, it's a stalled Tom decision (line status) blocking a real customer promise |
| FG-MUZ-JAS-200ML / FG-MUZ-NEG-200ML | same Muza-line blocker, two more SKUs | 24 units unpicked each (2 orders each), oldest 33 days; no firmed `production_plan` row for either in the next 30 days | same root cause as above — bundling under one Tom decision, not two separate asks |
| FG-CAL-500ML / FG-CAL-1L | committed backlog with no firmed coverage, **not previously logged** | 22u (500ML) + 16u (1L) unpicked, no firmed batch in next 30 days | unlike ENE (explicitly deprioritized per Tom's 2026-07-04 tradeoff), CAL's exclusion doesn't trace to a documented decision — flagging as new, needs Tom confirmation whether this is deliberate or an oversight |
| — (systemic) | committed-demand blind spot, still 100%, third run running | Every open LionWheel order with backlog_units > 0 checked this run (31 items) had `backlog_units_dateless = backlog_units` — i.e. **zero** committed demand is currently visible to `fn_compute_daily_fg_projection`'s own internal calculation; every committed-shortage number in this run came from a direct mirror read, not the engine | unchanged since 07-03; the engine-contract fix recommended on 07-05 (net firmed production + dateless-aware committed demand) has not landed — re-flagging, still out of guardian's write scope |

Data-quality flags (not forecast): 6 open PO lines still missing `expected_receive_date` (RAW-LUISA 48u, RAW-LIME-PUREE 80u, PKG-LABEL-DET-1L-NS 1000u, PKG-LABEL-NAM-500ML 1000u, RAW-ROSE-DRY-GARNISH 10u, RAW-DRIED-ORANGE 5u) — 3 of these (LUISA, LIME-PUREE, LABEL-NAM-500ML) are the exact components showing as short in Stage 2's RM/PKG coverage, i.e. already on order, just needs a date set. Physical-count staleness (from session `00802e4a`'s own `input_integrity`): only 4/33 session-scoped targets fresh, 19 stale, 10 never counted, oldest 66 days — caveats the confidence of every RM/PKG on-hand number this run, surfaced but not gate-blocking (`rebuild_verifier=0`).


## 2026-07-24 (restoration-verification run; manual, Friday; forecast c7e9db2a published 2026-07-23, age 0 🟢; rebuild_verifier=0 🟢)

Verdict 🟡 לעקוב (WATCH): stock truth clean, **zero committed (dated) orders at risk**, watch items are forecast-driven production + procurement (open weekly session) + a data-quality cluster.

| item | signal | evidence (live SQL 2026-07-24, 14d window 07-24..08-06) | direction |
|---|---|---|---|
| FG (38 of 67 items) | forecast shortfall, committed = 0 | `fn_compute_daily_fg_projection`: 38 items project a stockout; `demand_lionwheel_qty`=0 for every one; largest DETOX 1L −86.4, FRESH 1L −58.6, DETOX 1L NS −35.5, DETOX 0.5L −32.7 | not committed-driven — production-planning coverage. Summer DETOX/FRESH forecast looks high vs on-hand; confirm realism at Thursday retro |
| FG-MAT-30G | forecast shortfall persists | −17.7 / 14d vs forecast 88.6 | matcha demand still strong (consistent w/ 2026-07-03 finding); forecast holding |
| ADD-MUZ-PNMM-1L | reported yesterday, still short forward | 71 planned + 93 total reported 07-23; forward −8.4 / 14d | Muza Pink Mama now in production (was parked per 07-03/07-05) — good; keep on plan |
| (procurement, not forecast) PO-2026-00216 | overdue receipt 18d | PKG-BOTTLE-1L 32,999 u, expected 2026-07-05, still OPEN; projection counts it as arrived → suppresses reorder | chase supplier / update expected date / close line |
| (data-quality) RM/PKG counts | 15 of 25 in-scope components stale or never-counted, oldest 73d | purchase_session 62179ce9 `input_integrity.counts` | schedule counts before next purchase placement |

Delivery / run evidence (V4, observed this session — no fabrication): rebuild_verifier=**0**; FG via `fn_compute_daily_fg_projection`; RM via `planning_run_component_netting` (run 2ad307b8, 90/141 components net-positive, ₪38,990 across 10 draft POs in open session 62179ce9); email POSTed to Make webhook → **HTTP 200 "Accepted"**; Make execution `b987cb5a` status=success (2 ops, 27,873 B, 06:05:52Z); email confirmed delivered to tom@gteveryday.com (thread `19f92baf5228748a`, 06:05:53Z). Yesterday production report present (2/2 batches, 93 of 101 u; plan rows not linked-closed — `completed_submission_id` null — minor data note, not a missing-report red).

Root-cause of the 2026-07-05 → 07-24 log gap: fresh scheduled-session runs were not completing the full loop (live-SQL → fill → send → **commit findings-log** → chat). The Make→Gmail delivery leg itself continued to fire on some days (executions on 07-21/07-23), so delivery infra was not the failure — the full guardian loop was. Mitigation this session: skill hardened with a **connector pre-flight** (V9) so a connector-less session fails *loudly* (chat/push + Gmail-draft note + a FAILURE log row) instead of silently no-op'ing.

## 2026-07-25 (sunday-prep / motzash run; manual fire from an interactive session; Saturday 22:45 IL; rebuild_verifier=0 🟢; LionWheel mirror fresh 19:30Z)

Verdict 🔴 דחוף (URGENT): Sunday 26/7's firmed DETOX tank is **materially blocked** — one raw material, and the PO that fixes it has been sitting unplaced since Thursday.

| item | signal | evidence (live SQL 2026-07-25) | direction |
|---|---|---|---|
| RAW-LIME-PUREE | blocks a firmed batch | `BOM-BASE-DET-REG` active version needs **15.00 kg** per 500 L tank; `current_balances` on-hand **0.98 kg** (last refreshed 2026-07-21). Sunday 26/7 has a firmed 500 L DETOX tank (`TEAEDD:w2-rebuild-v2-20260716:BOM-BASE-DET-REG`, pack 365×FG-DET-1L + 270×FG-DET-500ML) | place `PO-2026-00267` (Ristretto, 80 kg, ₪2,736 — `APPROVED_TO_ORDER` since 23/7 19:18Z) Sunday first thing, or move the tank |
| PKG-LABEL-DET-500ML | packaging short for the same batch | on-hand **40** vs **270** bottles planned → short 230. Count last refreshed 2026-07-21 | physical check before 06:00; count age makes the number itself untrustworthy |
| FG-MAT-30G | committed demand, zero stock, unverifiable packaging | on-hand **0**; **330 units** open on 2 LionWheel orders; Monday 27/7 plans only 30 TIN. `PKG-TIN-MAT-30G`, `PKG-LID-MAT-30G`, `PKG-LABEL-MAT-30G` have **no `current_balances` row at all** (never counted/received) while the same run's netting shows both tin and lid net-short 10,485 | bulk matcha is fine (RAW-MATCHA-BULK 366.5 kg) — the constraint is 30G packaging. Needs a real count + a buy decision |
| ADD-MUZ-PRPL-1L / ADD-MUZ-TRIL-1L | committed vs zero stock, plan covers partially | PRPL: 180 open / 0 on-hand / 126 planned Mon. TRIL: 120 open / 0 on-hand / 96 planned Mon (both plan rows created by Tom 25/7 18:53Z) | gap stays open toward customers; confirm tranche pacing |
| (procurement) 4 POs | zombie-queue pattern, 2 days old | `APPROVED_TO_ORDER`, none placed: 00266 Havshush (apple 20 kg + nana 15 kg), 00267 Ristretto (lime 80 kg), 00268 Holyland (ODK peach 84), 00269 Neve HaTavlin (luiza 40 kg). Line totals ₪1,320+₪360+₪2,736+₪2,938.32+₪2,800 = **₪10,154.32** | Sunday 09:00 first task per the Thursday clock |

**No `GUARD:` drafts written this run — deliberate.** Sunday already carries Tom's own firmed rows (`TEAEDD:%` / `PLAN14D:%`, untouchable per V5), and the gap is *materials*, not an unplanned slot; a draft batch would not have made lime puree exist. Reported instead. Consistent with §C1 (draft-writes only) and V3 (committed first).

Gauges: G1 45% 🟡 (`fn_compute_daily_fg_projection` 67 items projected / 37 with a stockout day; `demand_lionwheel_qty`=0 across all of them → forecast-driven, not dated-committed). G2 36% 🔴 (planning run `2ad307b8`: 141 components in netting, 90 net-short; lime puree blocks a firmed batch inside its lead time). G3 100% 🟢 (yesterday = Friday, not a working day → not applicable).

Delivery evidence (V4, observed this run): email POSTed to the Make webhook → **HTTP 200 "Accepted"**; Make execution **`fa250a049d904d77b355e0d192f9bde6`** 2026-07-25T19:46:30.602Z, status=1 success, 2 ops, 31,396 B → tom@gteveryday.com.

Infrastructure note: the three weekly Routines (`sunday-prep`, `queue-guard`, weekly `scorecard`) were **self-bound to dead sessions** and had been firing into empty sessions with no repo on disk — the same silent-death class as the 07-05→07-24 gap, one layer up. Rebuilt this session as fresh-session-per-fire on `env_017y3uFy4wo8cdvYBA2FP68T` with an explicit in-prompt repo-bootstrap chain (`add_repo` → clone → `register_repo_root`) plus a loud-failure fallback. This particular sunday-prep run was executed manually from an interactive session because the fired routine had not produced drafts or a send within ~8 minutes; its bootstrap path remains **unverified end-to-end** and must be confirmed on the next scheduled fire (Thu 30/7 queue-guard is the earliest natural test).

## 2026-07-28 (daily 06:30 IL run; scheduled trigger; rebuild_verifier=0 🟢; forecast c7e9db2a age 5d 🟡; planning run `2ad307b8` age 5d — stale, next scheduled ritual is Thursday 30/7)

Verdict 🔴 דחוף (URGENT): RM/PKG coverage has degraded sharply against the *same* firmed production window flagged clean-ish on 07-24/07-25 — this is now the dominant signal, not a new item.

| item | signal | evidence (live SQL 2026-07-28) | direction |
|---|---|---|---|
| RM/PKG coverage (systemic) | 90 of 141 netted components net-short, up from 90/141 on 07-24 (same count, but `earliest_short_bucket` is now **2026-07-20** — already 8 days in the past relative to today) | `planning_run_component_netting` for run `2ad307b8` (executed 07-23, now 5 days old): worst lines — `PKG-CAP-PLASTIC-28` (Plastic Cap 28mm) net **233,872.3** short, `PKG-BOTTLE-500ML` (Dark Glass Bottle 500ml) **79,703.9** short, `RAW-SUGAR` **39,551.7** short, `PKG-BOTTLE-MUZ-1L` **23,578.5** short, `PKG-CAP-MUZ-1L` **22,410.5** short. Firmed (`status='planned'`) batches exist today 07-28 through 08-06, so this is not a future-only number. | the netting run itself is 5 days stale — recommend a fresh planning run / procurement-planning session **before** trusting these absolute totals for buying decisions, but the direction (severe, worsening) is real and live. No new purchase-session draft written this run — session `ecad3ef0` (opened 2026-07-26) is already open; duplicating it would violate V2/C1 draft discipline. Escalating for Tom review instead. |
| FG sell-coverage (14d, `2026-07-28`..`2026-08-10`) | 37 of 67 FG items project a stockout day; **`demand_lionwheel_qty`=0 for every one** (same pattern as every run since 07-03/07-24) | `fn_compute_daily_fg_projection`: largest gaps `FG-NAM-1L` (NAMASTEA 1L, −42.2, forecast 484.8/14d), `FG-NAM-500ML` (−20.9, forecast 197.7), `FG-REV-500ML` (REVIVE 0.5L, −15.5, forecast 128.8), `FG-FRE-1L-NS` (FRESH 1L NO SUGAR, −14.4, forecast 155), `FG-DET-1L-NS` (DETOX 1L NO SUGAR, −35.5, forecast 302.7) | forecast-driven, not committed — confirmed by `dated_pickups_next14d=0` (all 1,277 open orders are dateless backlog). 🟡 watch, not urgent; no committed order at risk so **no `production_plan` draft written** (V3/C4 — correct per Stage 3 rule). |
| PO-2026-00263 (RAW-NANA) | open PO line missing expected delivery date | purchase-session `ecad3ef0`'s own warnings: 5.00 units on a PARTIAL line, no `expected_receive_date` set → engine will not count it as incoming supply and may recommend re-buying stock already on order | quick fix — set the date, not a new order |
| RM/PKG count trust | of 30 in-scope count targets: 11 fresh, 15 stale, 4 never counted, oldest gap **77 days** | purchase-session `ecad3ef0` `input_integrity.counts` | caveats confidence in every on-hand number feeding this run's Stage 2 totals; schedule counts before the next placement round |
| Yesterday (Mon 07-27) production | off-plan but not a V7 violation | `production_plan`: 0 firmed batches for 07-27; `production_actual`: 6 reports, 995 units logged anyway | no firmed plan existed to compare against — not the "planned, no report" red flag; flagging as a process note for Thursday's retro (why was nothing firmed for a day with real output?) |
| Findings-log gap, 07-26/07-27 | Make delivery fired and succeeded both days (executions `047ebf44`/07-26 16,037B and `08837c48`/07-27 27,570B, both status=1) but **no findings-log entry was committed** for either day | `mcp__Make__executions_list` scenario `6439326`, live this session | same delivery-succeeds/log-commit-optional gap already flagged on 07-24 (V4/Stage-4 checklist item) — recurring, not yet fixed process-side; not fabricating those two days' numbers here, just recording the gap |

Gauges: G1 **45%** 🟡 (67 items projected, 37 with a stockout day, committed=0 across all). G2 **36%** 🔴 (141 in netting, 90 net-short, earliest bucket already past). G3 **100%** 🟢 (no firmed plan yesterday to miss).

Delivery evidence (V4, observed this run): rebuild_verifier=**0**; FG via `fn_compute_daily_fg_projection`; RM via `planning_run_component_netting` run `2ad307b8`; email POSTed to Make webhook — see chat summary for HTTP status this run; findings-log entry committed same run (closing the gap noted above for today at least).

## 2026-07-29 (daily 06:30 IL run; scheduled trigger; rebuild_verifier=0 🟢; forecast c7e9db2a age 6d 🟡; planning run `2ad307b8` age 6d — stale, Thursday 30/7 ritual will refresh it)

Verdict 🔴 דחוף (URGENT): Dennis's iron rule fired for real this run — a firmed 500 L `DETOX 1L` batch planned for yesterday (07-28) has **zero** production report against it.

| item | signal | evidence (live SQL 2026-07-29) | direction |
|---|---|---|---|
| `FG-DET-1L` (V7 fired) | firmed plan, no matching production report | `production_plan` plan `2397a73a-1de7-4adb-8d09-e139759fbd1d`: 500 units planned 07-28, status `planned`, `completed_submission_id` null. `production_actual` for `FG-DET-1L` around that date: only one row, `da9ce7fb…`, 226 units, dated **07-27**, linked to a different plan (`fc45aac2…`, plan_date 07-26) — nothing at all for the 07-28 plan. The 2 reports that *did* land on 07-28 (20 units total) are `FG-MAT-500G` (Matcha 0.5kg), linked to their own completed 07-27/07-28 matcha plans and matching those exactly — unrelated to DETOX. | this is the report-exists check (Stage 0.5.1) firing, not just a quantity gap — nobody entered a production report for the DETOX tank at all. Needs Dennis/floor follow-up before trusting today's numbers on that line. |
| RM/PKG coverage (systemic, unchanged direction) | still 90 of 141 netted components net-short; netting run `2ad307b8` now **6 days old**, `earliest_short_bucket` remains 2026-07-20 (9 days stale) | worst lines unchanged from 07-28: `PKG-CAP-PLASTIC-28` −233,872.3, `PKG-BOTTLE-500ML` −79,703.9, `RAW-SUGAR` −39,551.7, `PKG-BOTTLE-MUZ-1L` −23,578.5, `PKG-CAP-MUZ-1L` −22,410.5 | same escalation as 07-28 — no new purchase-session draft written (session `ecad3ef0`, opened 07-26, still open; V2/C1 draft discipline). Thursday 30/7 ritual is tomorrow and is the right vehicle to refresh this. |
| FG sell-coverage (14d, `2026-07-29`..`2026-08-11`) | 37 of 67 FG items project a stockout day; `demand_lionwheel_qty`=0 for every one (confirmed again: `dated_pickups_next14d`=0, all 1,293 open orders dateless) | largest: `FG-MAT-18G` −62.2 (forecast 517.8/14d), `FG-DET-1L` −48.8 (first short 08-03, forecast 562.9), `FG-NAM-1L` −42.2 (forecast 463.8) | forecast-driven only, no committed order at risk → correctly **no** `production_plan` draft written (V3/C4). |
| PO-2026-00263 (`RAW-NANA`) | still open, still missing expected-receive date | purchase-session `ecad3ef0`'s own warnings, unchanged since 07-28: 5.00 units, `PARTIAL` line, no `expected_receive_date` | quick fix, not a new order — set the date so the engine stops treating it as absent supply. |
| RM/PKG count trust | unchanged: 11 fresh / 15 stale / 4 never-counted of 30 targets, oldest gap 77 days | `purchase_session.input_integrity.counts` (session `ecad3ef0`) | still caveats every on-hand number feeding Stage 2; schedule counts before the Thursday ritual if possible. |

Gauges: G1 **45%** 🟡 (unchanged from 07-28 — forecast, not calendar-driven, doesn't move day to day). G2 **36%** 🔴 (unchanged count, netting run now 6 days stale). G3 **4%** 🔴 (V7: `production_plan` planned_qty 500 vs `production_actual` matched output 0 for the DETOX plan; the 20 units reported yesterday belong to an unrelated, already-completed matcha plan).

Delivery evidence (V4, observed this run): rebuild_verifier=**0**; FG via `fn_compute_daily_fg_projection`; RM via `planning_run_component_netting` run `2ad307b8`; email POSTed to Make webhook → **HTTP 200 "Accepted"**; Make execution `378d8d76fdbd43c2ba0ded40d86a993e` (2026-07-29T03:38:58.390Z), status=1 success, 2 ops, 28,172 B — matches this run's payload; delivered to tom@gteveryday.com via scenario `6439326` → Gmail `sendAnEmail`.

## 2026-08-06 (daily 06:30 IL run; scheduled trigger; rebuild_verifier=0 🟢; forecast c7e9db2a age 14d 🟡; planning run `2ad307b8` age 14d — stale, three planning rituals have now passed without a refresh)

**Correction issued mid-run — logged here in full for the audit trail.** This run's first email (sent 03:42:07Z, HTTP 200, Make execution `cdb27e407b584caebe1b416cbf931772`) led with the claim "the guardian's log fell silent for 8 days" based on the `git log` gap alone, without first checking whether the daily email itself had actually gone out. It had. A follow-up check against `mcp__Make__executions_list` (scenario `6439326`) and `mcp__Gmail__search_threads` showed real, full-payload "בדיקת בוקר" emails delivered to tom@gteveryday.com on **07-30, 07-31, 08-03, 08-04, 08-05** (each independently confirmed by message ID and matching Make execution timestamp/size) — so Tom's inbox was *not* dark for 8 days. A corrective email was sent at 03:45:34Z (Make execution triggered same call, HTTP 200, Gmail thread `19fd52d28f8af31f`) retracting the "silent" framing and restating the real, verified findings below. Root cause of the false claim: checking `data/forecast-findings-log.md` git history alone and treating a documentation gap as a delivery gap — a due-diligence miss, not a fabricated number (every figure used was real, just mis-attributed to the wrong failure). Recorded per the ABSOLUTE RULE (no fabrication) — the miss gets logged, not buried.

| item | signal | evidence (live SQL / Make / Gmail, 2026-08-06) | direction |
|---|---|---|---|
| (production, live/open) DETOX 500L batch, plan `d93b31c7-8666-491a-9120-9e187981a446` | firmed batch entered production **2 days ago**, still open | `production_plan`: `plan_date=2026-08-04`, `planned_qty=500`, `status='in_production'`, `completed_submission_id` null. Tom's own note on the row: "pulled from 05/08 ... 0.5L rationed to 180 bottles, PKG-BOTTLE-500ML supplier out." `production_actual` for 2026-08-04 shows only 3 `FG-MAT-500G` (matcha) reports — nothing for this batch, on 08-04 **or since**. First flagged (V7, zero report) in the 08-05 06:41 email; still open now. | this is the real "no report" story this run — not a guardian-silence story. Needs a floor check today: is the tank still running, or was it finished and just never closed out in the portal? |
| (procurement, systemic, unchanged) RM/PKG netting `2ad307b8` | now **14 days** stale — three planning rituals (Thu 07-30, Wed 08-05, and today's Thu placement day) have passed without a refresh | `planning_run_component_netting`: 141 in netting, 90 net-short (64%), `earliest_short_bucket` still `2026-07-20`. Same absolute totals reported 07-24 through 08-05 (`PKG-CAP-PLASTIC-28` −233,872, `PKG-BOTTLE-500ML` −79,704, `RAW-SUGAR` −39,552, `PKG-BOTTLE-MUZ-1L` −23,579, `PKG-CAP-MUZ-1L` −22,411) — the *numbers* haven't moved because the run hasn't moved, not because the shortage resolved. Already flagged in the 08-03 (11d), 08-04 (12d), and 08-05 (13d) emails, each asking for a fresh run before trusting these for buying decisions. | escalating again — recommend running a fresh `plan-production-14d` planning pass before today's placement-queue actions (queue-guard fires 15:50). |
| (infrastructure, real but narrower than first framed) findings-log git commits | `data/forecast-findings-log.md` has no entry between 07-29 and today, despite the daily email firing correctly every day in between | `git log --all` (this repo) vs `mcp__Make__executions_list` scenario `6439326`: 8 full-payload successful executions 07-30→08-05 (17.5–35.6 KB each, consistent with the real branded template), one 405-byte near-empty payload at 08-03T15:29:44Z (same shape as prior known "empty send" incidents), and 08-02 (Sunday) correctly carried the unified `sunday-prep`/`weekly-opening` email instead (by design, not a bug — flagged as a false alarm in the 08-05 email, corrected here). | this is a documentation/audit-trail gap only — Tom was informed daily regardless. Still worth fixing: the guardian's Stage 4 (commit findings-log) keeps getting dropped even when Stage 5 (send) succeeds, across at least 3 separate incident write-ups now (07-24, 07-28, 08-05) without a structural fix landing. |
| FG sell-coverage (14d, `2026-08-06`..`2026-08-19`) | 22 of 56 FG items project a stockout day; `demand_lionwheel_qty=0` for every one (`dated_pickups_next14d=0`, all 1,431 open orders dateless backlog) | `fn_compute_daily_fg_projection`: largest gaps `FG-NAM-1L` NAMASTEA 1L (−42.2, forecast 421.8/14d, first short 08-11), `FG-MAT-18G` MATCHA 18G (−17.3, forecast 622.3), `FG-DET-1L-NS` DETOX 1L NO SUGAR (−24.5, forecast 280.5), `FG-CAL-1L` CALM 1L (−15.7, forecast 156.8) | forecast-driven only, no committed order at risk → correctly **no** `production_plan` draft written (V3/C4, Stage 3 rule). |
| Yesterday (Wed 08-05) production | not V7 — report exists, but off-plan | `production_plan`: 1 firmed batch, 498 planned. `production_actual`: 2 reports, 629 total (+26.3% over plan) — `FG-DET-1L` 384u + `FG-DET-500ML` 245u, both logged 08-05T08:58Z. | over-production, not a shortfall — still worth a floor check on why the variance wasn't sized to plan. |

Gauges: G1 **61%** 🟡 (56 items projected, 22 short, committed=0 across all). G2 **36%** 🔴 (141 in netting, 90 net-short, run now 14 days stale). G3 **100%** 🟡 (yesterday's plan was met and exceeded — capped display at 100%; real ratio 629/498 — flagged as a variance, not a shortfall, in the exceptions above).

Delivery evidence (V4, observed this run): rebuild_verifier=**0**; FG via `fn_compute_daily_fg_projection`; RM via `planning_run_component_netting` run `2ad307b8`; **first email** POSTed to Make webhook 03:42:07Z → HTTP 200 "Accepted", Make execution `cdb27e407b584caebe1b416cbf931772` (2 ops, 29,161 B), Gmail thread `19fd52a04f2a2d5b` — contained an overstated hero headline, corrected below; **correction email** POSTed 03:45:34Z → HTTP 200 "Accepted", Gmail thread `19fd52d28f8af31f` (7,484 B payload). Both delivered to tom@gteveryday.com. Cross-run verification: `mcp__Make__executions_list` scenario `6439326` (32 execution rows reviewed back to 2026-06-30) and `mcp__Gmail__search_threads` (5 matching "בדיקת בוקר" threads in the last 7 days, message bodies read for 08-05).

## 2026-08-12 (daily 06:30 IL run; scheduled trigger; rebuild_verifier=0 🟢; forecast `c7e9db2a` age 20d 🔴; planning run `2ad307b8` age 20d — stale, five planning rituals have now passed without a refresh)

Verdict 🔴 דחוף (URGENT): Dennis's iron rule fired again — a firmed 500L `DETOX 1L NO SUGAR` batch planned for yesterday (08-11, Tue) has zero production report against it. Two other criticals open in parallel: Green Invoice auth still failing (401, 3rd occurrence, latest last night 21:00), and the RM/PKG netting pipeline has now been stale for 20 days straight.

| item | signal | evidence (live SQL 2026-08-12) | direction |
|---|---|---|---|
| plan `69424041-3681-4be2-b37c-552eee44f6ad` (V7 fired) | firmed 500L `DETOX NO SUGAR` (bom `BOM-BASE-DET-NS`) planned 08-11, status `planned`, `completed_submission_id` null | `production_plan`: 1 planned batch 08-11, planned_qty=500; `production_actual`: 0 reports, 0 output for 08-11 | Stage 0.5.1 report-exists check firing — needs a floor check + report entry before any further draw from that tank. |
| exceptions (open, critical) | 2 unrelated critical categories open simultaneously | `private_core.exceptions` (status open/new, severity critical/high) = **4** rows: `gi_auth_failure` ×3 (2026-08-06 21:00 ×2, **2026-08-11 21:00** most recent — HTTP 401, token rejected), `forecast_stale` ×1 (created 2026-07-30, `latest_success_at=2026-07-23T14:31:38Z`, now 10088+min over the 10080min/7d warn threshold) | GI: rotate `GREENINVOICE_KEY_ID`/`GREENINVOICE_SECRET` per `docs/runbooks/credential_rotation.md`. Forecast: root cause of the RM/PKG staleness below — nothing has republished since 07-23. |
| RM/PKG netting (systemic, unchanged run) | still 90 of 141 netted components net-short; run `2ad307b8` now **20 days** stale, `earliest_short_bucket` still `2026-07-20` | `planning_run_component_netting` run `2ad307b8` (executed 2026-07-23T21:46:12Z — confirmed still the latest **completed** run via `planning_runs`, no newer completed run exists): `PKG-CAP-PLASTIC-28` −233,872.3, `PKG-BOTTLE-500ML` −79,703.9, `RAW-SUGAR` −39,551.7, `PKG-BOTTLE-MUZ-1L` −23,578.5, `PKG-CAP-MUZ-1L` −22,410.5 (absolute totals unchanged since 07-24 — the numbers haven't moved because the run hasn't). Confirmed there ARE current firmed batches this run should be netting against (08-12, 08-13, 08-16, 08-17, 08-19, 08-20, each 500L `planned`) that the stale run predates entirely. | no purchase-session draft written this run — drafting quantities off a 20-day-stale netting run (that predates six of today's own firmed batches) would itself be an evidence-standard violation (V4). Recommend a fresh `plan-production-14d` pass before trusting Stage 2 for buying decisions; latest purchase-session (`1814d724`, opened 08-08) already inherited a 16-day-old forecast at creation and its `input_integrity.counts` shows 7/14 stale + 3/14 never-counted (oldest gap 86 days) — count trust is degrading in parallel. |
| FG sell-coverage (14d, `2026-08-12`..`2026-08-25`) | 23 of 56 real FG items (`EXCLUDED-NONSTOCK` accounting placeholder excluded, per established treatment) project a stockout day; `demand_lionwheel_qty=0` for every one (`dated_pickups_next14d`=0, all 1,519 open orders dateless backlog) | `fn_compute_daily_fg_projection`: largest gaps `FG-MAT-18G` MATCHA 18G (−62.2, forecast 622.3/14d, first short 08-17), `FG-NAM-1L` NAMASTEA 1L (−42.2, forecast 421.8, first short 08-13), `FG-FRE-1L` FRESH 1L (−39.5, forecast 394.5, first short 08-19), `FG-DET-1L-NS` DETOX 1L NO SUGAR (−28, forecast 280.5, first short 08-20) | forecast-driven only, no committed order at risk → correctly **no** `production_plan` draft written (V3/C4, Stage 3 rule). |

Gauges: G1 **59%** 🟡 (56 real items projected, 23 short, committed=0 across all). G2 **36%** 🔴 (141 in netting, 90 net-short — number unchanged since 07-24 because the underlying run is unchanged; flagged red primarily for pipeline staleness, not a fresh signal). G3 **0%** 🔴 (V7: planned_qty 500 vs actual_output 0 for the 08-11 DETOX plan).

Delivery evidence (V4, observed this run): rebuild_verifier=**0**; FG via `fn_compute_daily_fg_projection`; RM via `planning_run_component_netting` run `2ad307b8` (confirmed still latest completed via `planning_runs` history, 10 rows reviewed back to 2026-05-14); open-exceptions via `private_core.exceptions`; email POSTed to Make webhook 03:40:16Z (payload built via `python3 json.dump`, 28,460 B) → **HTTP 200 "Accepted"**; Make execution `cba29e76a3c5433a9951d4f111e03116` (2026-08-12T03:40:16.863Z), status=1 success, 2 ops, 27,569 B — transfer size matches this run's payload; delivered to tom@gteveryday.com via scenario `6439326` → Gmail `sendAnEmail`. Findings-log entries for 08-07 through 08-11 were not backfilled this run (out of scope for today's loop; daily Make executions for those dates show status=1 success in the list above, so sends were not silently missed — only this log's Stage-4 commit cadence lags, a recurring gap already flagged 07-24/07-28/08-05/08-06).

## 2026-08-13 (daily 06:30 IL run; scheduled trigger; rebuild_verifier=0 🟢; forecast `c7e9db2a` age 21d 🔴; planning run `2ad307b8` age 21d — stale, six planning rituals have now passed without a refresh)

Verdict 🔴 דחוף (URGENT): Dennis's iron rule fired for the **second consecutive day** — a firmed 500L `FRESH NO SUGAR` base batch planned for yesterday (08-12, Wed) has zero production report against it (yesterday's run flagged the same pattern for the 08-11 `DETOX NO SUGAR` batch). RM/PKG netting pipeline now stale 21 days straight; Green Invoice auth failure from 08-11 21:00 still shows open/unresolved with no newer occurrence logged.

| item | signal | evidence (live SQL 2026-08-13) | direction |
|---|---|---|---|
| plan `59e60708-369c-4f28-af75-a95784188fa2` (V7 fired) | firmed 500L `FRESH NO SUGAR` base (bom `BOM-BASE-FRE-NS`), planned 08-12, status `planned`, `completed_submission_id` null. Tom's own note (chat approval 2026-08-06): "FRE-NS pulled to freed 12.08 slot, runout ~18.08." Packs to `FG-FRE-500ML-NS` (200u, 20%) + `FG-FRE-1L-NS` (400u, 80%). | `production_plan`: 1 planned batch 08-12, planned_qty=500; `production_actual`: 0 reports, 0 output for 08-12 | needs a floor check + production report entered before the next FRE-NS draw — Tom's own runout estimate (~18.08) lines up almost exactly with this run's projected first-short days for both packed SKUs (`FG-FRE-500ML-NS` 08-18, `FG-FRE-1L-NS` 08-19), so this miss has a real, dated coverage consequence if not resolved today. |
| RM/PKG netting (systemic, unchanged run) | still 90 of 141 netted components net-short; run `2ad307b8` now **21 days** stale, `earliest_short_bucket` still `2026-07-20`, totals unchanged since 07-24 | `planning_run_component_netting` run `2ad307b8` (executed 2026-07-23T21:46:12Z — confirmed still latest **completed** via `planning_runs`): `PKG-CAP-PLASTIC-28` −233,872.3, `PKG-BOTTLE-500ML` −79,703.9, `RAW-SUGAR` −39,551.7, `PKG-BOTTLE-MUZ-1L` −23,578.5, `PKG-CAP-MUZ-1L` −22,410.5 | still no purchase-session draft written — buying off a 21-day-stale netting run would itself violate the evidence standard (V4). Latest open purchase-session (`1814d724`, opened 08-08, still `open` — now 5 days old) inherited a 16d-old forecast at creation; `input_integrity.counts` = 7/14 stale + 3/14 never-counted (oldest gap 86 days) — recommend a fresh `plan-production-14d` pass before the next buy. |
| exceptions (open, critical) | 4 open, unchanged from 08-12 — no new `gi_auth_failure` occurrence logged overnight | `private_core.exceptions` (status open/new, severity critical/high) = 4 rows: `gi_auth_failure` ×3 (most recent still 2026-08-11 21:00, HTTP 401 — status unchanged, not confirmed resolved, just no newer failure row this run), `forecast_stale` ×1 (`latest_success_at=2026-07-23T14:31:38Z`, now 30240+min over the 7d warn threshold) | GI: still needs `GREENINVOICE_KEY_ID`/`GREENINVOICE_SECRET` rotation per `docs/runbooks/credential_rotation.md` — absence of a new failure row is not evidence it's fixed, only that no further attempt has been logged since 08-11. |
| FG sell-coverage (14d, `2026-08-13`..`2026-08-26`) | 23 of 56 real FG items (`EXCLUDED-NONSTOCK` placeholder excluded) project a stockout day; `demand_lionwheel_qty=0` for every one (`dated_pickups_next14d`=0, all 1,537 open orders dateless backlog) | `fn_compute_daily_fg_projection`: largest gaps `FG-MAT-18G` MATCHA 18G (−62.2, forecast 622.3/14d, first short 08-17), `FG-NAM-1L` NAMASTEA 1L (−42.2, forecast 421.8, first short **today** 08-13), `FG-FRE-1L` FRESH 1L (−39.5, forecast 394.5, first short 08-19), `FG-DET-1L-NS` DETOX 1L NO SUGAR (−28, forecast 280.5, first short 08-20) | forecast-driven only, no committed order at risk → correctly **no** `production_plan` draft written (V3/C4, Stage 3 rule). |

Gauges: G1 **58%** 🟡 (56 real items projected, 23 short, committed=0 across all). G2 **36%** 🔴 (141 in netting, 90 net-short — unchanged since 07-24, flagged red for pipeline staleness). G3 **0%** 🔴 (V7: planned_qty 500 vs actual_output 0 for the 08-12 FRE-NS plan).

Delivery evidence (V4, observed this run): rebuild_verifier=**0**; FG via `fn_compute_daily_fg_projection`; RM via `planning_run_component_netting` run `2ad307b8` (confirmed still latest completed via `planning_runs`); open-exceptions via `private_core.exceptions`; email POSTed to Make webhook 03:40:2x UTC (payload 28,194 B) → **HTTP 200 "Accepted"**; Make execution `a758903b94eb4e30a114b43c6fa8ab13` (2026-08-13T03:40:24.950Z), status=1 success, 2 ops, 27,323 B transfer — matches this run's payload size; delivered to tom@gteveryday.com via scenario `6439326` → Gmail `sendAnEmail`.

## 2026-08-14 (daily 06:30 IL run; scheduled trigger; connector pre-flight passed — Supabase `select 1` ok, Make + Gmail connected; rebuild_verifier=0 🟢; forecast `c7e9db2a` age 22d 🔴; planning run `2ad307b8` age 22d — stale, seven planning rituals have now passed without a refresh)

Verdict 🔴 דחוף (URGENT): Dennis's iron rule fired for the **third consecutive working day** — a firmed 500L `CALM` base batch planned for yesterday (08-13, Thu) has zero production report against it (preceded by 08-12 `FRESH NO SUGAR` and 08-11 `DETOX NO SUGAR`, same pattern each day). RM/PKG netting pipeline now stale 22 days straight. Green Invoice auth failed again overnight (08-13 21:00) — a **new** occurrence since yesterday's run, now 8+ days unresolved.

| item | signal | evidence (live SQL 2026-08-14) | direction |
|---|---|---|---|
| plan `5b42adeb-c97e-4f20-a64a-21bdc400e4e1` (V7 fired, 3rd consecutive day) | firmed 500L `CALM` base (bom `BOM-BASE-CAL-REG`), planned 08-13, status `planned`, `completed_submission_id` null. Packs to `FG-CAL-500ML` (347u, 34.7%) + `FG-CAL-1L` (327u, 65.3%). | `production_plan`: 1 planned batch 08-13, planned_qty=500; `production_actual`: 0 reports, 0 output for 08-13 | needs a floor check + production report entered today — three straight days of unreported firmed batches (DETOX 08-11 → FRE-NS 08-12 → CALM 08-13) is now a pattern, not a one-off; worth a floor-process conversation, not just a daily nudge. |
| RM/PKG netting (systemic, unchanged run) | still 90 of 141 netted components net-short; run `2ad307b8` now **22 days** stale, `earliest_short_bucket` still `2026-07-20`, totals unchanged since 07-24 | `planning_run_component_netting` run `2ad307b8` (executed 2026-07-23T21:46:12Z — confirmed still latest **completed** via `planning_runs`, 5 most recent rows reviewed): `PKG-CAP-PLASTIC-28` −233,872.3, `PKG-BOTTLE-500ML` −79,703.9, `RAW-SUGAR` −39,551.7, `PKG-BOTTLE-MUZ-1L` −23,578.5, `PKG-CAP-MUZ-1L` −22,410.5 | still no purchase-session draft written — buying off a 22-day-stale netting run would itself violate the evidence standard (V4). Latest open purchase-session (`1814d724`, opened 08-08, still `open` — now 6 days old, 8 draft POs totaling ₪10,370) inherited a 16d-old forecast at creation; recommend a fresh `plan-production-14d` pass before the next buy. |
| exceptions (open, critical) | 5 open — **1 new** `gi_auth_failure` row since yesterday's run | `private_core.exceptions` (status open/new, severity critical/high) = 5 rows: `gi_auth_failure` ×4 (2026-08-06 21:00 ×2, 2026-08-11 21:00, **2026-08-13 21:00 — new**, all HTTP 401), `forecast_stale` ×1 (`latest_success_at=2026-07-23T14:31:38Z`, now 31680+min over the 7d warn threshold) | GI: `GREENINVOICE_KEY_ID`/`GREENINVOICE_SECRET` rotation per `docs/runbooks/credential_rotation.md` still not done — it failed again last night, 8+ days now with zero invoice evidence captured. |
| FG sell-coverage (14d, `2026-08-14`..`2026-08-27`) | 23 of 56 real FG items (`EXCLUDED-NONSTOCK` placeholder excluded) project a stockout day; `demand_lionwheel_qty=0` for every one (`dated_pickups_next14d`=0, all 1,546 open orders dateless backlog) | `fn_compute_daily_fg_projection`: largest gaps `FG-MAT-18G` MATCHA 18G (−62.2, forecast 622.3/14d, first short 08-17), `FG-NAM-1L` NAMASTEA 1L (−42.2, forecast 421.8, first short **today** 08-14), `FG-FRE-1L` FRESH 1L (−39.5, forecast 394.5, first short 08-20), `FG-DET-1L-NS` DETOX 1L NO SUGAR (−28, forecast 280.5, first short 08-19) | forecast-driven only, no committed order at risk → correctly **no** `production_plan` draft written (V3/C4, Stage 3 rule). |

Gauges: G1 **59%** 🟡 (56 real items projected, 23 short, committed=0 across all). G2 **36%** 🔴 (141 in netting, 90 net-short — unchanged since 07-24, flagged red for pipeline staleness). G3 **0%** 🔴 (V7: planned_qty 500 vs actual_output 0 for the 08-13 CALM plan).

Delivery evidence (V4, observed this run): rebuild_verifier=**0**; FG via `fn_compute_daily_fg_projection`; RM via `planning_run_component_netting` run `2ad307b8` (confirmed still latest completed via `planning_runs`); open-exceptions via `private_core.exceptions`; email POSTed to Make webhook 03:41:xx UTC (payload 28,776 B) → **HTTP 200 "Accepted"**; Make execution `6eb70fe70fc643daa3fb41094842a56c` (2026-08-14T03:42:02.418Z), status=1 success, 2 ops, 27,867 B transfer — matches this run's payload size; delivered to tom@gteveryday.com via scenario `6439326` → Gmail `sendAnEmail`.

## 2026-08-15 (motzash `sunday-prep` run, ~20:10–20:25 IL; scheduled trigger, fresh session; connector pre-flight passed — Supabase `select 1` ok, Make + Gmail loaded; rebuild_verifier=0 🟢; planning run `2ad307b8` age 23d 🔴; forecast `c7e9db2a` age 23d 🔴)

Verdict 🔴 דחוף (URGENT). **Sunday's materials are not the problem — the plan/reality gap is.** Sunday 08-16 carries exactly one firmed batch (500L `BOM-BASE-FRE-REG` → `FG-FRE-1L` ×400 + `FG-FRE-500ML` ×200) and **every one of its 14 material lines is covered** (tank base 5/5, packaging 9/9). The two real reds are (a) three firmed batches from this past week with zero production reports, and (b) `FG-NAM-1L` sitting at −31 units while 5 open orders depend on it and the next NAMASTEA tank is not until Thu 08-20.

| item | signal | evidence (live SQL 2026-08-15) | direction |
|---|---|---|---|
| production reports (V7, 3 batches) | three firmed 500L batches still `status='planned'`, `completed_submission_id` null, with **zero** `production_actual` rows: 08-11 `BOM-BASE-DET-NS`, 08-12 `BOM-BASE-FRE-NS`, 08-13 `BOM-BASE-CAL-REG` — 1,500L unreported | `production_plan` 08-10..08-13: 4 planned rows × 500L. `production_actual` for 08-09..08-15: **only 08-10** has reports (3 reports, 612 units output). 08-11/12/13 = 0 reports, 0 output. | Dennis's iron rule ("no exit before the production report is entered") must close before Sunday's first draw. Three consecutive unreported days — already flagged individually on the 08-12/13/14 daily runs, now confirmed as an unbroken run, i.e. a floor-process gap, not three one-offs. |
| `FG-NAM-1L` NAMASTEA 1L | on_hand **−31.0** (negative), blocking 5 open orders totalling 54 open units — incl. מתן בארי אילת (42 u), נונומימי פ״ת (6 u), קפה מרי ראשל״צ (3 u), קפה ספסל ת״א (2 u), Yami חיפה (1 u) | `current_balances`: `FG-NAM-1L` = −31.0, `last_event_at` 2026-08-13 14:47:47Z, `last_refreshed_at` 2026-08-13 15:36:22Z. Per-order coverage join over `orders_mirror`/`orders_mirror_lines`. Next NAM batch in `production_plan` = **2026-08-20** (Thu, `BOM-BASE-NAM-REG`). Sunday produces FRESH, not NAMASTEA. | Tom decision needed: pull a NAMASTEA tank forward into the Sunday/Monday slot, or accept that 5 orders wait until 08-20. Guardian did **not** draft a re-plan — see the drafts row below. |
| Sunday materials (the actual prep question) | **all covered**, no shortage for Sunday morning | Tank `BOM-BASE-FRE-REG` × 500L vs `current_balances`: RAW-LEMON-ACID need 0.98 / have 11.07; RAW-HIBISCUS 27.45 / 82.14; RAW-LIME-PUREE 10.78 / 80.28; RAW-SUGAR 171.57 / 3,674.83; RAW-WATER 392.16 / plentiful. Pack `BOM-PACK-FRE-1L` ×400 + `BOM-PACK-FRE-500ML` ×200: PKG-BOTTLE-1L 400 / 1,683; PKG-BOTTLE-500ML 200 / 414; PKG-CAP-PLASTIC-28 400 / 10,922; PKG-CAP-PLASTIC-28-500ML 200 / 12,500; PKG-LABEL-FRE-1L 400 / 5,856; PKG-LABEL-FRE-500ML 200 / 2,780; PKG-CARTON-1L 66.7 / 1,322.7; PKG-CARTON-500ML 33.3 / 1,497.7. Tightest margin is PKG-BOTTLE-500ML at +214. | 🟢 no material blocks Sunday. Caveat carried forward: RAW-HIBISCUS and both FRESH labels were last counted **2026-08-03** (12 days) — coverage is real but rests on a 12-day-old count. |
| purchase queue (zombie POs) | 2 POs `APPROVED_TO_ORDER`, approved 2026-08-09, **9 days in queue, never placed** | `PO-2026-00304` — א.ק שוק הפיצוחים, ₪3,398 gross, `RAW-APPLE-DRY`, `expected_receive_date` 2026-08-13 → shows 2 days overdue on a PO that was never actually placed. `PO-2026-00299` — ריסטרטו, `RAW-LIME-PUREE` + `RAW-LYCHEE-PUREE`, no `expected_receive_date`, ₪0 gross recorded. | Neither blocks Sunday (lime purée on hand 80.28kg vs 10.78 needed). Still the zombie-PO pattern the Thursday `queue-guard` exists to kill — it did not clear this week. First task for Dorin Sunday 09:00. |
| FG sell-coverage (14d, 08-15..08-28) | 24 of 57 items project a stockout day (23 of 56 excluding the `EXCLUDED-NONSTOCK` placeholder); `demand_lionwheel_qty` = **0.0** across the entire horizon; forecast demand 4,669.1 | `fn_compute_daily_fg_projection('2026-08-15','2026-08-28')`. Largest: `FG-MAT-18G` −62.2 (fc 622.3, first short 08-17), `FG-NAM-1L` −42.2 (fc 421.8, first short **today**), `FG-FRE-1L` −39.5 (fc 394.5, first short 08-20), `FG-DET-1L-NS` −28.0 (fc 280.5, first short 08-19). | Forecast-only → 🟡 per the documented committed=0 treatment. Note the engine's `FG-NAM-1L` gap and the −31 balance are the same item seen two ways; the balance is the operative fact because it already blocks picking. |
| open orders / weekend intake | 29 truly open orders (25 `UNASSIGNED` + 4 `ASSIGNED`), **all 29 dateless**; only **1** arrived since Thu 14:00 IL | `orders_mirror` (retired_at null, `lw_status` in UNASSIGNED/ASSIGNED/ACTIVE) = 29, `pickup_at is null` = 29, `dated_open` = 0. New since Thu 14:00: `27038778` בבקה כיכר הבימה ת״א (DET-1L-NS ×6, FRE-1L-NS ×6, MAT-18G ×1), created 2026-08-13 15:45 IL. Mirror freshness `max(updated_at)` = 2026-08-15 17:00:19Z (fresh, ~20:00 IL). | Quiet weekend, as expected for Fri/Sat. **Scoping correction:** earlier entries in this log quoted "1,519 / 1,537 / 1,546 open orders" — that is `retired_at is null` with **no status filter**, which includes 1,350 COMPLETED + 87 CANCELED + 80 ROUNDTRIP_DELIVERED. Total non-retired today = 1,546; genuinely open = **29**. Prior figures were mis-scoped, not a collapse in volume. |
| negative balances (stock trust) | 6 items below zero | `ADD-ODK-STR-1L` −12, `GT-MAT-KIT` −4, `ADD-UBE-500G` −4, `AP-DRI-COC-CHI` −1, plus `FG-NAM-1L` −31 and the `EXCLUDED-NONSTOCK` placeholder −518. `rebuild_verifier()` = **0**, so ledger and projection agree — these are real ledger states (unposted receipts / oversell), not a projection fault. | Each negative is an unposted receipt or an oversell needing a floor check. Not a Sunday blocker except NAMASTEA. |
| open critical exceptions | 5 open critical/high | `private_core.exceptions`: `gi_auth_failure` ×4 (HTTP 401; 3 under "Green Invoice auth failed (HTTP 401)" latest **2026-08-13 21:00**, 1 legacy-titled from 08-06), `forecast_stale` ×1 (opened 2026-07-30). | Green Invoice credential rotation (`docs/runbooks/credential_rotation.md`) still not done — failed again 08-13, now 9 days with no supplier-invoice evidence captured. |

**Drafts written this run: NONE — deliberately.** Per C1/C4/V3 the trigger for a `production_plan` draft is committed demand inside the horizon that the firmed plan does not cover. Committed demand (`demand_lionwheel_qty`) is **0.0 across all 14 days** — all 29 open orders are dateless staged backlog, engine-blind by design — so no committed order is uncovered and the draft condition never fires. The two real problems this run are (a) three production reports that were never entered and (b) a negative NAMASTEA balance; **a draft plan row creates neither a production report nor raw material**, so writing one would add noise, not coverage. No purchase-session draft either: quantities derived from a 23-day-stale netting run (`2ad307b8`, which predates every one of this week's firmed batches) would themselves violate the evidence standard (V4). Tom's `TEAEDD:%`/firmed rows untouched.

**Wave-1 pick preview (Sunday route = מרכז, Maxim 07:30):** 7 center-zone orders are fully dispatchable from current FG stock, verified free of contention when picked together — `25315254` אליטה אופק ת״א, `26470734` מוזה קוקטיילים רמת השרון, `26863679` אליטה אופק ת״א, `27026188` קפה גינה ר״ג, `27026583` קפה אנסטסיה ת״א, `27027378` בבקה הוד השרון, `27038778` בבקה כיכר הבימה ת״א. Aggregate check over the 7 leaves every line ≥ 0; tightest are `FG-MAR-CLA-300ML` (132 needed / 132 on hand → 0 left) and `FG-MAT-500G` (3 / 3 → 0 left), so those two must be picked exactly and cannot absorb an eighth order. `26991132` פרש קיטשן חדרה is also fully dispatchable but is not on the מרכז route.

Gauges: G1 **59%** 🟡 (56 real FG items projected, 23 short, committed=0 across all). G2 **100%** 🟢 — *scoped to Sunday's firmed batch materials* (14/14 lines covered), not the repo-wide netting run, which remains 23 days stale and is reported as its own exception rather than folded into this gauge. G3 **25%** 🔴 (4 firmed batches 08-10..08-13, only 08-10 reported).

Delivery evidence (V4, observed this run): `rebuild_verifier()` = **0**; FG via `fn_compute_daily_fg_projection`; Sunday explosion via `bom_lines`⋈`bom_version` (both `status='ACTIVE'`) vs `current_balances`; POs via `purchase_orders`⋈`suppliers` (note `suppliers.supplier_name` does not exist — the live column is `supplier_name_short`/`supplier_name_official`; sql_library callers should use those). Email built from `references/email_template.html` (leading author comment stripped, sent from `<!DOCTYPE html>`; 6 exception rows + 3 action rows; zero unfilled `{{TOKEN}}` asserted before send), POSTed to the Make webhook → **HTTP 200 "Accepted"**, 35,067 B uploaded; Make execution **`620416c657544227b136a84bcacf42b0`** (2026-08-15T17:12:05.107Z), status=1 success, 2 ops, 33,967 B transfer — matches this run's payload; scenario `6439326` → Gmail `sendAnEmail` → tom@gteveryday.com, subject `GT Factory OS · טיוטת ראשון · 2026-08-16`. Gmail fallback draft not needed (webhook returned 200). Step-6 `weekly-opening` chain **not run this session** — this fire was scoped by its trigger prompt to the sunday-prep loop and its own email; flagging the gap rather than silently claiming the unified מוצ״ש email went out.

## 2026-08-17 (daily 06:30 IL run; scheduled trigger; connector pre-flight passed — Supabase `select 1` ok, Make + Gmail connected; rebuild_verifier=0 🟢; forecast `c7e9db2a` age 25d 🔴; planning run `2ad307b8` age 25d — stale, eight+ planning rituals have now passed without a refresh)

Verdict 🔴 דחוף (URGENT): Dennis's iron rule fired for the **fourth time in the last week** — a firmed 500L `FRESH` (regular) base batch planned for yesterday (08-16, Sun) has zero production report against it (preceded by `DETOX NO SUGAR` 08-11, `FRESH NO SUGAR` 08-12, `CALM` 08-13 — this is now a recurring pattern, not an isolated miss). RM/PKG netting pipeline and forecast publication both stale 25 days straight. Green Invoice auth failure unchanged since 08-13 (no new occurrence logged, which is not evidence it's fixed). New this run: a spot-check of current live stock against the stale netting run's "short" list shows the netting numbers no longer track reality, and a previously-approved PO has quietly passed its expected-receive date without being placed.

| item | signal | evidence (live SQL 2026-08-17) | direction |
|---|---|---|---|
| plan `8288399d-7b23-4667-8556-a0440135a608` (V7 fired, 4th occurrence in a week) | firmed 500L `FRESH` base (bom `BOM-BASE-FRE-REG`), planned 08-16, status `planned`, `completed_submission_id` null. Packs to `FG-FRE-1L` (400u, 80%) + `FG-FRE-500ML` (200u, 20%). | `production_plan`: 1 planned batch 08-16, planned_qty=500; `production_actual`: 0 reports, 0 output for 08-16 | needs a floor check + production report entered today. Four unreported firmed batches in seven calendar days (DETOX-NS 08-11 → FRE-NS 08-12 → CALM 08-13 → FRESH-REG 08-16) is a structural process gap, not bad luck — worth a floor-process conversation with Dennis, not just another daily nudge. |
| RM/PKG netting — data-quality escalation (new this run) | still 90 of 141 netted components net-short per run `2ad307b8`, now **25 days** stale; **spot-checked against live `current_balances` this run and found materially wrong**: top "short" components (`PKG-LABEL-CON-500ML` net −4,716.9, `PKG-BOTTLE-500ML` net −79,703.9, `PKG-CAP-PLASTIC-28` net −233,872.3) show live on-hand of 2,000 / 414 / 10,922 units respectively — healthy-looking stock, not the shortage the stale run implies | `planning_run_component_netting` run `2ad307b8` (executed 2026-07-23T21:46:12Z, confirmed still latest completed via `planning_runs`) cross-checked against `current_balances` (refreshed 2026-08-03 to 2026-08-10 depending on component — also stale, but far fresher than the 07-23 run). | **no purchase-session draft written this run, explicitly because of this check** — buying off a 25-day-stale netting run whose top line items no longer match live stock would be a real evidence-standard violation (V4), not just a staleness caveat. This is stronger evidence than prior days' generic "run is stale" flag: the numbers are now demonstrably wrong, not just old. Fresh `plan-production-14d` run is now overdue by a wide margin. |
| exceptions (open, critical) | 5 open, unchanged from 08-14 — no new `gi_auth_failure` occurrence logged since 08-13 21:00 | `private_core.exceptions` (status open/new, severity critical/high) = 5 rows: `gi_auth_failure` ×4 (2026-08-06 21:00 ×2, 2026-08-11 21:00, 2026-08-13 21:00 — still the most recent), `forecast_stale` ×1 (`latest_success_at=2026-07-23T14:31:38Z`, now 36,000+ min over the 7d/10,080min warn threshold) | GI: `GREENINVOICE_KEY_ID`/`GREENINVOICE_SECRET` rotation per `docs/runbooks/credential_rotation.md` still not done — 4 days since the last failed attempt was logged, which only means nothing has tried since, not that it's fixed. |
| `PO-2026-00304` (new finding) | `APPROVED_TO_ORDER`, expected_receive_date `2026-08-13` — **already 4 days past due** and still not placed | `purchase_orders`: `po_id=PO-2026-00304`, supplier `SUP-039`, `total_gross=₪3,398.4`, `order_date=2026-08-09`, created `2026-08-06`. Sibling `PO-2026-00299` (supplier `SUP-012`, no total/date set) also still `APPROVED_TO_ORDER`, same creation date. | 2 POs sitting in the placement queue 11 days; one has already missed its own expected-receive date. This is exactly the pattern the Thursday `queue-guard` sweep exists to catch — flagging here since it's real and evidence-based, even though today isn't a queue-guard fire. |
| FG sell-coverage (14d, `2026-08-17`..`2026-08-30`) | 23 of 56 real FG items (`EXCLUDED-NONSTOCK` placeholder excluded) project a stockout day; `demand_lionwheel_qty=0` for every one (`dated_pickups_next14d`=0, all 1,584 open orders dateless backlog) | `fn_compute_daily_fg_projection`: largest gaps `FG-NAM-1L` NAMASTEA 1L (−73.2, forecast 421.8/14d, first short **today** 08-17), `FG-MAT-18G` MATCHA 18G (−62.2, forecast 622.3, first short 08-18), `FG-DET-1L-NS` DETOX 1L NO SUGAR (−28, forecast 280.5, first short 08-20), `FG-FRE-1L` FRESH 1L (−39.5, forecast 394.5, first short 08-23 — same base as today's V7 batch) | forecast-driven only, no committed order at risk → correctly **no** `production_plan` draft written (V3/C4, Stage 3 rule). |
| RM/PKG count trust | latest open purchase-session (`1814d724`, opened 08-08, still `open` — now 9 days old) `input_integrity.counts`: 4/14 fresh, 7/14 stale, 3/14 never-counted, oldest gap 86 days | `purchase_session.input_integrity` jsonb | count trust keeps degrading in parallel with the stale netting run — reinforces that Stage 2 numbers need a fresh planning + count pass together, not just a re-run. |

Gauges: G1 **59%** 🟡 (56 real items projected, 23 short, committed=0 across all — `dated_pickups_next14d`=0 confirmed again). G2 **36%** 🔴 (141 in netting, 90 net-short by the stale run's count — flagged red for pipeline staleness *and*, new this run, confirmed-wrong top-line numbers vs live balances). G3 **0%** 🔴 (V7: planned_qty 500 vs actual_output 0 for the 08-16 FRESH plan).

Findings-log gap note: Make execution history (scenario `6439326`) shows real, non-empty sends on 08-15 (17:12 UTC, 33,967 B — the `sunday-prep`/`weekly-opening` slot) and 08-16 (06:14 UTC, 17,368 B) that were never backfilled into this log — the same recurring Stage-4 commit-cadence gap already flagged 07-24/07-28/08-05/08-06/08-12. Not re-investigated in depth this run (out of scope for today's loop); noted so the gap doesn't read as an unflagged blind spot.

Delivery evidence (V4, observed this run): rebuild_verifier=**0**; FG via `fn_compute_daily_fg_projection`; RM via `planning_run_component_netting` run `2ad307b8` (confirmed still latest completed via `planning_runs`) cross-checked against `current_balances`; open-exceptions via `private_core.exceptions`; PO queue via `purchase_orders`; email POSTed to Make webhook 03:42:0xZ (payload 34,049 B) → **HTTP 200 "Accepted"**; Make execution `351dda759a674bbf9efac98ba3908166` (2026-08-17T03:42:06.207Z), status=1 success, 2 ops, 32,944 B transfer — consistent with this run's real payload size, not the known 405B empty-send pattern; delivered to tom@gteveryday.com via scenario `6439326` → Gmail `sendAnEmail`.
## 2026-08-18 (daily 06:30 IL run; connector pre-flight passed — Supabase `select 1` ok, Make + Gmail connected; rebuild_verifier=0 🟢; forecast `c7e9db2a` age 26d 🔴; planning run `2ad307b8` age 26d — stale)

**Gap since last run: no findings-log entry for 08-15 (Fri), 08-16 (Sun) or 08-17 (Mon)** — last committed entry was 08-14 (#133). **Checked live this run, correcting the initial read:** `mcp__Make__executions_list` on scenario `6439326` shows the guardian's email step *did* fire and succeed all 3 days — `620416c6…` (2026-08-15T17:12:05Z, status=1, 33,967 B), `0fb9f897…` (2026-08-16T06:14:05Z, status=1, 17,368 B), `351dda75…` (2026-08-17T03:42:06Z, status=1, 32,944 B) — so Tom received all 3 emails and this is **not** the pre-V9 connector-dead silent-failure pattern. The gap is narrower: those 3 runs never reached, or failed silently at, the Stage 4 git commit+push step — confirmed via `git log --all --since/--until` across every branch for that window (only one unrelated commit, #127; nothing from the guardian). Root cause not yet identified; recommend checking whether those 3 headless sessions had repo write access, or whether Stage 4 threw and was swallowed.

Verdict 🔴 דחוף (URGENT): Dennis's iron rule fires again — a firmed 500L `CONSCIOUSNESS REGULAR` base batch planned for yesterday (08-17, Mon) has zero production report against it. New this run: a `lionwheel_stale` critical exception (mirror sync gap), not present in the 08-13/08-14 logs.

| item | signal | evidence (live SQL 2026-08-18) | direction |
|---|---|---|---|
| plan `27ea28d0-7611-4a77-a4da-12210736e1b9` (V7 fired) | firmed 500L `CONSCIOUSNESS REGULAR` base (bom `BOM-BASE-CON-REG`), planned 08-17, status `planned`, `completed_submission_id` null. | `production_plan`: 1 planned batch 08-17, planned_qty=500; `production_actual`: 0 reports, 0 output for 08-17 | needs a floor check + production report entered today. Packs toward `FG-CON-500ML`, which Stage 1 below shows first-short **today** (08-18) at −6 units — this miss has an immediate, dated coverage consequence. |
| gap in guardian runs (git only, not email) | no findings-log commit for 08-15/08-16/08-17, but all 3 days' emails confirmed sent via Make | `git log --all`: last guardian commit before this run = `aeb0246` (2026-08-14); `mcp__Make__executions_list` scenario `6439326`: 3 successful executions in the gap window (see note above table) | recommend Tom (or the next guardian run) check whether the 08-15/16/17 sessions had repo write access at all — email delivery + SQL both worked, only the git-append step is missing, so this is a narrower bug than a full connector outage. |
| RM/PKG netting (systemic, unchanged run) | still 90 of 141 netted components net-short; run `2ad307b8` now **26 days** stale, `earliest_short_bucket` still `2026-07-20`, totals unchanged since 07-24 | `planning_run_component_netting` run `2ad307b8` (executed 2026-07-23T21:46:12Z — confirmed still latest **completed** via `planning_runs`): `PKG-CAP-PLASTIC-28` −233,872.3, `PKG-BOTTLE-500ML` −79,703.9, `RAW-SUGAR` −39,551.7, `PKG-BOTTLE-MUZ-1L` −23,578.5, `PKG-CAP-MUZ-1L` −22,410.5 | still no purchase-session draft written — buying off a 26-day-stale netting run would itself violate the evidence standard (V4). Latest open purchase-session (`1814d724`, opened 08-08, still `open` — now 10 days old) has `input_integrity.counts` fresh=4/stale=7/never_counted=3 of 14 targets, oldest count gap 86 days; 2 POs still sitting `APPROVED_TO_ORDER` unplaced since 08-06 (12 days, ₪3,398 total) — recommend a fresh `plan-production-14d` pass before the next buy, and a Doreen placement sweep on the 2 aged POs. |
| exceptions (open, critical/high) | 6 open — **1 new type** since 08-14: `lionwheel_stale` | `private_core.exceptions` (status open/new, severity critical/high) = 6 rows: `gi_auth_failure` ×3 (2026-08-06 21:00, 2026-08-11 21:00, 2026-08-13 21:00, all HTTP 401 — no new occurrence logged since 08-13, not confirmed fixed, just no further attempt row), `forecast_stale` ×1 (`latest_success_at=2026-07-23T14:31:38Z`, now 33840+min over the 7d warn threshold), `lionwheel_stale` ×1 **new** (created 08-17 20:10, `latest_success_at`=08-17 19:37, 33min gap vs 30min threshold) | GI rotation still outstanding. New `lionwheel_stale` exception worth a one-time check that the mirror poller recovered — `current_balances.last_refreshed_at` also reads 08-13 15:36 this run, 5 days stale, consistent with a wider sync-freshness gap this week. |
| FG sell-coverage (14d, `2026-08-18`..`2026-08-31`) | 23 of 56 real FG items (`EXCLUDED-NONSTOCK` placeholder excluded) project a stockout day; `demand_lionwheel_qty=0` for every one (`dated_pickups_next14d`=0, all 1,604 open orders dateless backlog) | `fn_compute_daily_fg_projection`: largest gaps `FG-NAM-1L` NAMASTEA 1L (−73.2, forecast 421.8, first short **today**), `FG-MAT-18G` MATCHA 18G (−62.2, forecast 622.3, first short 08-19), `FG-DET-1L-NS` DETOX 1L NO SUGAR (−28, forecast 280.5, first short 08-23), `FG-FRE-1L` FRESH 1L (−39.5, forecast 394.5, first short 08-24) | forecast-driven only, no committed order at risk (committed=0 across all short items) → correctly **no** `production_plan` draft written (V3/C4, Stage 3 rule). |

Gauges: G1 **59%** 🟡 (56 real items projected, 23 short, committed=0 across all). G2 **36%** 🔴 (141 in netting, 90 net-short — unchanged since 07-24, flagged red for pipeline staleness). G3 **0%** 🔴 (V7: planned_qty 500 vs actual_output 0 for the 08-17 CONSCIOUSNESS plan).

Delivery evidence (V4, observed this run): rebuild_verifier=**0**; FG via `fn_compute_daily_fg_projection`; RM via `planning_run_component_netting` run `2ad307b8` (confirmed still latest completed via `planning_runs`); open-exceptions via `private_core.exceptions`; email POSTed to Make webhook 2026-08-18T03:39:5xZ (payload 28,366 B) → **HTTP 200 "Accepted"** (body "Accepted"); Make execution `74ef186b9878420d9012fb0b67a581ba` (2026-08-18T03:41:19.570Z), status=1 success, 2 ops, 27,480 B transfer — matches this run's payload size; delivered to tom@gteveryday.com via scenario `6439326` → Gmail `sendAnEmail`. Findings-log commit: `44d555d` (+ this correction commit), pushed to `claude/kind-fermat-6uhrff`, PR #140 opened.

## 2026-08-19 (daily 06:30 IL run; scheduled trigger; connector pre-flight passed — Supabase `select 1` ok, Make + Gmail connected; rebuild_verifier=0 🟢; forecast `c7e9db2a` age 27d 🔴; planning run `2ad307b8` age 27d — stale)

**Correction to this run's own first email.** The 08-19 report sent at 03:40 UTC opened with "השומר לא רץ 4 ימים ברצף" (the guardian did not run for 4 days) — that claim is **wrong** and should not have gone out; it was written before checking `mcp__Make__executions_list`/Gmail history, in violation of the no-fabrication rule. Corrected via a same-thread Gmail reply immediately after discovery. **What actually happened, now fully root-caused:** the 08-15 (`sunday-prep`), 08-17, and 08-18 daily runs all executed correctly and delivered real emails (Make executions `620416c6`/`351dda75`/`74ef186b`, all status=1 with full-size payloads) — the 08-18 run had already caught and flagged this as "3 ימים בלי לוג בוקר" and opened PR #140 to fix it. The true root cause, found this run: **every guardian session opens its findings-log commit on a brand-new throwaway branch + draft PR against `main`, and nobody has been merging them** — the same pattern affects most automated PRs in this repo (dozens open, back to 2026-08-06). PRs #134 (08-15), #137 (08-17) and #140 (08-18) sat unmerged with the real findings-log entries inside them. **Action taken this run:** marked all three ready for review, resolved the resulting append-conflicts (each PR was branched before the previous one merged) by ordering the blocks chronologically, and merged all three into `main` — `data/forecast-findings-log.md` on `main` now has an unbroken run from 08-14 through 08-18. One residual, unrecovered gap: **08-16 (Sun)** — a real "GT Scorecard" email went out that morning (Make execution `0fb9f897…`, 06:14 UTC, 17,368 B, status=1) but no PR or commit for a findings-log entry from that date was found anywhere in the repo's PR history; that day's specific figures are not recoverable from git and are logged here as a genuine (not merge-backlog) content gap.

Verdict 🔴 דחוף (URGENT), driven by two systemic issues rather than a new stockout: the PR-merge backlog above (governance/evidence-trail risk — findings existed but were invisible to anyone reading `main`), and the RM/PKG planning pipeline now 27 days stale.

| item | signal | evidence (live SQL 2026-08-19) | direction |
|---|---|---|---|
| production reports (Stage 0.5, yesterday 08-18) | **zero firmed batches planned for 08-18** — nothing to violate V7 against | `production_plan` 08-18: 0 rows `status='planned'`; `production_actual` 08-18: 0 reports. | Not a V7 miss (no plan existed to miss), but worth naming: no production was scheduled at all for Tuesday — consistent with a planning engine that hasn't produced a fresh run in 27 days and nobody hand-firming in its place. |
| RM/PKG netting (systemic, unchanged run) | still 90 of 141 netted components net-short per run `2ad307b8`, now **27 days** stale (executed 07-23), `earliest_short_bucket` still `2026-07-20` — and the 08-17 run already spot-checked these top lines against live balances and found them **materially wrong** (e.g. `PKG-BOTTLE-500ML` shown net-short −79,703.9 vs 414 units actually on hand) | `planning_run_component_netting` run `2ad307b8` (confirmed still latest **completed** via `planning_runs`); `current_balances.last_refreshed_at` = **2026-08-13** (6 days stale) — compounding rather than independent staleness | still no purchase-session draft written — buying off a 27-day-stale, already-proven-wrong netting run would violate the evidence standard (V4) twice over. A fresh `plan-production-14d` pass is now the single highest-leverage action available; every day it's deferred, G2's "36%" reading becomes less meaningful in either direction. |
| purchase queue | session `1814d724` (opened 08-08) still `open`, now **11 days** old, 8 draft POs totalling ₪10,370; separately **2 POs** sitting `APPROVED_TO_ORDER` unplaced (`PO-2026-00304` ₪3,398, past its own `expected_receive_date`; `PO-2026-00299`) | `purchase_session` + `purchase_session_po`; `purchase_orders` where `status='APPROVED_TO_ORDER'` = 2 | today is not a `queue-guard` fire (Thursday), but both items are already aged past a normal cycle — worth a Doreen placement sweep before Thursday's sweep finds them anyway. |
| exceptions (open, critical/high) | 6 open — `lionwheel_stale` (critical, opened 08-18 23:40, describing a **~28-hour** mirror sync gap back to 08-17 19:37) now appears **resolved in practice**: `orders_mirror.updated_at` = 2026-08-19 03:30 UTC, i.e. fresh as of this morning, but the exception row itself is still `status=open` | `private_core.exceptions`: `lionwheel_stale` ×1, `gi_auth_failure` ×4 (unchanged since 2026-08-13 21:00 — no new occurrence in 6 days, still not confirmed fixed), `forecast_stale` ×1 (opened 07-30) | recommend acknowledging/closing the `lionwheel_stale` row now that the mirror has caught up, so the next run's exception count reflects live state rather than a resolved-but-unclosed incident. GI credential rotation still outstanding. |
| FG sell-coverage (14d, `2026-08-19`..`2026-09-01`) | 23 of 56 real FG items (`EXCLUDED-NONSTOCK` placeholder excluded) project a stockout day; `demand_lionwheel_qty=0` for every one (`dated_pickups_next14d`=0, all 1,619 open orders dateless backlog) | `fn_compute_daily_fg_projection`: largest gaps `FG-NAM-1L` NAMASTEA 1L (−73.2, forecast 421.8, first short **today**), `FG-MAT-18G` MATCHA 18G (−62.2, forecast 622.2, first short 08-20), `FG-DET-1L-NS` DETOX 1L NO SUGAR (−28.1, forecast 280.5, first short 08-24), `FG-FRE-1L` FRESH 1L (−39.5, forecast 394.5, first short 08-25) | forecast-driven only, no committed order at risk → correctly **no** `production_plan` draft written (V3/C4, Stage 3 rule). |

Gauges: G1 **59%** 🟡 (56 real items projected, 23 short, committed=0 across all). G2 **36%** 🔴 (141 in netting, 90 net-short — unchanged since 07-24, and independently confirmed wrong by the 08-17 spot-check; flagged red for both staleness and inaccuracy). G3 **100%** 🟢 (no firmed plan yesterday — no V7 gap to measure, not evidence of a clean day).

Delivery evidence (V4, observed this run): rebuild_verifier=**0**; FG via `fn_compute_daily_fg_projection`; RM via `planning_run_component_netting` run `2ad307b8` (confirmed still latest completed via `planning_runs`); open-exceptions via `private_core.exceptions`; email POSTed to Make webhook 03:40:07Z (payload 28,930 B) → **HTTP 200 "Accepted"**; Make execution `5192ee4f3bdb44d39771031470444a70` (2026-08-19T03:40:07.814Z), status=1 success, 2 ops, 28,037 B transfer — matches this run's payload size; delivered to tom@gteveryday.com via scenario `6439326` → Gmail `sendAnEmail`, subsequently corrected via Gmail reply on the same thread (see note above). **This run also merged PRs #134, #137, #140** (findings-log entries for 08-15/08-17/08-18) into `main`, resolving append-conflicts by chronological reordering — merge commits `3b16f5d`, `d93d5a4`, `e2bc9c1`.

## 2026-08-20 (daily 06:30 IL run; scheduled trigger; connector pre-flight passed — Supabase `select 1` ok, Make `users_me` ok, Gmail tool loaded)

**New this run: Stage 0's `rebuild_verifier()` itself failed to execute** — `ERROR 25006: cannot execute TRUNCATE TABLE in a read-only transaction` (the function truncates `current_balances_shadow` internally as part of computing drift; this session's Supabase MCP connection opens read-only transactions). This is a narrower gap than a dead connector (Stage 0 pre-flight `select 1` succeeded, and every other SELECT-only query in this run worked normally) — but it means **no live verifier_drift number exists for today**, only the value confirmed at the end of yesterday's run (`0`, 2026-08-19). Per V1/V4 (no fabricated numbers, 🔴-or-unverifiable ⇒ no drafts), this run treated the gate as failed-closed: **zero drafts written**, and the email footer reports "not available this session" rather than inventing a `0`. Flagging for whoever configures the next session's Supabase MCP connection: if the guardian is expected to keep confirming this live, it needs write-capable (not strict read-only) access to run `rebuild_verifier()`.

Verdict 🔴 דחוף (URGENT): Dennis's iron rule fires **again** — a firmed 500L `DETOX REGULAR` base batch planned for yesterday (08-19, Wed) has zero production report against it. This is the **sixth** unreported firmed batch in 9 calendar days (`DETOX-NS` 08-11, `FRESH-NS` 08-12, `CALM` 08-13, `FRESH-REG` 08-16, `CONSCIOUSNESS-REG` 08-17, now `DETOX-REG` 08-19 — 08-18 had no firmed plan so wasn't a miss). This is a structural floor-process gap, not a string of bad luck.

| item | signal | evidence (live SQL 2026-08-20) | direction |
|---|---|---|---|
| plan `9ab79d6a-a3aa-4d7a-8674-5de2b2f20f63` (V7 fired, 6th occurrence in 9 days) | firmed 500L `DETOX REGULAR` base (`base_bom_head_id=BOM-BASE-DET-REG`), planned 08-19, status `planned`, `completed_submission_id` null. | `production_plan` 08-19: 1 planned batch, planned_qty=500; `production_actual` 08-19: 0 reports, 0 output. | needs a floor check + production report entered today. Six unreported firmed batches in nine days is no longer a nudge-worthy exception — it's a process that isn't working; recommend a direct floor-process conversation with Dennis rather than another daily flag. |
| Stage 0 integrity gate — execution failure (new this run) | `rebuild_verifier()` errored instead of returning a drift value; root cause is this session's read-only Supabase transaction blocking the function's internal `TRUNCATE` on its shadow table | Postgres error `25006` on `select private_core.rebuild_verifier();` — full text in the run notes above | zero drafts written this run as a direct consequence (fail-closed). Last confirmed live value: `0` (2026-08-19 run). Needs the next session's Supabase connection to be write-capable if live confirmation is to resume; otherwise this is a repeatable gap every time a read-only session fires. |
| RM/PKG netting (systemic, unchanged run) | still 90 of 141 netted components net-short per run `2ad307b8`, now **28 days** stale (executed 07-23) — already proven materially wrong vs live balances by the 08-17 spot-check | `planning_run_component_netting` run `2ad307b8` (confirmed still latest **completed** via `planning_runs`): `PKG-CAP-PLASTIC-28` −233,872.3, `PKG-BOTTLE-500ML` −79,703.9, `RAW-SUGAR` −39,551.7, `PKG-BOTTLE-MUZ-1L` −23,578.5, `PKG-CAP-MUZ-1L` −22,410.5 | no purchase-session draft written — buying off a 28-day-stale, already-proven-wrong netting run would violate the evidence standard twice over. A fresh `plan-production-14d` pass remains the single highest-leverage action outstanding. |
| exceptions (open, critical) | 6 open, unchanged categories from 08-19 — `lionwheel_stale` (opened 08-19 04:40, describing a gap back to 08-17 19:37) is **still open** even though the mirror itself recovered (`orders_mirror.updated_at`=2026-08-20 03:30, fresh) — now ~19h unacknowledged since the underlying issue cleared | `private_core.exceptions` (status open/new, severity critical/high) = 6 rows: `lionwheel_stale` ×1 (created 08-19 04:40), `gi_auth_failure` ×4 (2026-08-06 21:00 ×2, 2026-08-11 21:00, 2026-08-13 21:00 — no new occurrence in 7 days, still not confirmed fixed), `forecast_stale` ×1 (opened 07-30, `latest_success_at`=07-23) | recommend closing the `lionwheel_stale` row now that the mirror is current, so open-exception counts reflect live state. GI credential rotation now 2 weeks overdue. |
| purchase queue | session `1814d724` (opened 08-08) — still `open`, now **12 days** old, 8 draft POs totalling ₪10,370; `PO-2026-00299` and `PO-2026-00304` (₪3,398.40, `expected_receive_date` 08-13) both still `APPROVED_TO_ORDER`, unplaced since 08-06 (14 days) | `purchase_session`+`purchase_session_po`; `purchase_orders` where `status='APPROVED_TO_ORDER'` = 2 | `PO-2026-00304` is now **7 days past its own expected-receive date**. Today is not a `queue-guard` fire (that's Thursday 15:50 — today, later), so this will be caught again by that sweep if not actioned first; flagging now so it isn't news then. |
| FG sell-coverage (14d, `2026-08-20`..`2026-09-02`) | 23 of 58 real FG items (`EXCLUDED-NONSTOCK` placeholder excluded) project a stockout day; `demand_lionwheel_qty=0` for every one (`dated_pickups_next14d`=0, all 1,637 open orders dateless backlog) | `fn_compute_daily_fg_projection`: largest gaps `FG-DET-1L` DETOX 1L (−48.8, forecast 487.8, first short 08-27), `FG-NAM-1L` NAMASTEA 1L (−42.2, forecast 421.9, first short 08-25), `FG-NAM-500ML` NAMASTEA 0.5L (−20.9, forecast 208.6, first short 08-24), `FG-FRE-1L` FRESH 1L (−39.5, forecast 394.5, first short **today**) | forecast-driven only, no committed order at risk (committed=0 across every short item) → correctly **no** `production_plan` draft written (V3/C4, Stage 3 rule). |

Gauges: G1 **60%** 🟡 (58 real items projected, 23 short, committed=0 across all). G2 **36%** 🔴 (141 in netting, 90 net-short — unchanged since 07-24, now 28 days stale and previously confirmed wrong). G3 **0%** 🔴 (V7: planned_qty 500 vs actual_output 0 for the 08-19 DETOX REGULAR plan).

Delivery evidence (V4, observed this run): rebuild_verifier could **not** be confirmed live this run (see note above; last confirmed value 0, 2026-08-19) — reported honestly rather than restated; FG via `fn_compute_daily_fg_projection`; RM via `planning_run_component_netting` run `2ad307b8` (confirmed still latest completed via `planning_runs`); open-exceptions via `private_core.exceptions`; PO queue via `purchase_orders`; email POSTed to Make webhook 2026-08-20T03:40:3xZ (payload 31,947 B) → **HTTP 200 "Accepted"**; Make execution `456cd798145f4e1fbea943d3c273e616` (2026-08-20T03:40:38.369Z), status=1 success, 2 ops, 30,926 B transfer — consistent with this run's payload size; delivered to tom@gteveryday.com via scenario `6439326` → Gmail `sendAnEmail`. Zero `production_plan`/purchase-session drafts written this run (V1/V2: unverifiable integrity gate + no committed shortfall independently justify no writes).

## 2026-08-21 (daily 06:30 IL run; scheduled trigger; connector pre-flight passed — Supabase `select 1` ok, Make + Gmail connected; Stage 0 gate resolved via `job_runs`, see note; forecast `c7e9db2a` age 29d 🔴; planning run `2ad307b8` age 29d — stale)

**Stage 0 read-only limitation resolved this run, not just worked around.** Same as 08-20, this session's Supabase MCP connection opens strict read-only transactions, so `select private_core.rebuild_verifier();` still errors (`25006`, internal `TRUNCATE` on the shadow table). New this run: found `private_core.job_runs`, which logs every automated execution of the nightly `cron_nightly` rebuild-verifier job — a pure `SELECT`, safe under read-only. Today's row: `run_id b75b7652…`, `started_at 2026-08-21 03:00:00Z`, `status succeeded`, `output_summary.drift_count=0`, 226 rows processed, 0 exceptions opened/auto-resolved — i.e. a real, live-queried, same-morning integrity result (34 minutes before this session started), not a fabricated or day-old number. Recommend the skill's Stage 0 SQL be updated to read `job_runs` first and only attempt the direct function call as a fallback when a write-capable session is available — this removes the "gate unverifiable" fail-closed path entirely for the common case going forward.

Verdict 🔴 דחוף (URGENT), same three systemic drivers as recent runs, none newly resolved: Dennis's floor-report gap (now 7 occurrences in 10 days), Green Invoice auth still down, and the RM/PKG planning pipeline now 29 days stale.

| item | signal | evidence (live SQL 2026-08-21) | direction |
|---|---|---|---|
| plan `cc96ff6a-5f98-49a7-b1bc-8eff50543c4f` (V7 fired, **7th occurrence in 10 days**) | firmed 500L `NAMASTEA REGULAR` base (`base_bom_head_id=BOM-BASE-NAM-REG`), planned 08-20, status `planned`, `completed_submission_id` null. | `production_plan` 08-20: 1 planned batch, planned_qty=500, `item_id` null (pre-pack base-mix row); `production_actual` 08-20: 0 reports, 0 output. Sequence so far: `DETOX-NS` 08-11, `FRESH-NS` 08-12, `CALM` 08-13, `FRESH-REG` 08-16, `CONSCIOUSNESS-REG` 08-17, `DETOX-REG` 08-19, now `NAMASTEA-REG` 08-20 (08-18 had no firmed plan, not a miss). | this is no longer a daily nudge — 7 of the last ~8 working days with a firmed batch have shipped with zero production report. Repeating the same recommendation as 08-20: this needs a direct floor-process conversation with Dennis, not another automated flag. |
| RM/PKG netting (systemic, unchanged run) | still 90 of 141 netted components net-short per run `2ad307b8`, now **29 days** stale (executed 07-23) — already proven materially wrong vs live balances by the 08-17 spot-check, still not refreshed | `planning_run_component_netting` run `2ad307b8` (confirmed still latest **completed** via `planning_runs` — no run since 07-23, in spite of 4 Wednesday/Thursday rituals having passed: 07-30, 08-06, 08-13, 08-20): `PKG-CAP-PLASTIC-28` −233,872.3, `PKG-BOTTLE-500ML` −79,703.9, `RAW-SUGAR` −39,551.7, `PKG-BOTTLE-MUZ-1L` −23,578.5, `PKG-CAP-MUZ-1L` −22,410.5 — identical to every run since 07-24, confirming the numbers are frozen, not tracking reality | no purchase-session draft written — same evidence-standard reasoning as every run since 07-28. A fresh `plan-production-14d` pass remains the single highest-leverage action outstanding and is now nearly a month overdue. |
| exceptions (open, critical/high) | 5 open (down from 6 — `lionwheel_stale` from 08-19 is gone, mirror recovered and the row was closed) | `private_core.exceptions`: `forecast_stale` ×1 (opened 07-30, `latest_success_at`=2026-07-23T14:31:38Z, 10,088+ min over the 7-day threshold), `gi_auth_failure` ×4 (HTTP 401, first occurrence 2026-08-06 21:00 ×2, then 08-11 21:00 and 08-13 21:00 — **no new occurrence logged since 08-13**, 8 days with no fresh attempt row, still not confirmed fixed either way) | GI credential rotation now **15 days** overdue (`docs/runbooks/credential_rotation.md`). Since no new `gi_auth_failure` row has appeared in 8 days, worth confirming directly whether the integration recovered silently or the health-check itself stopped firing — the exception count alone can't distinguish those. |
| purchase queue | session `1814d724` (opened 08-08) — still `open`, now **13 days** old, unchanged draft-PO total; `PO-2026-00299` (SUP-012 Ristretto, `latest_safe_order_date_snapshot`=08-06, now **15 days past its own safe-order date**) and `PO-2026-00304` (SUP-039 א.ק שוק הפיצוחים, ₪3,398.40, `expected_receive_date`=08-13, now 8 days past due) both still `APPROVED_TO_ORDER`, unplaced since 08-09 | `purchase_session`; `purchase_orders` where `status='APPROVED_TO_ORDER'` = 2; `input_integrity.counts` (session `1814d724`): 4/14 fresh, 7/14 stale, 3/14 never-counted, oldest gap 86 days | today is `queue-guard`'s fire day (Thursday 15:50) — no, today is **Friday**; queue-guard fired yesterday (Thu 08-20) per its own clock, so these 2 POs should already be on Tom's radar from that sweep. If not placed by yesterday's guard, they carry into next week untouched — flagging again so it isn't lost over the weekend. |
| FG sell-coverage (14d, `2026-08-21`..`2026-09-03`) | 23 of 58 real FG items (`EXCLUDED-NONSTOCK` placeholder excluded) project a stockout day; `demand_lionwheel_qty=0` for every one (`dated_pickups_next14d`=0, all 1,644 open orders dateless backlog — up from 1,637 yesterday) | `fn_compute_daily_fg_projection`: largest gaps `FG-DET-1L` DETOX 1L (−48.8, forecast 487.8/14d, first short 08-30), `FG-NAM-1L` NAMASTEA 1L (−42.2, forecast 421.9, first short 08-26), `FG-DET-1L-NS` DETOX 1L NO SUGAR (−28.1, forecast 280.5, first short 08-26), `FG-DET-500ML` DETOX 0.5L (−20.4, forecast 203.3, first short 08-26), `FG-NAM-500ML` NAMASTEA 0.5L (−20.9, forecast 208.6, first short 08-25) | forecast-driven only, no committed order at risk (committed=0 across every short item) → correctly **no** `production_plan` draft written (V3/C4, Stage 3 rule). |

Data-quality footnote (not forecast): `GT-MAT-KIT` (Matcha additional kit, forecast 7.3/14d) and `AP-DRI-COC-CHI` (Coconut Chips, forecast 0/14d) both show `first_short_day=2026-08-21` — today — with tiny magnitudes (4 and 1 units). Low-priority add-on items, not flagged as an action item this run, noted for pattern-tracking only.

Gauges: G1 **60%** 🟡 (58 real items projected, 23 short, committed=0 across all). G2 **36%** 🔴 (141 in netting, 90 net-short — unchanged since 07-24, now 29 days stale and previously confirmed wrong). G3 **0%** 🔴 (V7: planned_qty 500 vs actual_output 0 for the 08-20 NAMASTEA REGULAR plan).

Delivery evidence (V4, observed this run): rebuild_verifier confirmed **0** via `private_core.job_runs` (today's `cron_nightly` execution, 03:00:00Z — see Stage 0 note above; direct function call still blocked by this session's read-only transaction, same as 08-20); FG via `fn_compute_daily_fg_projection`; RM via `planning_run_component_netting` run `2ad307b8` (confirmed still latest completed via `planning_runs`); open-exceptions via `private_core.exceptions`; PO queue via `purchase_orders`+`suppliers`; email POSTed to Make webhook 2026-08-21T03:42:5xZ (payload 27,489 B) → **HTTP 200 "Accepted"**; Make execution `72eb86a95d2a4d2a9c5e488112b3149b` (2026-08-21T03:42:59.767Z), status=1 success, 2 ops, 27,664 B transfer — matches this run's payload size; delivered to tom@gteveryday.com via scenario `6439326` → Gmail `sendAnEmail`; findings-log entry committed same run. Zero `production_plan`/purchase-session drafts written this run (V2/C4: no committed shortfall anywhere in the 14d window, and the only netting run available is 29 days stale — writing a purchase-session draft off it would itself violate V4).
