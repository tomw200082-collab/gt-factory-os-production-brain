# Preserves data/invoices/suppliers/unprocessed/ directory structure under git.
# Contents (scanned supplier invoices awaiting entry into Green Invoice / the system) are
# ignored as binaries — see PRODUCTION/.gitignore.
# See data/README.md for purpose.
