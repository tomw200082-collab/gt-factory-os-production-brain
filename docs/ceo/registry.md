# GT — לוח המנכ״ל: מטרות · פרוייקטים · משימות

> הבית של השכבה הרוחבית — מה שאין לו מערכת חי כאן.
> נקרא ומתעדכן בכל **פתיחת שבוע** (`.claude/skills/weekly-opening/`). המפעל התפעולי (מלאי/ייצור/רכש/הזמנות) חי במערכות ונשלף חי — ⊥ משוכפל לכאן.
> **שוחזר 2026-08-01** מתוך: `CURRENT_STATE.md` (lean) + `docs/archive/CURRENT_STATE.pre-lean-2026-07-31.md` + `docs/archive/ACTIVE_NOW.pre-lean-2026-07-31.md` + `docs/factory-mapping/`. שום דבר לא אבד — המקור המלא בארכיון ובגיט.
> כללי כתיבה: תוכן = מהמילים של טום (ישיבה / הדבקת דשבורד). קלוד מוסיף סטטוסים, לינקים והצלבות — ⊥ ממציא משימות.

## מטרות הרבעון — ⏳ ממתין לריקון ראש (הישיבה הראשונה)

- (ימולא במוצ״ש — מה שחשוב באמת לרבעון הקרוב: מכירות? יציבות? צוות? מוצרים חדשים?)

## פרוייקטים

| פרוייקט | מצב | שורה תחתונה | הצעד הבא |
|---|---|---|---|
| Factory OS — פלטפורמה | 🟢 חי | ~85–90%, כל שערי v1 סגורים, stock truth מחזיק (`rebuild_verifier()=0` לילה-לילה) | סגירת ה-gaps במשימות למטה |
| Shopify sync | 🟢 חי | 53/53 פריטים, כל 5 דק׳, watchdogs פעילים | ניטור בלבד; ⊥ לגעת |
| LionWheel bridge | 🟢 חי | בשימוש ייצור רציף מאז 10.5 | — |
| פורטל production-ready | 🟡 בתהליך | 92/100, ‏9/10 קטגוריות ≥8 | שאריות A11Y + tranches פתוחים |
| מיפוי מפעל v3 — הטמעה ארגונית | 🟡 בתהליך | playbook v2 בתוקף, baseline שבוע 0 נמדד 23.7 | שיחת אלכס (חבילה מוכנה) · מדידת שבועות 1+ |
| פתיחת שבוע — CEO OS | 🟡 נבנה | סקיל + דשבורד + לולאת אבנים — נבנה 1.8 | ריצה ראשונה במוצ״ש הקרוב |
| גיוס | ❓ לאימות | סקיל outreach קיים ופעיל | טום: מה סטטוס המשרות והמועמדים? |
| שיווק / CRM | ⏸ בשער | מוקפא עד `MODULE_TEMPLATE` + אישור טום בכתב | החלטת טום מתי פותחים |

## משימות פתוחות — ממתין לטום (משוחזר מ-CURRENT_STATE + pre-lean)

- [ ] **מקסים — משתמש פורטל** (מייל). דניס provisioned. החתיכה האחרונה של tranche 137.
- [ ] **שיחת אלכס** — החבילה מוכנה: `docs/factory-mapping/2026-07-22-alex-package-he.md`. שערי rollout שבועות 5–6.
- [ ] **Telegram bot token + chat_id** — התראות ניטור (runbook §10).
- [ ] **app_users uuid** — לייבוא ספירות.
- [ ] **`JOB_RUNNER_TOKEN` + קונטיינר audit-skill** — סוגר את `audit_runs` הריק (P2 דיווח; ⊥ stock-truth — הליבה ירוקה 83/83).
- [ ] **אימות read-only** של ה-literal ‏`LIONWHEEL_FG_OUT_BRIDGE_ENABLED` ב-Railway.

## נדחה בהחלטה — לא חוסם (⊥ לאבד)

`tier` semantics ב-SQL · `cover_days` over-buffers (81/81 מדגם) · הגדרת "סשן קודם" למגמת ₪-בסיכון · קונטרסט `--fg-subtle` ‏3.09:1 · A11Y-003/004/010 · פיצול פרסונת דורין · רשומת נהג מיידן ב-LionWheel · תזכורת לקוחות שבת · הרחבת CI typecheck ל-`api/src` · ‏`supabase` postinstall הבהוב ב-CI.

## ❓ לאימות — הופיע ב-pre-lean, סטטוס לא ודאי היום

- `.mcp.json` disposition (סומן Tom-pending לפני חודשים — עדיין רלוונטי?)
- מדיניות bundles ‏(GTSET-*) — נסגר עם ניקוי ה-exceptions או פתוח?

## איפה השאר

- כרטיסי שבוע (3 אבנים + החלטות + רטרו): `docs/ceo/weeks/`
- דשבורד חי: לינק ב-`.claude/skills/weekly-opening/SKILL.md`
- מצב מערכת מלא: `CURRENT_STATE.md` · היסטוריה מלאה: `docs/archive/` + git log
