# פתיחת יום ראשון 30.08 — סיכום עצירה, סוף שבוע 27.08.2026

> **סטטוס מסמך:** snapshot מידע (informational) — לא מסמך סמכות. נאסף ביום חמישי 27.08 ~19:00 IL
> על ידי סשן `סיכום עבודה לסוף השבוע` מתוך: 100 סשנים אחרונים, 45 רוטינות, 87 PRs פתוחים,
> מסמכי מצב בכל 4 הריפואים, gap_registry, יומן הגרדיאן, ו-Notion (משימות + פרויקטים).
> **הערה:** 7 סשנים מלווים תוזמנו לבדוק את ה-PRs שלהם ביום חמישי בערב (18:46–21:45) — ייתכן
> שחלק מה-PRs "הירוקים" כבר מוזגו עד ראשון. הסשן המסכם מתעורר ביום ראשון 07:00 ומרענן את התמונה.

---

## 0. שורה תחתונה

השבוע (23–27.08) היה שבוע המכירות הגדול: intake לידים חי דרך Make, פורטל מכירות v2 (טראנצ'ים 164–175),
תיקון שרשרת ה-pick של LionWheel, ותמחור-מחדש מלא (FOOD COST + קטלוג + תפריט). **38 PRs מוזגו מאז 20.08.**
העבודה נעצרה באמצע גל סגירה: ~15 PRs של הימים האחרונים ירוקים וממתינים למיזוג/פריסה, מערכת הרוטינות
כולה כבויה, ויש 3 חובות אמת-מלאי (דיווח ייצור, repost של LionWheel, קבלה של 15 מיליון יחידות) שמעוותים
את תמונת המלאי עד שיטופלו.

---

## 1. שלוש פעולות לפני הכל (ראשון בבוקר)

### 1.1 להחיות את מערכת הרוטינות — הכול כבוי
מצב מדויק (מ-45 הרוטינות בחשבון, רק 8 פעילות — 7 חד-פעמיות של חמישי בערב + אקסל חודשי):

- **נהרגו כי הסביבה נמחקה** (`env_017y3uFy4wo8cdvYBA2FP68T`, ended_reason=`auto_disabled_env_deleted`):
  `daily-ops-guardian` (06:30 א'–ה'... בפועל היה מוגדר ב'–ו' — לתקן ל-`0-4`) · `sunday-prep` (שבת 20:00) ·
  `GT weekly scorecard` (ראשון 09:00, הריצה האחרונה שלו ABANDONED) · תזכורת DNS ל-pricelist · לינק מכירות שבועי · Agora Scout.
  **כדי להחזיר אותם חייבים קודם להצביע על סביבה חיה** — אחרת ייפלו שוב בהפעלה הראשונה.
- **הושהו ידנית סביב 10.08** (כנראה בעקבות "שגרות אוכלות טוקנים"): `messi-checkpoint` (13:00) · `messi-evening` (20:00) ·
  `cos-day-open` (07:30) · `cos-day-close` (17:00) · `queue-guard` (חמישי 15:50) · `דוח מכירות שבועי` (רביעי 07:30) · quota-watch.
- **השלכות שכבר קרו:** queue-guard לא רץ היום (חמישי) — אין בדיקת POs לא-מושמים לפני שדורין יצאה;
  sunday-prep לא ירוץ בשבת — **לא תחכה טיוטת ייצור ליום ראשון בבוקר**; הגרדיאן חשוך מ-26.08;
  יומן הממצאים על main נעצר ב-24.08 (ממצאי 25.08 תקועים ב-PR ‎#158 שלא מוזג).
- **החלטה לראשון:** אילו רוטינות חוזרות, על איזו סביבה, ואולי בפרומפטים רזים יותר. משפט אחד ממך — ואני מבצע.

### 1.2 סבב מיזוג + פריסה של ה-PRs הירוקים של הימים האחרונים
כולם draft, כולם עברו בדיקות (ה-CI חזר לעבוד היום אחרי שהמכסה שוחררה):

| PR | מה זה | פעולה |
|---|---|---|
| `gt-factory-os` [#243](https://github.com/tomw200082-collab/gt-factory-os/pull/243) | הסרת טוקן Shopify מהקוד + שער CI + ‎.env.cloud.example | למזג, **ואז 1.3 למטה** |
| `gt-factory-os-portal` [#225](https://github.com/tomw200082-collab/gt-factory-os-portal/pull/225) | concurrency + שער עלות ל-portal-pr-guard (החוסך הגדול) | למזג |
| `gt-factory-os` [#242](https://github.com/tomw200082-collab/gt-factory-os/pull/242) | repost של FG-out מאז ספירת 19.08 — המיגרציה כבר הוחלה בפרוד | לוודא שה-1,074 יחידות נרשמו (reconciler) ואז למזג |
| `gt-factory-os` [#244](https://github.com/tomw200082-collab/gt-factory-os/pull/244) | 3 מוצרי REPACK (מאצ'ה 30g, הוג'יצ'ה 30g, אובה 50g) — הוחל בפרוד, D1–D7 ✅ | למזג + החלטות §6 שלך (B/D/F/C) |
| `brain` [#171](https://github.com/tomw200082-collab/gt-factory-os-production-brain/pull/171) | סריקת drift של catalog-truth + מאסטרפרומפט הוג'יצ'ה 1kg | למזג; המאסטרפרומפט מוכן להרצה |
| `brain` [#170](https://github.com/tomw200082-collab/gt-factory-os-production-brain/pull/170) | ספק W3 של מסע הליד (הקשר קמפיין + בעלות) | לאשר ספק → תוכנית ביצוע |
| `brain` [#172](https://github.com/tomw200082-collab/gt-factory-os-production-brain/pull/172) | סקיל `report-production` מקצה-לקצה | דורש טוקן Supabase להרצת אימות חיה (ר' 1.3) |
| `brain` [#169](https://github.com/tomw200082-collab/gt-factory-os-production-brain/pull/169) | מאסטרפרומפט מסע-הליד + 9 סקילים | למזג |
| `brain` [#168](https://github.com/tomw200082-collab/gt-factory-os-production-brain/pull/168) | פתיחת משימת ניקוי חנות Shopify (6 שערי החלטה שלך) | למזג; ההחלטות בהמשך |
| `gt-factory-os` [#241](https://github.com/tomw200082-collab/gt-factory-os/pull/241) | fanout של התראת ליד לכל הדסק + שחזור מיגרציות 0338–0342 | למזג → **לפרוס `sales-leads-poll` מ-main** → להפיל את הטריגר-גשר (SQL בגוף ה-PR) |
| `brain` [#166](https://github.com/tomw200082-collab/gt-factory-os-production-brain/pull/166) | השלמת תמחור 81% (xlsx + גנרטור) | **dirty — צריך merge של main לענף** ואז למזג |

⚠️ **התנגשות מספור מיגרציות:** גם ‎#242 וגם ‎#244 נושאים קובץ `0338_*.sql` (שניהם כבר רצו בפרוד תחת שמות שונים).
מי שמתמזג שני — חייב מספור מחדש. בנוסף קיים slot כפול `0330` על main (lionwheel + sales) — לתעד או ליישר.

### 1.3 אבטחה — רוטציית טוקן Shopify (GAP-029, P0, פתוח מ-18.08)
הטוקן (admin, קריאה/כתיבה על הכול) היה בקוד והריפו היה זמנית public. ‎#243 מסיר אותו מהעץ אבל **הרוטציה לא בוצעה**:
1. להנפיק טוקן חדש ב-Shopify. 2. **באותה דקה** לעדכן את ה-secret ב-Supabase Edge (אחרת reconciler cron 24 נשבר). 3. למחוק את ה-ref ‏`refs/heads/stash/4-…` שמכיל גיבוי ‎.env. 4. אחרי זה אפשר לשקול ריפו public.
באותה הזדמנות: **רוטציית Green Invoice באיחור של 18+ ימים** (runbook קיים), ולוודא שה-health-check שלה בכלל רץ.

---

## 2. אמת מלאי וייצור — שלושה חובות פתוחים

1. **דיווח ייצור לא קורה ברצפה (V7).** לפי הגרדיאן האחרון (24.08): שום `production_actual` מאז 10.08,
   ‏11+ אצוות מתוכננות ללא דיווח. כל תמונת FG מוטה כלפי מטה. שני מסלולי טיפול שנבנו השבוע:
   - סקיל חדש `report-production` (brain ‎#172) — מדווח הכול דרך ה-API בפעולה אחת. חסר רק **טוקן Supabase** להרצת אימות.
   - סשן `דיווח ייצור Namastea ו-Matcha` חסום על אותו טוקן — מחכה לדווח 502 בקבוקי Namastea + 20 שקיות מאצ'ה.
   - ובנוסף: שיחת הרצפה עם דניס (הומלצה 4 ימים ברצף ע"י הגרדיאן) — תהליך, לא כלי.
2. **LionWheel repost (‎#242)** — הוחל הבוקר; לוודא ביום ראשון שכל ה-175 שורות/1,074 יחידות נרשמו כ-`FG_OUT_PICK` ושאין exceptions חדשים.
3. **GAP-026 (P0):** קבלת סחורה של **15,005,002 יח'** `PKG-BAG-MAT-500G` — כל קריאת כיסוי אריזות חסרת משמעות עד שתאושר רברסל. ממתין לאישורך.
   נלווים: GAP-027 (3 יתרות FG שליליות), GAP-028 (150 ק"ג מאצ'ה שנרשמו WASTE במקום העברה לניקיטין), אובה 1kg ספירה.

**ותכנון:** ריצת ה-netting בת **32+ ימים** (בוצעה 23.07), הוכחה שגויה מול יתרות חיות. `plan-production-14d` לא רץ
כי טקס רביעי מושהה. — הפעולה בעלת המינוף הגבוה ביותר לפי הגרדיאן. שים לב: בלי sunday-prep בשבת, גם את זה פותחים ידנית בראשון.

---

## 3. מכירות ולידים — הפרויקט המרכזי, ואיפה בדיוק עצרנו

**מה חי ועובד:** intake דרך Make (החלטה D-006 — ‏"MAKE."), scenario ‎7075235 + דופק שעתי ‎7075243, heartbeat יומי;
פורטל מכירות v2 ‏(`/sales/today|leads|orgs|attention|settings`), תור עם cap ‏15, ‏3 אנשי צוות (planner מחזיק את ציר המכירות
אחרי היפוך 174→175); התראת ליד מגיעה לתום.

**העובדה המארגנת (מתוך המאסטרפרומפט של 27.08):** ‏195 לידים במערכת, **190 מעולם לא טופלו**, ‏0 לקוחות חדשים
לפי ההגדרה שלך לניצחון. הצינור קיים; הקצה הרחוק חסר. לכן הספק (‎#170) הפך את הסדר: **Phase 0 → W3 (הקשר
קמפיין + בעלות) → W1 (טבלת ניתוב) → W2 dry-run → W2 חי** — בזה ממשיכים.

**פערים חמים:**
- ‎#240 מוזג אבל **לא נפרס** — ‏`sales-leads-poll` עדיין v12; אלכס ואבי מקבלים בינתיים התראה מהגשר (רזה יותר).
  המיזוג של ‎#241 + פריסה מ-main + הפלת הטריגר-גשר סוגרים את זה.
- **P0 בתור:** ‏`due_follow_up` מקבל 0 כרטיסים (תקציב משותף שנשאב ע"י new_lead) — הקולבק היחיד שנקבע לא ייראה.
  מאסטרפרומפט מוכן ולא הורץ: brain ‎#157 (S1–S5: תור, זהות/הרשאות, תזכורות, won, undo).
- **סשן חסום ברשותך:** `לידים מהטבלה- פנייה` — רשימת פנייה של **230 עסקים** מוכנה, מחכה לאישור שליחת WhatsApp
  (דגל ה-outreach קפוא `false` — נשאר קפוא עד החלטתך).
- רגישות זמן: **Meta שומרת לידים 90 יום** — לוודא שה-Make סחב את כל הבקלוג (הישנים נופלים בתחילת ספטמבר).
  חיבור Facebook של Make בתוקף עד **23.10**.

---

## 4. תמחור, קטלוג ו-Shopify — כסף על הרצפה

- **מחירי AMERICAN הפוכים בחנות עכשיו:** ‏1L נמכר ב-₪33 ו-0.5L ב-₪65 (צריך 65/33). ליטר בחצי מחיר — תיקון ידני שלך בשופיפיי, 2 דקות.
- **מע"מ:** החנות מסומנת `taxesIncluded=true` בעוד המחירים הם ללא מע"מ, והשיעור מוגדר 17% במקום 18% (הוזמנה אמיתית עם חיוב חסר). שאלה לרו"ח + החלטה — פתוח מ-05.08.
- **הושלם השבוע:** FOOD COST נבנה מחדש מלמטה (‎#167 מוזג היום), קטלוג המשקאות והתפריט הפותח יושרו ל-48
  הספרות המאושרות. שולי רווח 76%–87%, ממוצע 80.9%; ‏17/48 משקאות מתחת ל-80% — תוצאה של החלטתך לא להעלות מחירים (מתועד).
- **פתוח:** ‎#166 (ה-xlsx ללקוח + גנרטור) dirty וממתין; GAP-031 — `GT_Summer_Menu_2026.xls` דורש תיקון במקור (עמודת מחיר ריקה,
  autofill במינוני מאצ'ה, בלוק ICE UBE מוזח); 4 מוצרים במחיר מתחת לעלות/אפס עדיין חיים; עמוד 36 בקטלוג — "מקל וניל" מול בסיס עלות של תמצית.
- **חנות:** ‏74 מ-122 מוצרים ACTIVE במלאי אפס/שלילי; ‏78 SKUs עודפים נרשמו כחוב ניקוי (‎#168) — ‏6 החלטות שלך חוסמות את הביצוע,
  והוקלט גבול קשיח: אין לתקן מלאי בשופיפיי (ה-reconciler ידרוס תוך 5 דקות).

## 5. מוצרים חדשים (REPACK) — כמעט סגור

- **בוצע היום (‎#244):** ‏`FG-MAT-CAN-30` ‏(GT מאצ'ה 30g), ‏`FG-HOJ-CAN-30`, ‏`FG-UBE-CAN-50` ‏(backfill למוצר שכבר נמכר) —
  פריטים, BOMs, מיפויים, מוצרי Shopify ‏(DRAFT, מלאי 0, מחירים 63.72/51.92/40.12 כולל מע"מ). נשאר לך: איזה גרייד מאצ'ה בפחית (B),
  שורת מכסה ב-BOM (D, ברירת מחדל: לא), מדבקה נפרדת מאליטה (F, ברירת מחדל: נפרדת), ותיקון ספירה ל-‎-100 של האובה (C) — ואז לפרסם את שני ה-DRAFTs.
- **הבא בתור:** מאסטרפרומפט **הוג'יצ'ה 1kg בשקית הזהב** (בתוך ‎#171) — כתוב, נבדק, לא הורץ. בשופיפיי המוצר קיים ומושלם חוץ מברקוד `TEMP-`;
  אצלנו לא קיים כלום (פריט/BOM/רכיבים/מיפוי). סשן חדש עם המאסטרפרומפט = שעה של עבודה.
- אזהרה שחוזרת בשני המסמכים: ‏`FG-MAT-30G` הוא של **אליטה** — לא לגעת.

## 6. סשנים חסומים שמשתחררים במשפט אחד ממך

| סשן | מה חסר |
|---|---|
| `סיעור מוחות` (W3) | להריץ SQL אחד ב-Supabase ‏(`vault.create_secret`) ולענות "בוצע" |
| `דיווח ייצור Namastea ו-Matcha` | טוקן Supabase — ואז נרשם דיווח 502+20 |
| `לידים מהטבלה- פנייה` | אישור/דחייה לשליחת WhatsApp ל-230 עסקים (דגל outreach) |
| `הקמת מוצר UBE CAN 50 G` | מחיר/כמות סופיים + החלטת BOM (חלקית נסגר ע"י ‎#244 — לוודא ולארכב) |
| `מסלול של אריק למחר` | תקוע 7 ימים על שאלה — כנראה כבר לא רלוונטי, לארכב |
| `תכנון ייצור ורכש` | ‏`SUPABASE_ACCESS_TOKEN` ל-workflow — ואז ריצת תכנון |
| `הזמנה לפרש דה מרקט חדרה` | אישור ₪590/שק למאצ'ה של מולה בוטני (5 טיוטות ממתינות) |
| `הוספה למלאי` | ‏PO-2026-00310 עדיין ממתין |

(שני סשני כשל מ-529 — `רכש ותכנון ייצור`, `בעיה בספירת מנגו סמודי` — פשוט להריץ מחדש אם עדיין רלוונטיים;
לשניהם יש PRs פתוחים: ‎#225 backend ו-‎#216 portal של ספירת המנגו.)

## 7. משימות Notion פתוחות (30, רובן באיחור)

המרכזיות במבט של ראשון: **להכריע בקבוקי חצי ליטר (אלכס)** — מסומן כחוסם תכנון ייצור · ספירת שקיות אלומיניום לרכש ·
תוויות אחוריות ל-3 אבקות 500g + הזמנת תוויות אנגלית · מקציפי חלב USB-C · ‏pricelist.gteveryday.com ‏(Vercel+GoDaddy) ·
מזומן נזיל לרכש לחודש הבא (אלכס — רביעי האחרון עבר ב-26.08) · משתמש חדש Shopify+Dropbox · לסגור brain ‎#111 (עיצוב ארגון סוכנים) ·
פסיפלורה/SEEDLESS יתום · חריגות מלאי — 3 פריטים · מסגרת תפקידים על הקיר · "כל אחד מזין בעצמו" (דניס+מקסים — קשור ישירות ל-V7) ·
חדשה מהיום: ניקוי קוד מת בארבעת הריפואים (לבחון לפני ביצוע).

## 8. היגיינת מוחות ומסמכי מצב (חשוב, לא דחוף)

- **brain:** ‏`CURRENT_STATE.md` ו-`ACTIVE_NOW.md` קפואים על 01.08 — לא מזכירים את מודול המכירות ולא את התמחור.
  ‏`docs/ceo/weeks/` מכיל רק כרטיס אחד (02.08). יומן גרדיאן חסר 25–27.08 (‏25.08 ב-‎#158).
- **Sales-Machine:** ‏`CURRENT_STATE.md` מפגר בחצי יום מהראיות (סנריו Make כן נבנו); ‏registry לא מעודכן (9 כרטיסים לא רשומים);
  ‏U-013 מנוסח על הנחה שהופרכה (הטופס כן שואל שם עסק — השאלה האמיתית: למה 39 לידים בלי טלפון ומייל).
  **ראיון ‎#1 ‏(ICP)** הוא הפותח של כל Phase 2 (דוקטרינה, פלייבוקים, קונטקסט שיווקי).
- **portal:** ‏scorecard ‏94/100 מיושן (23.07, לפני כל סביבת המכירות) — להריץ `/portal-scorecard`; ‏`_active.txt` עדיין מצביע על 175 שהסתיים;
  ‏2 פעולות Supabase Studio ידניות ממתינות (משתמש בדיקה לא-אדמין + שורת auth לאבי).
- **טריאז' 87 ה-PRs הפתוחים:** ‏52 ב-brain (רובם יומני גרדיאן ותיעוד מ-27.07–16.08 שאפשר למזג בבת אחת), ‏21 ב-gt-factory-os
  (הישנים — ‏#177/#186/#188/#194/#197/#201/#203/#206/#207/#208/#213/#218/#221 — חלקם כנראה כבר בוצעו בפרוד ואפשר לסגור),
  ‏8 בפורטל, ‏6 ב-Sales-Machine. ‏#98 ‏(Amendment A) התייתר — האישור כבר על main מ-17.08. ‏#212 (הזנת ההזמנות, ‏₪142.7K) חי ומתעדכן — למזג כשנוח.

## 9. לוח פתיחה מוצע לראשון

1. ☑️ החייאת רוטינות (1.1) — החלטה שלך, ביצוע שלי.
2. ☑️ סבב מיזוגים (1.2) + רוטציית טוקן (1.3).
3. ☑️ שחרור 3 הסשנים החסומים בעלי הערך המיידי (טוקן דיווח ייצור · SQL של W3 · אישור/דחיית פנייה ל-230).
4. ☑️ תיקון מחירי AMERICAN בשופיפיי.
5. ☑️ ריצת `plan-production-14d` (32 יום) + אימות ה-repost של LionWheel.
6. במהלך השבוע: ביצוע W3 (‏#170) → queue-integrity ‏(#157) → הוג'יצ'ה 1kg (‏#171) → טריאז' PRs → עדכון מסמכי מצב.

---
*נאסף אוטונומית; כל עובדה מגובה במקור (PR, ‏gap_registry, יומן גרדיאן, ‏Notion, ‏evidence). טעויות אפשריות — המקורות מנצחים.*
