# מסי · 2026-08-06

## תור
- [ ] 1. עדכון BOM-BASE-REV-REG — איחוד שני חומרי הגלם של הפסיפלורה · נקלט 2026-08-06 · ממתין להכרעת טום על שני ה-BOM הנוספים

## ספקים
### 1. עדכון BOM-BASE-REV-REG — איחוד שני חומרי הגלם של הפסיפלורה
מטרה:        גרסת BOM חדשה ל-`BOM-BASE-REV-REG` שמחליפה את `RAW-PASSION-FRUIT-PUREE`
             (ריסטרטו, SUP-012, מלאי 0, ₪34.97/ק"ג) ב-`RAW-PASSION-SEEDLESS`
             (פרי הבוסתן, SUP-048, מלאי 80.85 ק"ג, ₪19.00/ק"ג). הכמות ⊥ משתנה:
             22 ק"ג לבאצ' של 500 ליטר (0.04222649 ק"ג לליטר תפוקה).
             סיבה: לא מייצרים מיקסרים יותר ⇒ שני חומרי הגלם מתאחדים לאחד, ל-REVIVE בלבד.
ריפו/נתיבים: `gt-factory-os/db/migrations/` — מיגרציה חדשה בלבד. ⊥ נתיב אחר.
done-criterion: `BOM-BASE-REV-REG` בגרסה פעילה חדשה, שורת הפסיפלורה מפנה ל-
             `RAW-PASSION-SEEDLESS` ב-22 ק"ג, הגרסה הישנה בארכיון, pgTAP ירוק.
פתוח להכרעת טום: `BOM-BASE-ARK-PAS-REG` (ARAK PASSION FRUIT, 55.75 ליטר לבאצ') ו-
             `BOM-MUZ-PSC-200ML` (MUZA PASSION SPRITZ 0.2L, 0.02 ק"ג ליחידה) עדיין
             מפנים לריסטרטו. גם ההערה על `RAW-PASSION-SEEDLESS` ("Used ONLY for Muza
             Purple Kiss") כבר לא נכונה וצריכה עדכון.
חוסם:        הזמנת 20,000 בקבוקי 300ML (5,000 מכל טעם: FRESH / REVIVE / CALM / DETOX)
             נכנסת עכשיו לתור רכש. שורת הפסיפלורה תלויה בהכרעה הזאת.
נושן:        https://app.notion.com/p/3b4042982afb8190bd3bca2aa5739caf

§גבולות (חובה בכל ספק כותב):
- כתיבות מותרות — רשימה ממצה, נתיב ∉ כאן ⇒ ⊥ כתיב:
  `gt-factory-os/db/migrations/NNNN_*.sql` · `gt-factory-os/db/tests/NNNN_*.test.sql`.
  לפני כתיבת קובץ ממוספר — לרשום את `db/migrations/` מיד לפני ומיד אחרי; קובץ חדש
  שהופיע בין לבין ⇒ HALT + `contract_failure`. ⊥ מספור מחדש בשקט.
- ⊥ לעולם: merge · deploy · מיגרציית פרוד · `production_plan` / firm / place ·
  ledger/projections · דגלים קפואים · מערכות חיצוניות (Shopify/LionWheel/Green Invoice) ·
  authority docs (`CLAUDE.md`, `CURRENT_STATE.md`, `EXECUTION_POLICY.md`) · קוד פורטל ·
  הודעות לעובדים (טיוטות בלבד — טום שולח).
- `git add -A` / `git add .` — ⊥ לעולם. נתיבים מפורשים בלבד.
- ⊥ לגעת ב-`stock_ledger` / `balance_anchors` / `current_balances` — קריאה בלבד.
- ⊥ להחליט על `BOM-BASE-ARK-PAS-REG` / `BOM-MUZ-PSC-200ML` — טום מכריע.
- תנאי עצירה (`CLAUDE.md` §Stop conditions) ⇒ HALT + שורה רועשת + ניתוב ל-`factory-os-governor`.

## אירועים
- 2026-08-06 — נקלטה משימת עדכון ה-BOM מזריקת טום בשיחת הזמנת 20K הבקבוקים.
