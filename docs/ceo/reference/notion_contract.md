# חוזה נושן — Chief-of-Staff OS

> **המאסטר לפרויקטים ומשימות הוא נושן** (G1). `docs/ceo/registry.md` = מראה אוטומטית בלבד.
> כל שם property כאן **אומת חי 2026-08-01** מול `notion-fetch` + קריאה + כתיבה + היפוך. ⊥ לנחש שמות.
> שינוי סכימה בנושן → הריצה הבאה תיפול. נפילה = `assumption_failure` + עצירה, ⊥ ניחוש.

## מזהים

| מה | מזהה |
|---|---|
| עמוד הורה | `91304298-2afb-8395-80a1-016b4a23d575` — ״מעקב אחר פרויקטים ומשימות gt״ |
| DB משימות | `collection://c6604298-2afb-8258-8026-87e9538244c3` |
| DB פרויקטים | `collection://cb704298-2afb-8226-9b23-876996b62c5d` |
| Workspace | `d4c04298-2afb-81a2-b22c-00037ae42a14` · Tom Witt `tom@gteveryday.com` |

## סכימה — משימות

| Property | סוג | ערכים / הערות |
|---|---|---|
| `שם` | title | חובה |
| `אחראי` | person | JSON array של user IDs |
| `בעל תפקיד` | multi_select | `תום` `אלכס` `דורין` `מקסים` `דניס` `עדי` `אדי` `אמיר` |
| `גל` | select | `גל 0` `גל 1` `גל 2` `גל 3` `גל 4` `שלב ב` `שוטף` `מנהלה` |
| `מסלול` | select | `שטח` `מעבדה` `ציר-אלכס` `מנהלה` |
| `פרויקטים` | relation → פרויקטים | **limit 1** |
| `תאריך יעד` | date | `date:תאריך יעד:start` |
| `תאריך השלמה` | date | `date:תאריך השלמה:start` — **זה סמן ה״בוצע״** |
| `תאריך התחלה` | date **(datetime)** | `date:תאריך התחלה:start` + `date:תאריך התחלה:is_datetime` = **1** — שעה חובה (טום 2026-08-05). נחתם רק ע"י מסי ברגע **השיגור בפועל** או "אני על זה" של טום. ריק+יש השלמה = בוצע בלי מעקב, תקין |
| `Google Drive קובץ` | relation | `collection://33104298-2afb-80f0-b804-000be2544e3c` |
| `בוצע` | **button** | ⊥ קריא ב-querySql, ⊥ ניתן לכתיבה. הכפתור ממלא `תאריך השלמה` |

**⊥ זמין ב-querySql:** `בוצע`.

**עמודות מערכת — אומתו חי 2026-08-05:** `createdTime` קיימת ועובדת (`date(createdTime)`).
`created_time` ⊥ קיימת. `last_edited_time` **ו-**`lastEditedTime` ⊥ קיימות —
`no such column`. ∴ "בלי תזוזה" ⊥ ניתן למדידה ישירה בנושן; ראה §מתכוני מסי.

## סכימה — פרויקטים

| Property | סוג | ערכים / הערות |
|---|---|---|
| `שם` | title | חובה |
| `סטטוס` | **status** | `לא התחיל` \| `בתהליך` \| `בהמתנה` \| `הושלמו` \| `בוטל` |
| `לוח זמנים לפרויקט` | date **range** | `:start` + `:end` — **זה הגאנט** |
| `עדיפות` | number | 0 = הכי דחוף. חי היום: 0–6 |
| `ציר` | select | `INBOUND` `OUTBOUND` `מנהלה` |
| `מסלול` | select | `שטח` `מעבדה` `ציר-אלכס` `מנהלה` |
| `אחראי` | person | |
| `חסימה על ידי` | self-relation | **תלויות** |
| `משימות` | relation → משימות | |
| `KPI` · `מה צריך מאלכס` · `Description` | text | |
| `התקדמות` | rollup | `percent_not_empty` על `תאריך השלמה` |
| `עיכוב` · `משך` | formula | |

**⊥ זמין ב-querySql:** `עיכוב` · `משך` · `התקדמות` · `Place`.
∴ **לחשב לבד מהתאריכים** — הנוסחאות למטה.

### קיבוץ סטטוס (מבנה `status` בנושן)

`to_do` = {`לא התחיל`} · `in_progress` = {`בתהליך`, `בהמתנה`} · `complete` = {`הושלמו`, `בוטל`}.
**פעיל** ≡ `סטטוס ∈ {לא התחיל, בתהליך, בהמתנה}`. **סגור** ≡ `∈ {הושלמו, בוטל}`.

## חישובים — ⊥ לגזור מחדש כל פעם

`today` = תאריך IL של הריצה.

| מדד | הגדרה |
|---|---|
| **עיכוב** (פרויקט) | פעיל ∧ `end < today` ⇒ `days_late = today − end`. אחרת 0. |
| **מזדקנת** (משימה) | פתוחה (`תאריך השלמה IS NULL`) ∧ `תאריך יעד < today` ⇒ `days_overdue`. |
| **תקוע** (GTD) | פרויקט פעיל ∧ **אפס משימות פתוחות** (כל המשימות המקושרות סגורות, או אין בכלל). ⇐ ״פרויקט פעיל בלי next-action״. **מסומן בכל מוצ״ש.** |
| **waiting-on** | משימה פתוחה ∧ `בעל תפקיד` ⊅ `תום` ⇒ ממתינה לאדם אחר. `checkback` = `תאריך יעד`; עבר ⇒ צף. |
| **עומס פר-אדם** | `count(*)` משימות פתוחות group by `בעל תפקיד` (multi — פריט נספר לכל בעליו). |
| **התקדמות** | `סגורות / סה״כ` על המשימות המקושרות. ⊥ לקרוא את ה-rollup. |

## מתכוני קריאה — להעתיק כמו שהם

**גאנט מלא (פרויקטים לפי עדיפות):**
```sql
SELECT "שם", "סטטוס", "עדיפות", "ציר", "מסלול", "KPI",
       "date:לוח זמנים לפרויקט:start" AS start,
       "date:לוח זמנים לפרויקט:end"   AS fin,
       "חסימה על ידי" AS blocked_by, "משימות" AS tasks, url
FROM "collection://cb704298-2afb-8226-9b23-876996b62c5d"
WHERE "סטטוס" NOT IN ('הושלמו','בוטל')
ORDER BY "עדיפות" ASC
```

**משימות פתוחות (מזדקנות קודם):**
```sql
SELECT "שם", "בעל תפקיד", "גל", "מסלול",
       "date:תאריך יעד:start" AS due, "פרויקטים" AS proj, url
FROM "collection://c6604298-2afb-8258-8026-87e9538244c3"
WHERE "date:תאריך השלמה:start" IS NULL
ORDER BY due ASC NULLS LAST
```

**מה נסגר בשבוע האחרון (לרטרו):**
```sql
SELECT "שם", "בעל תפקיד", "date:תאריך השלמה:start" AS done, url
FROM "collection://c6604298-2afb-8258-8026-87e9538244c3"
WHERE "date:תאריך השלמה:start" >= date('now','-7 days')
ORDER BY done DESC
```

**waiting-on שהבשיל היום:**
```sql
SELECT "שם", "בעל תפקיד", "date:תאריך יעד:start" AS due, url
FROM "collection://c6604298-2afb-8258-8026-87e9538244c3"
WHERE "date:תאריך השלמה:start" IS NULL
  AND "בעל תפקיד" NOT LIKE '%תום%'
  AND "date:תאריך יעד:start" <= date('now')
ORDER BY due ASC
```

## מתכוני כתיבה — אומתו חי 2026-08-01 (כתיבה → קריאה → היפוך → קריאה)

**סימון בוצע** — `notion-update-page` · `command: update_properties`:
```json
{ "date:תאריך השלמה:start": "YYYY-MM-DD", "date:תאריך השלמה:is_datetime": 0 }
```
⊥ ללחוץ על ה-button `בוצע` (⊥ נגיש ל-API). מילוי `תאריך השלמה` **הוא** הסימון — ה-rollup `התקדמות` מתעדכן ממנו.

**חתימת התחלה** — `notion-update-page` · `command: update_properties` (אומת חי 2026-08-05):
```json
{ "date:תאריך התחלה:start": "YYYY-MM-DDTHH:MM:00+03:00", "date:תאריך התחלה:is_datetime": 1 }
```
שעה חובה — `is_datetime: 0` כאן = באג. השעה מזינה את `מ-<שעה>` באק; "בלי תזוזה" נמדד
מהחתימה עצמה (`started`), ⊥ מ-`last_edited_time` — היא ⊥ קיימת ב-querySql.
הערך שעבר הלוך-חזור באימות: כתיבה `2026-08-05T17:05:00+03:00` ⇒ קריאה
`2026-08-05 14:05:00Z`, `is_datetime` = 1. **נושן מחזיר UTC** — להמיר לפני הדפסה לטום.

**קישור לפרויקט** (relation, limit 1) — מערך עם URL אחד:
```json
{ "פרויקטים": ["https://app.notion.com/<page_id_ללא_מקפים>"] }
```

**ניקוי ערך:** `null`. אומת — מחזיר את השדה ל-NULL.

**משימה חדשה** — `notion-create-pages`, `parent: {type:"data_source_id", data_source_id:"c6604298-2afb-8258-8026-87e9538244c3"}`:
```json
{ "שם":"…", "בעל תפקיד":["תום"], "גל":"מנהלה", "מסלול":"מנהלה",
  "date:תאריך יעד:start":"YYYY-MM-DD", "date:תאריך יעד:is_datetime":0 }
```

**עדכון סטטוס פרויקט:** `{ "סטטוס": "בתהליך" }` — מחרוזת מהרשימה הסגורה בלבד.

## גבולות כתיבה (G2 · §6 של המאסטרפרומפט)

| פעולה | אישור |
|---|---|
| סימון בוצע · עדכון תאריך/סטטוס/עדיפות · משימה חדשה מהחלטה | **⊥ צריך** — פר-פריט חופשי |
| שינוי מהותי של תוכן קיים (ניסוח מחדש של שם/תיאור) | טום |
| מחיקה / ארכוב | **טום, תמיד.** ארכוב > מחיקה |
| שינוי סכימה (property חדש, אופציה חדשה ב-select) | טום |

⊥ להמציא משימות. תוכן = מהמילים של טום; קלוד מוסיף סטטוסים, תאריכים, קישורים והצלבות (C4).

## פריט בדיקה פתוח

`🧪 בדיקת חוזה כתיבה — Chief-of-Staff OS (ניתן למחוק)`
`3af04298-2afb-81d4-bc2a-c0163f007f72` — נוצר לאימות ה-Phase 1. **טום יכול למחוק**; קלוד ⊥ מוחק בלי אישור.

`בדיקת מסי — למחיקה`
`3b304298-2afb-8120-8602-ca6a4138f17b` — נוצר לאימות `תאריך התחלה` 2026-08-05. סומן
כהושלם ∴ ⊥ צף בשום מתכון. **ארכוב ידני ע"י טום** — לקונקטור נושן אין כלי archive/trash
לעמודים (אומת 2026-08-05), ומחיקה טעונה אישור טום ממילא.

## מתכוני מסי — שאילתות חתומות (אומתו חי 2026-08-05)

> **תחולה: `בעל תפקיד` = תום בלבד** (טום 2026-08-05, R1). של אחרים ⇒ מסלול
> ה-waiting-on, ⊥ מנוע הסגירה. `dup-check` היא היחידה ללא הסינון — כפילות
> נבדקת מול **כל** הפתוחות, גם של אחרים.
> **`[ארוך]`** בתחילת השם = ארוך-טווח (R3) ⇒ מוחרג מ-open-loops ו-opened-today.
> **⊥ מ-due-today** — הפטור הוא מ**סריקת** הסגירה היומית, ⊥ מ**יעד** שטום קבע:
> `[ארוך]` עם `תאריך יעד` = היום באמת דחופה היום וצפה. שתיים מוחרגות, אחת ⊥.

### RECIPE:open-loops — באוויר עכשיו
```sql
SELECT "שם", "date:תאריך התחלה:start" AS started, "בעל תפקיד", url
FROM "collection://c6604298-2afb-8258-8026-87e9538244c3"
WHERE "date:תאריך התחלה:start" IS NOT NULL AND "date:תאריך השלמה:start" IS NULL
  AND "בעל תפקיד" LIKE '%תום%'
  AND COALESCE("שם",'') NOT LIKE '[ארוך]%'
ORDER BY started
```

### RECIPE:due-today — דחופות היום שלא נסגרו
```sql
SELECT "שם", "date:תאריך התחלה:start" AS started, "בעל תפקיד", url
FROM "collection://c6604298-2afb-8258-8026-87e9538244c3"
WHERE date("date:תאריך יעד:start") = date('now') AND "date:תאריך השלמה:start" IS NULL
  AND "בעל תפקיד" LIKE '%תום%'
```

### RECIPE:opened-today — נפתחו היום (בתחולת מנוע הסגירה)
```sql
SELECT "שם", "date:תאריך יעד:start" AS due, "date:תאריך התחלה:start" AS started, url
FROM "collection://c6604298-2afb-8258-8026-87e9538244c3"
WHERE date(createdTime) = date('now') AND "date:תאריך השלמה:start" IS NULL
  AND "בעל תפקיד" LIKE '%תום%'
  AND COALESCE("שם",'') NOT LIKE '[ארוך]%'
  AND ("date:תאריך יעד:start" IS NOT NULL OR "date:תאריך התחלה:start" IS NOT NULL)
```

### RECIPE:dup-check — כפילות לפני יצירה
```sql
SELECT "שם", url FROM "collection://c6604298-2afb-8258-8026-87e9538244c3"
WHERE "date:תאריך השלמה:start" IS NULL
  AND ("שם" LIKE '%<מילה1>%' OR "שם" LIKE '%<מילה2>%' OR "שם" LIKE '%<מילה3>%')
```

**כלל מילות-המפתח ל-`dup-check`:** 2–3 **מילות תוכן** מהזריקה — שם עצם/פועל
נושאיים (⊥ מילות קישור: של, את, עם, על, לי, כדי, היום, מחר). מחוברות ב-`OR`.
**כל שורה שחוזרת = חשד** ⇒ נאמר באק (`⚠ דומה: <שם> — אותה משימה?`) ולא נוצרת
שורה שנייה עד שטום אומר. אפס תוצאות ⇒ יוצרים בשקט. פחות מ-2 מילות תוכן
בזריקה ⇒ מילה אחת, ובאק נכתב שהבדיקה חלשה.

**`בעל תפקיד` הוא multi_select** — `LIKE '%תום%'` תופס גם משימה משותפת (תום+אלכס).
זה הכיוון הרצוי, והוא בדיוק המראה של מתכון ה-waiting-on הקיים (`NOT LIKE '%תום%'`).
אין שם אחר ברשימה שמכיל "תום" כתת-מחרוזת ∴ אין תפיסת-שווא.

### שלוש סטיות מהתכנון — נכפו ע"י המנוע החי (2026-08-05)

1. **`last_edited_time` הוסר מ-`open-loops`.** ⊥ קיימת (גם ⊥ `lastEditedTime`):
   `no such column`. ∴ קריטריון הצ'קפוינט **"באוויר ≥3 שעות בלי תזוזה"** נמדד
   `started ≤ now − 3h` **+** היעדר אירוע מאוחר יותר לאותה משימה בלוג היום
   (`docs/ceo/messi/<תאריך>.md`). הלוג הוא מקור ה"תזוזה", ⊥ נושן.
2. **`created_time` ⇒ `createdTime`** ב-`opened-today`. השם עם הקו-התחתון ⊥ קיים.
3. **`COALESCE("שם",'')` עוטף כל `NOT LIKE`.** יש שורות עם `שם` = NULL במסד;
   `NULL NOT LIKE '…'` = NULL ⇒ השורה נושרת בשקט. בלי ה-COALESCE משימה חסרת-שם
   שנמצאת באוויר פשוט ⊥ תיראה. `[` נבדק ומתנהג כתו רגיל (1/0/0) ∴ `SUBSTR` ⊥ נדרש.

**`date('now')` מחזיר UTC.** בחלון הריטואלים (07:30–17:00 IL) תאריך UTC ≡ תאריך IL.
בין 00:00 ל-03:00 IL הוא **יום קודם** — ריצה בחלון הזה ! להשתמש ב-`date('now','+3 hours')`
(קיץ) / `'+2 hours'` (חורף).

**ראיות הריצה החיה (2026-08-05 17:0x IL):**

| מתכון | תוצאה |
|---|---|
| `open-loops` | 1 שורה — שורת הבדיקה בלבד (ה-property נולד היום ∴ ריק אצל השאר) |
| `due-today` | 9 שורות |
| `due-today` בלי סינון הבעלים | **9 שורות — דלתא 0 היום** |
| `opened-today` | 1 שורה — שורת הבדיקה |
| `dup-check` (`%מלאי%`/`%ספירה%`) | 3 שורות אמיתיות |

**הדלתא של סינון-הבעלים היא 0 היום, וזה ⊥ כשל.** התפלגות אמיתית של הפתוחות:
**68 סה"כ · 66 מכילות "תום" · 2 ⊥** (`["עדי","דורין"]`, `["אלכס","דורין"]`).
∴ טענת ה-"~66 משימות של אחרים יסומנו כמחליקות" שבתוכנית ובמסמך העיצוב **הפוכה** —
66 הן **של תום**. הסינון עדיין נכון (הכרעת טום R1) אבל מגן על 2 שורות, ⊥ 66.
משימה 8 ! לגזור את הדלתא מהנתונים החיים של יום הריצה, ⊥ לצפות לירידה במספר.

---
אומת: 2026-08-05 (סכימה + ארבעת מתכוני מסי, חי) · קודם: 2026-08-01 ·
הבא לאימות: כשמשתנה סכימה, או בכל `assumption_failure` מנושן.
