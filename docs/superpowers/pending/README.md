# תיקונים ממתינים להחלטת טום

תיקון שנכתב ואומת, אך **לא הוחל** — כי החלתו נחסמה במנגנון ההרשאות ודורשת
החלטה אנושית. קובץ `.patch` כאן **⊥ משנה כלום** עד שמריצים אותו במפורש.

## `2026-08-08-calendar-reconciliation.patch`

**מה זה מתקן.** ריצת הערב של מסי (משימה 6 בגל 1, קומיט `6de11f3`) קיבלה הרשאה
לכתוב ליומן אוטונומית. הביקורת מצאה שני עימותים מול חוקים נעולים קיימים:

1. **מיקום הסמן.** `docs/ceo/reference/luz_rules.md` (נעול טום 2026-08-01) קובע
   שהסמן `[cos-os] type=… week=…` יושב **בגוף** האירוע והכותרת נושאת קידומת
   `GT · `; חוק הבעלות שם בודק את הגוף. `6de11f3` כתב שהסמן הוא קידומת
   **בכותרת** — ∴ מסי ⊥ מזהה בלוקים קיימים כשלו, וסיכון לכפילויות.
2. **שתי סמכויות על אותו יומן.** `chief-of-staff-daily` עדיין מצהיר שכתיבה ליומן
   קורית רק אחרי אישור ב-day-close, והטריגר של 17:00 חי.

**מה הפאץ' עושה.** מיישר את ריצת הערב למוסכמה הנעולה (הנעול גובר — הכרעת טום
2026-08-06 נתנה אוטונומיה, ⊥ שינתה פורמט), ומוסיף הערות החלפה מפורשות
ב-`chief-of-staff-daily` (G5 · G6 · V2 · §C כתיבות · תיאור הפרונטמטר ·
שלבים 4–5 · סעיף הטריגרים) — כולן מצטטות "טום 2026-08-06".

**מצב אימות.** סריקת ניסוחים ישנים: 0 שאריות. שלושה קבצים, 30+/19−.

## החלה

```bash
git apply docs/superpowers/pending/2026-08-08-calendar-reconciliation.patch
git add .claude/skills/messi/SKILL.md \
        .claude/skills/messi/reference/evening-run.md \
        .claude/skills/chief-of-staff-daily/SKILL.md
git commit -m "messi+cos-daily: reconcile evening calendar grant with locked luz_rules"
```

**עד שזה מוחל: ⊥ לקשור את טריגר 20:00.** הטריגר כותב ליומן אוטונומית, ובלי
התיקון הוא עובד לפי מוסכמת סמן שגויה מול ריטואל שעדיין תובע את אותה סמכות.
נכון ל-2026-08-08 הטריגר ⊥ נקשר ו-⊥ רצה הרצת ניסיון — שום דבר בגל 1 אינו פעיל.
