# Cowork "Advance Your Lane Now" Prompt — WhatsApp Coexistence via Dualhook

**For:** Tom · **Date:** 2026-06-25 · **Use:** paste into Cowork (its division-of-labor charter is
already in the project instructions). This is the execution kick: drive the transport lane to the
furthest point reachable without Claude Code's webhook URL.

---

```
GO — advance your lane (WhatsApp Coexistence transport) as far as possible RIGHT NOW. Follow the division-of-labor in your project instructions. Method: Coexistence via Dualhook, the number is NEVER disconnected, the bot stays OFF the whole time.

TODAY'S TARGET (the furthest point reachable without Claude Code)
Coexistence LIVE on +972543982444 (the WhatsApp Business app still working) + the non-secret IDs collected, with Dualhook's Webhook Override left UNSET (you'll get that URL from Tom later). Get all the way there.

HOW YOU WORK
Autonomous by default — navigate, read, click, fill yourself. Pause ONLY at a human gate (login / SMS code / document upload / in-app QR approval / token / billing). At a gate: ONE exact Hebrew instruction to Tom, wait for "done"/the value, resume immediately. Talk to Tom in Hebrew; drive the English UIs yourself.

DO NOW, in order:
1) Profile lock check — say to Tom (Hebrew): "פתח WhatsApp Business → הגדרות → פרופיל עסקי, ותוודא שתמונה + שם + 'אודות' סופיים (הם ננעלים אחרי החיבור). כתוב 'מוכן'." Wait.
2) Dualhook free trial — open dualhook.com, start the free trial, guide Tom through signup screen by screen. (email/login verify → gate)
3) Start the WhatsApp COEXISTENCE onboarding inside Dualhook (Embedded Signup). Choose the coexistence / "connect your existing WhatsApp Business app account" path — NEVER migrate/disconnect. Select +972543982444. If Dualhook's UI differs from what you expect, describe what you see and proceed toward the coexistence option; if only a migrate/disconnect path is offered, STOP and tell Tom.
4) Connection approval — Tom scans the QR in the WhatsApp Business app (the prompt Dualhook/Meta shows). Say exactly where to tap; wait for "חיברתי".
5) Business verification — if Meta requests it, start it; Tom uploads docs (gate); record status (pending/approved) and CONTINUE, don't block.
6) Verify the number is connected on BOTH sides: still active/receiving in the WhatsApp Business app AND listed as connected in Dualhook / WhatsApp Manager. Confirm with Tom the app still works normally.
7) Leave the Webhook Override EMPTY — Claude Code's URL is not ready. Do not point it at anything (and never at Make).
8) Collect the NON-SECRET IDs and give them to Tom: Phone Number ID, WABA ID, App ID. Any secrets (Dualhook API key, Meta token, App Secret) → tell Tom to copy them into his .env himself; you never read, echo, or store them.

STOP POINT — report this to Tom in Hebrew when reached:
"Coexistence מחובר ✅ האפליקציה עובדת רגיל. IDs (לא סודי): Phone Number ID=…, WABA ID=…, App ID=…. Webhook עדיין לא מוגדר — ממתין ל-URL מ-Claude Code. אימות עסק: <אושר/ממתין>."

Reaching that stop point IS success for today. The remaining step (pointing the webhook at Claude Code's receiver) waits for the URL — that's expected; do not improvise a Make/no-code flow to fill the gap.

IF STUCK
After 2 failed attempts on a step: stop, describe exactly what's on screen, ask Tom one precise Hebrew question.
```
