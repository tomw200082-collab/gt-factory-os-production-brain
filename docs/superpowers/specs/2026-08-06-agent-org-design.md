# עיצוב הארגון של צוות הסוכנים — הכרעות וספר המבנה

> **סטטוס: DRAFT — הצעה, לא סמכות.** נכתב 2026-08-06 בסשן תכנון עם טום.
> אין לעדכן את הראוטר, את AGENT_TEMPLATE או כל מסמך סמכות מהקובץ הזה לפני אישור בכתב של טום (פרק 11 בספר).
> הספר המלא (צ'רטרים, תפרים, רוטינות, ניקיון): https://claude.ai/code/artifact/20859ed0-c508-4d3e-bdff-bd73aad914aa

## הכרעות טום — 2026-08-06 (סיעור מוחות, נרשמו בזמן אמת)

| # | מתח | הכרעה |
|---|---|---|
| T1 | סוכן מול סקיל | צ'רטר לכל סוכן; SKILL.md = שכבת ביצוע בלבד |
| T2 | שני עולמות | ארגון אחד תחת מסי; ההנדסה = מחלקה בראשות factory-os-governor |
| T3 | שיגור | רק מסי משגר; כל ספק מפנה לצ'רטר בשם + בלוק גבולות, נאכף ב-hook; אין רקורסיה (חריג: פאן-אאוט רמה-אחת של הפורטל) |
| T4 | הראוטר | מסי מריץ את AI_BRAIN_ROUTER; governor שומר ורדיקט הנדסי + HOLD; מחלוקת → טום |
| T5 | שער מודול | MODULE_TEMPLATE דו-מסלולי: הנדסי (18 סעיפים) / עסקי (מוקל); Sales-Machine = המודול העסקי הראשון |
| T6 | תקן ראיות | שני תקנים, עיקרון אחד: הנדסי = 6 שכבות; עסקי = דרגות סמכות + ראיות מתוארכות; טענה הניתנת לבדיקה מול מערכת חיה חייבת system_verified |
| T7 | ריצה בענן | טום מקים רוטינות בממשק claude.ai (קונקטורים נצמדים שם); הספר מספק פרומפט + הוראות שדה-שדה לכל רוטינה |
| T8 | אכיפה | סט מכני מינימלי M1–M5: hook שיגור · CI רישום במוח · יישור settings · עיגון דגל המכירות בקוד · הגנת ledger בבקאנד |
| T9 | אוטונומיה | סולם L0–L4 בכל צ'רטר; מסי מרכז לדייג'סט יומי; L3/L4 אצל טום |
| T10 | דריפט | CI מזהה, ops-docs-curator מתקן; פריט קבוע בצ'קפוינט של מסי; בלי סוכן חדש |
| T11 | פנקס יחיד | מעבר מנושן לגוגל שיטס; כל שיגור/טראנש/אבן = שורה בפנקס |
| V | החזון | שיפוט מואצל: צ'רטר + פרופיל החלטה + דוח מוכן-להחלטה + לולאת כיול ("כן, בדיוק") |
| W1 | גל ראשון | 6 סוכני שיפוט (L0–L1): מודיעין מכירות ולקוחות · רכש וציוד · תמחור ופיננסים · שיווק וקריאייטיב · funnel · SEO; מתחילים ברכש + מכירות |

## תמצית העיצוב

- **מבנה:** טום → מסי (מנכ"ל, דלת קדמית יחידה, מריץ הראוטר, משגר יחיד, בעל הפנקס) → 5 אגפים: לשכת המטה · תפעול · דלפק עסקאות · שיפוט ומחקר (חדש) · הנדסה (governor). בני אדם = הידיים; סוכנים מכינים טיוטות בלבד.
- **שיפוט מואצל:** כל סוכן שיפוט נושא פרופיל החלטה ("איך טום חושב בתחום") כקלפים מדורגים בבית `docs/judgment/<agent>/`, מחזיר דוח מוכן-להחלטה, ומתכייל משאלות בסוף כל דוח. מחקר = L0–L1, לכן אוטונומי מלא מהיום הראשון.
- **חוזה השיגור:** ספק-לפני-שיגור (dispatch.md) + הפניית `charter:` בשם במקום שני המשפטים הידניים; hook חוסם Task בלי צ'רטר/גבולות.
- **הפנקס:** Google Sheets — טאבים: משימות · פרויקטים · שבועות · יומן אירועים; כתיבה דרך מסי בלבד; ⊥ יעד בשישי/שבת; ארכוב במקום מחיקה. 4 כותבי-נושן (messi, chief-of-staff-daily, weekly-opening, meeting-summary) מוסבים; notion_contract → sheets_contract.
- **רוטינות (R1–R7):** guardian 06:30 א'–ה' (מתקן את באג ה-cron החי שני–שישי) · checkpoint 13:00 · day-open 07:30 · day-close 17:00 · queue-guard ה' 15:50 · sunday-prep+weekly מוצ"ש 20:00 · night-run חד-פעמי אחרי אישור ערב.
- **ניקיון לפני הוספה (עיקרי P0/P1):** ענף הסשן בשני workflows של הפורטל → main; gt-recruitment-outreach מ-~/.claude האפמרלי → המוח; שערים ל-drinks-pricelist / customer-setup / goods-receipt; תיקון כללי push/merge שבוטלו ב-5 קבצי סוכנים + AGENT_TEMPLATE + VERDICT_GLOSSARY; REGISTRY 7→42; session_start מיושן; מחיקת gt-axis-*×4 ו-manufacturing-intelligence (באישור טום).

## מקורות

- מאסטרפרומפט האודיט: `~/.claude/uploads/.../bf19f621-20260806agentorgdesignmasterprompt.md` (אודיט 4 ריפואים, 2026-08-06).
- סריקת עומק חיה, 9 קוראים מקבילים (סוכני מוח/פורטל, 17 סקילים, ממשל, אכיפה, Sales-Machine, מיפוי אנושי) — workflow `gt-org-audit`, 2026-08-06.
- סכימת הצ'רטר: `docs/factory-mapping/2026-07-22-mapping-v3-tom-responses.md` (10 שדות + 4 שדות תפר).

## הצעד הבא

טום מעיר במשבצות C-0..C-11 בספר → קלוד מדייק → נעילה → כתיבת קובצי הצ'רטר (`.claude/agents/`, וריאנט עסקי) ותחילת שלב 1 בתכנית ההגירה — הכל אחרי האישורים בפרק 11.
