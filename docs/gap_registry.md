# GT Factory OS — Gap Registry

> **Authority:** Persistent ranked list of all known operational gaps. Updated after every audit and tranche close.
> **Schema per entry:** ID | Severity | Layer | Description | Blocked by | Status | First detected | Closed

Severity scale:
- **P0** — Blocks any operator from performing their primary daily job
- **P1** — Operators can work but require significant manual workaround or developer help
- **P2** — Works but slower, less safe, or less clear than it should be
- **P3** — Polish, optimization, advanced feature, or stretch goal

---

## Open Gaps

| ID | Severity | Layer | Description | Blocked by | Status | First detected |
|----|----------|-------|-------------|------------|--------|---------------|
| GAP-032 | P1 | Layer 3 | Two Supabase Edge Functions are ACTIVE in production with **no source in `supabase/functions/`**: `sales-lead-fanout` (v5, hand-deployed 2026-08-25 to fan new-lead alerts to Alex and Avi while GitHub Actions had no runner) and `db-url-probe` (created 2026-08-27 during the credential outage, since neutralised to a read-only report with `verify_jwt` back on). Both were hand-deployed to work around blocked delivery; both are the deployed-vs-repo drift the workspace otherwise warns about. `db-url-probe` briefly served an unauthenticated route that returned a live connection string — that route is closed (401 verified) but the password it emitted should be rotated on a planned, verified cycle. | `db-url-probe`: delete from the dashboard (no MCP delete tool). `sales-lead-fanout`: deploy `sales-leads-poll` from `main` (gt-factory-os#240 is merged but never deployed — Actions restored 2026-08-27, so no longer blocked), then `drop trigger if exists lead_alert_fanout on sales_core.lead_event;` per gt-factory-os#241. | OPEN | 2026-08-28 |
| GAP-033 | P2 | Layer 3 | Every Edge Function reaching Postgres (`factory_os_jobs`, `sales-leads-poll`, `shopify_available_reconcile`) connects **only** through the hand-maintained `DATABASE_URL_POOLED`. When that secret goes stale — as on 2026-08-27, for 19 hours — all of them fail simultaneously, including the lead intake. `SUPABASE_DB_URL` is injected and kept current by the platform and stayed valid the whole time; a fallback to it would have made the outage invisible. Also `api/src/server.ts` reads `DATABASE_URL_POOLED ?? DATABASE_URL`, so a stale POOLED silently overrides a fresh DATABASE_URL. | Needs a reviewed PR: try the pooled URL, fall back to `SUPABASE_DB_URL` on connection failure, and log which one served. Direct connections have a lower cap than the pooler, so the fallback must stay a fallback, not the default. | OPEN | 2026-08-28 |
| GAP-003 | P2 | Layer 1 | LionWheel demand partially excluded — hybrid resolver live (rows_unknown_sku=0 post-deploy); 330/404 lines (81.7%) resolved; 74 unresolved lines across 24 distinct SKUs remain outside planning model: bundles (GTSET-*, 9 SKUs, 44 lines — bundle policy tranche needed), held aliases (GTCC-MUZ-JASM-1L + GTCC-MUZ-PNMM-1L — Tom mapping confirmation needed, 5 lines), non-catalog (13 SKUs, ~25 lines), malformed (2 SKUs, 2 lines). Exception count: 65 open = 41 stale (now-resolvable SKUs with historical exceptions) + 24 current. False-green: exception count overstates true current failures. **Visibility now live (Loop 8 2026-04-23):** `GET /api/v1/queries/planning/demand-coverage` endpoint returns live counts; planning runs page tile now shows dynamic resolved/bundle/unresolved breakdown. | Bundles blocked on bundle-policy tranche (Loop 2). JASM/PNMM held for Tom. Non-catalog needs catalog review. Stale exceptions need admin cleanup (low priority P3). | OPEN | 2026-04-23 |
| GAP-004 | P3 | Layer 1 | Dashboard KPI aggregation mostly live — jobs health (Loop 5), rebuild_verifier/parity-check tile (Loop 15), break-glass state tile (Loop 15) all wired. Remaining: integration freshness tile (W4 spec complete, Loop 15; W1 endpoint needed: `GET /api/v1/queries/integration-freshness`), RUNTIME_READY registry tile (UNRESOLVED-DR-1: no DB-backed table). runs-today and last-movement still backend-blocked (lower priority). | integration-freshness endpoint needed (W4 spec ready); runtime_ready_signals table migration needed | OPEN | 2026-04-23 |
| GAP-005 | P1 | Layer 1 | Recent-submissions surface missing — **CLOSED Loop 9 2026-04-23**: `GET /api/v1/queries/submissions/recent` built and deployed (Railway commit 061cda5); portal `/stock/submissions` page live with status badges (posted/pending/rejected/cancelled); "My Submissions" nav entry added. | — | CLOSED | 2026-04-23 |
| GAP-008 | P1 | Layer 2 | LionWheel SKU alias seeding workflow portal had two blocking bugs: (1) missing `idempotency_key` fixed in 8543d2b; (2) `extractExternalSku()` always null (reads `detail` as JSON object; LionWheel sets plain text) fixed in 96516ae — both portal bugs resolved; portal now shows unmapped SKUs, shows item names, surfaces resolved count | Deploy portal to Vercel + seed 39 Bucket A aliases from `/admin/sku-aliases` | OPEN | 2026-04-23 |
| GAP-009 | P2 | Layer 1 | Admin CRUD surfaces partial — `/admin/users` now fully wired (role change + status toggle, Loop 14); `/admin/jobs` and `/admin/integrations` health screens still backend-blocked (no dedicated admin mutation endpoints) | Jobs/integrations endpoints needed | OPEN | 2026-04-23 |
| GAP-010 | P2 | Layer 1 | Count discrepancy threshold (auto-post vs. approval) is a proposed default only — not data-calibrated | Must review after 2 weeks of real operation | OPEN | 2026-04-23 |
| GAP-011 | P2 | Layer 2 | Scrap semantics not confirmed with operators — operators must understand scrap reduces FG only, NOT RM consumption | Requires explicit operator training | OPEN | 2026-04-23 |
| GAP-012 | P2 | Layer 3 | On-prem read-only replica refresh cadence not decided — acceptable staleness for dashboard fallback undefined | Pending Tom input | OPEN | 2026-04-23 |
| GAP-013 | P2 | Layer 2 | Green Invoice price-change auto-update threshold — **decided 25% by Tom 2026-06-11 (T3, improvement-plan)**; policy-key implementation landed with the price-truth pipeline (cost drafts 0229/0230). Remaining: GI expense-pull ingest itself is still contract-only (see chain audit 2026-06-12 finding CHAIN-P6 — build-vs-descope pending) | GI ingest build decision | DECIDED / IMPLEMENTATION IN FLIGHT | 2026-04-23 |
| GAP-019 | P2 | Layer 1 | Bundle demand excluded from planning — 9 GTSET bundle SKUs (44 lines, 47 bundle-units) are in LionWheel orders but have no matching items; each bundle = 3-6 FG bottles; true FG demand from bundles is ~140-280 hidden units (composition unknown). Planner sees partial demand only. `api_read.v_excluded_demand` view created 2026-04-23 for visibility. Needs: (1) Tom to define bundle composition (which FG items in each GTSET type), (2) W1 migration adding bundle BOM table, (3) demand explosion logic. | Blocked on bundle BOM business decision (Tom) | OPEN | 2026-04-23 |
| GAP-020 | P2 | Layer 1 | Non-catalog SKUs in LionWheel orders — 10 SKUs (22 lines, 52 units) not in items table. Breakdown: GTCC-MUZ-APPZ-1L/PSSP-1L/ANBL-1L/SMAR-1L/BLBR-1L/TROJ-1L (Muza cocktail flavors likely real products), GT-PUE-FRE-1L (possibly real), GT-GLA-CUP (glass/merchandise, not a beverage), AP-DRI-ORA/ROS (possibly Aperol-brand product). Plus GTMN-PIK-254 (bottle deposit fee, explicitly excluded — not a product). Fee/deposit line items should be permanently excluded. New catalog items should be added to `items` table and aliases seeded. | Business decision needed on which SKUs are real products to catalog vs. permanent non-product exclusions | OPEN | 2026-04-23 |
| GAP-021 | P3 | Layer 2 | Production movement types use inconsistent casing — `production_output/consumption/scrap` (lowercase) vs `GR_POSTED/WASTE_POSTED` (uppercase). No immediate functional issue but inconsistent for filtering and reporting. Should be normalized in a future migration + handler update. | Low priority; no functional impact today | OPEN | 2026-04-23 |
| GAP-014 | P3 | Layer 2 | `.mcp.json` disposition undecided — keep, remove, or document as non-runtime reference | Pending Tom input | OPEN | 2026-04-23 |
| GAP-015 | P2 | Layer 2 | LionWheel live API field names not verified — sandbox credential exists but field names require live inspection | Requires live API inspection session | OPEN | 2026-04-23 |
| GAP-016 | P0 | Layer 0 | Tom has never logged into production portal via magic-link — self-link mechanism untested with real credentials | Human-only step | OPEN | 2026-04-23 |
| GAP-023 | P1 | Layer 2 | BOUGHT_FINISHED items have no seeded cost — 7 supplier_items rows with `item_id IS NOT NULL` have `std_cost_per_inv_uom = null` after migration 0075 seed. Admin must set costs manually for BOUGHT_FINISHED items via the new cost edit cell. Tom action required. | Tom must set costs for 7 BOUGHT_FINISHED items via `/admin/masters/suppliers/[id]` supplier-items tab | OPEN | 2026-04-23 |
| GAP-026 | P0 | Layer 2 | Goods receipt booked **15,005,002 units** of `PKG-BAG-MAT-500G` (Matcha 500G Bag). `GR_POSTED`, `event_at` 2026-08-06, `source_id c7db9391-d7e8-44c2-b973-fd466b0cef27`, uom UNIT. Shape of a barcode typed into the quantity field; real figure is plausibly 5,002 or 5,000. Single row accounts for the whole 15.1M `GR_POSTED` net in the last 40 days, so any packaging coverage or valuation read off `current_balances` is meaningless until corrected. | Stock-truth write — correction is a reversal row only, needs Tom's approval and the sanctioned count/reversal path. ⊥ UPDATE the ledger. | OPEN | 2026-08-06 |
| GAP-027 | P1 | Layer 2 | Three negative / impossible finished-goods balances: `ADD-UBE-500G` = −26, `GT-MAT-KIT` = −2, `FG-MAT-30G` = 0 on hand with 30 shipped in the last 40 days. Goods left the building without a matching production or receipt posting, so the repack/kit legs are under-reported. | Needs a physical count per item, then the approved count-correction path. Overlaps the open ube 1kg count Tom owes. | OPEN | 2026-08-06 |
| GAP-028 | P2 | Layer 2 | 150 kg of matcha booked as `WASTE_POSTED` (100 kg 2026-07-26, 50 kg 2026-08-04) while the row notes say it was sent to Nikitin for repacking into 18G. That is a **transfer, not waste** — it inflates the waste report and hides the external-holding position. `RAW-MATCHA-AT-AMITEA` already exists as the correct pattern for stock held at an external packer; Nikitin has no equivalent item. | Needs an item for the Nikitin holding + reversal/re-post of the two rows. Stock-truth write, Tom approval. | OPEN | 2026-08-06 |
| GAP-029 | P0 | Layer 0 | A live-shaped **Shopify Admin API token is hardcoded in tracked source** at `gt-factory-os` `scripts/cleanup_shopify_sku_map.mjs:73` (`shpat_…`, store `greenteaeveryday.myshopify.com`), present in every one of the 55 commits visible from a shallow clone. The repo is private today, so this is not yet an exposure — but deleting the line does **not** remove it, because going public exposes history, not just HEAD. Holder could read orders and customers (PII), change inventory and prices, create discounts. Scan of the other three repos (portal, brain, Sales-Machine — the first two already public) found nothing. | Rotate first, per `docs/runbooks/credential_rotation.md` §SHOPIFY_ADMIN_API_TOKEN, then set the new value in Supabase Edge secrets in the same minute (no dual-key window — the reconciler cron 24 breaks until both are done), then strip the literal from source. Blocks making `gt-factory-os` public. Token value deliberately not recorded here. | OPEN | 2026-08-18 |
| GAP-030 | P2 | Layer 1 | Portal e2e `tests/e2e/production-picking.spec.ts:95` is timing-fragile: it clicks a plain Next `<Link>` and asserts `toHaveURL` with a **5s** timeout, while CI runs `next dev`, so `/production/runs/[run_id]` compiles **on demand** at click time. Observed passing in 4.6s on one run and failing on the next with byte-identical app code, purely because that runner was ~23% slower. Costs a CI cycle each time it fires and trains readers to discount red builds. | Raise the timeout on that one navigation, or pre-warm the route. Deliberately not fixed inside tranche 163 — the spec is outside that tranche's manifest and widening it would break portal invariant 1. Needs its own XS tranche. | OPEN | 2026-08-18 |
| GAP-031 | P2 | Layer 1 | `GT_Summer_Menu_2026.xls` (Tom's drink price list, the source the FOOD COST model is built from) carries three data defects: the `מחיר ₪` column exists but is **empty** for all 48 drinks, so sale prices cannot be taken from it; the matcha rows carry an autofill drag (`50, 51, 52, 53, 54, 55 מ״ל`) instead of a constant 50 ml dose; and the ICE UBE block is shifted one row against its column headers. The 2026-08-27 cost model worked around all three rather than patching the file. | Tom to fix the sheet at source — otherwise every future re-run has to re-apply the same three workarounds, and the empty price column will keep blocking any attempt to drive prices from the file. | OPEN | 2026-08-27 |

---

## Closed Gaps

| ID | Severity | Layer | Description | Closed | Evidence |
|----|----------|-------|-------------|--------|---------|
| GAP-006 | P1 | Layer 2 | GR reversal does not decrement PO `received_qty` (UNRESOLVED-GP-1) | 2026-04-24 | Migration 0082 reversal trigger verified applied; `CURRENT_STATE.md` UNRESOLVED-GP-1 CLOSED. Registry row was stale-OPEN until the 2026-06-12 chain audit cross-reference flagged it |
| CLOSED-001 | P0 | Layer 0 | Railway API not confirmed healthy | 2026-04-23 | `GET /health` → `{"ok":true}` HTTP 200 |
| CLOSED-002 | P0 | Layer 0 | Railway env vars not confirmed set | 2026-04-23 | `railway variables` confirmed: DATABASE_URL_POOLED, SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, NODE_ENV=production, ENABLE_DEV_SHIM_AUTH=false |
| CLOSED-003 | P0 | Layer 0 | Vercel portal not confirmed live | 2026-04-23 | `https://gt-factory-os-portal.vercel.app/` → HTTP 200; `/dashboard` → 307 → `/login` (middleware gate confirmed) |
| CLOSED-004 | P0 | Layer 0 | Vercel env vars not confirmed set | 2026-04-23 | `vercel env ls` confirmed: API_BASE, NEXT_PUBLIC_ENABLE_DEV_SHIM_AUTH, NEXT_PUBLIC_API_BASE, NEXT_PUBLIC_SUPABASE_ANON_KEY, NEXT_PUBLIC_SUPABASE_URL all present in Production |
| CLOSED-005 | P0 | Layer 0 | `operational_dataflow_blueprint.md` not authored | 2026-04-23 | `PRODUCTION/docs/operational_dataflow_blueprint.md` created |
| CLOSED-006 | P0 | Layer 0 | First live production event never posted (was GAP-001) | 2026-04-23 | `WASTE_POSTED` RAW-WHISKEY -0.01L, `movement_id=429b94d5`, `reported_by=tom@gteveryday.com`, `rebuild_verifier()=0`; ledger→projection chain verified; planning round-trip confirmed (run_id=0b53afb8) |
| CLOSED-007 | P0 | Layer 0 | Tom never logged into production portal (was GAP-016) | 2026-04-23 | `reported_by_user_id=0db008a9` (tom@gteveryday.com, role=admin) confirmed on live ledger event; magic-link auth chain confirmed end-to-end |
| CLOSED-008 | P1 | Layer 0 | Railway env vars not confirmed via live DB query (was GAP-002) | 2026-04-23 | First live DB write succeeded (WASTE_POSTED reached Supabase); `DATABASE_URL_POOLED` confirmed pointing to correct project (rvadsozabmxkkrktwgnv); `reported_by_snapshot` fix also deployed via `railway up` (deployment c3d66703, health=ok) |
| CLOSED-009 | P0 | Layer 0 | Layer 0 step 5f — exception/approval path not yet tested end-to-end | 2026-04-23 | Exception `7283a2d2` confirmed: positive_adjustment, RAW-VODKA, qty=50, status=open; form_submission `56c1be71` pending NOT posted; `current_balances` RAW-VODKA=0.00 unchanged; zero ledger writes. Tom declared Layer 0 CLOSED 2026-04-23. |
| GAP-017 | P1 | Layer 1 | Approval review surface (waste/adjustment + physical count) lacked decision-grade context | 2026-04-23 | Backend GET `/api/v1/queries/waste-adjustments/:id` + `/api/v1/queries/physical-counts/:id` deployed (c29d354, Railway). Portal approval pages rewritten with `useQuery`-driven detail cards showing item name, direction/delta, amount, reason, submitter, timestamps, exception category (70cc491, `dpl_9Uaq5iZic2fYansc9ZGnojBQA6aG`). Live DB verify: submission `56c1be71` → `{item: Vodka, direction: positive, qty: 50 L, submitter: Tom, exception: positive_adjustment}`. Railway GET → 401 (route registered). Portal build clean 63 pages. |
| GAP-007 | P1 | Layer 2 | Production Actual portal form stub needs wiring — RUNTIME_READY(ProductionActual) emitted but portal form is a stub | 2026-04-23 | FALSE GREEN RESOLVED: portal form at `src/app/(ops)/stock/production-actual/page.tsx` is fully wired — two-step flow (item pick → BOM snapshot → output/scrap submit), consumption preview panel, pinned bom_version_id submission. Proxy routes `/api/production-actuals/route.ts` + `/api/production-actuals/open/route.ts` present. Audit confirmed 2026-04-23 Loop 6 opening sequence. |
| GAP-024 | P0 | Layer 2 | `GET /api/v1/queries/stock/value` SQL error — lateral join referenced `supplier_items.std_cost_per_inv_uom` which did not exist (column lives on `components`). Endpoint failed at DB runtime on every authenticated call. Introduced Loop 10. | 2026-04-23 Loop 11 | Fixed: migration 0075 added `std_cost_per_inv_uom` to `supplier_items` (Path B); 165/198 supplier_items seeded; value-handler.ts lateral join now valid; supply_method added to response. Endpoint returns HTTP 200 with real cost data. Commits 5e65e31 + 71c58c5. |
| GAP-022 | P1 | Layer 2 | Supplier-items GET list response did not return `std_cost_per_inv_uom` — admin cost edit cell started blank (user could not see current value before overwriting). | 2026-04-23 Loop 13 | W1 2-line fix: `std_cost_per_inv_uom::text` added to SELECT in `handler.ts`; field added to `SupplierItemRow` interface in `schemas.ts`. pgTAP 1/1 green. Railway commit `3b787a0`. |
| GI-EDGE-FN-UUID-CAST | P1 | Layer 3 | Green Invoice Edge Function `giResolveSupplier()` used `$1::uuid` cast on `suppliers.green_invoice_supplier_id` (a text column added by migration 0071). Caused hourly GI poll failures since migration 0071 applied (~10:00 UTC 2026-04-23). 379 invoice rows already mirrored; watermark paused. | 2026-04-23 Loop 13 | Changed cast to `$1::text`. Commit `ec7b600`. Edge Function redeployed. GI watermark resumed from prior position. Self-contained fix — not a gap in application logic, a type mismatch introduced by migration. |
| GAP-025 | P2 | Layer 3 | `ADD-ORANGE-100G` (DRIED ORANGE 100G, REPACK) holds on_hand=29 from a balance anchor with **zero `stock_ledger` rows ever** and no `integration_sku_map` rows. Its twin `ADD-GAR-ORA-DRY` is the live item (35 ledger rows, last movement 2026-07-31, mapped `shopify:AP-DRI-ORA`, syncing). Two items for one physical product; the 29 units sit on the orphan. | Needs a physical count — resolution is a stock-truth write (reversal + re-post onto the twin, or retire the orphan), not a mapping change. ⊥ map both to one `external_sku`: the reconciler would write two answers per cycle for the same variant. | CLOSED | 2026-08-01 | Closed 2026-08-01 by migration 0314. Tom counted 29 units physically; COUNT_ADJUST pair (-29 orphan / +29 ADD-GAR-ORA-DRY, reason PHYSICAL_COUNT_CORRECTION) re-posted the stock onto the live mapped record, and ADD-ORANGE-100G was retired to INACTIVE. rebuild_verifier()=0 after. Shopify AP-DRI-ORA now syncs 29. |

---

*Registry initiated: 2026-04-23.*
*Next update: after Layer 0 closed-loop validation (GAP-001 and GAP-016 closure requires Tom's first live login and first stock event).*
