# הזמנה גדולה · 18,400 בקבוקי 300ML · מסירה 16.08.2026

דשבורד ייצור שעה-שעה לשבוע 09–16.08. `week-0816-dashboard.html` — קובץ יחיד, ללא תלויות
חיצוניות, פונט מוטמע. נפתח בדפדפן, שומר סימונים והערות ב-localStorage, ומייצא את הכל
כטקסט אחד לסשן חדש.

## מה יש בו

- **שש כרטיסיות חסם** — כל אחת עם הפעולה שסוגרת אותה, איש הקשר, והשעה.
- **שעה-שעה, ראשון 09.08 → ראשון 16.08** — ארבעה מסלולים במקביל
  (בישול · מילוי ופקק · תיוג ואריזה · רכש ולוגיסטיקה), 71 שורות.
- **ערכות שקילה** לבאצ׳ של 460 ליטר, לארבעת הטעמים.
- **טבלת חומרי גלם** של הפרויקט בלבד, ומולה **טבלת אריזה**.

## המספרים הנעולים

| | |
|---|---|
| הזמנה | 4 × 4,500 = 18,000 |
| ייצור | 4 × 4,600 = **18,400** (100 רזרבה לטעם) |
| באצ׳ים | **12 × 460 ל׳** = 5,520 ל׳ |
| למה 460 | מתחת לשיא ההיסטורי 507.5 ל׳ — אין סיכון קיבולת מיכל |
| חלון ייצור | שני 10.08 → שישי 14.08. שבת = חלון חירום. |

סדר הבישול — FRESH, DETOX, CALM, REVIVE — נקבע כדי ש-REVIVE יהיה אחרון, כי הפסיפלורה
היא הפער הפתוח היחיד בחומרי הגלם. זה קונה את מירב הזמן לסגור אותו.

## מקור הנתונים

כל הכמויות נשלפו חי מ-Postgres (`rvadsozabmxkkrktwgnv`) ב-09.08.2026:

- ערכות שקילה — `private_core.bom_head` + `bom_lines`, יחס
  `final_component_qty / final_bom_output_qty × 460`.
  **⚠ `qty_per_l_output` בטבלה סותר את היחס הזה** בעד 8.5% (היביסקוס: 0.0549 מול 0.0596).
  הדשבורד משתמש ביחס המתכון המקורי — הוא הנכון פיזית. פער ה-BOM עצמו פתוח.
- מלאי — `private_core.current_balances`.
- הזמנות פתוחות — `purchase_order_lines` בסטטוס `OPEN` תחת PO `OPEN`/`APPROVED_TO_ORDER`/`PARTIAL`.
- ספקים וזמני אספקה — `supplier_items` + `suppliers`.

## הנחות שטרם אומתו

1. **418 בקבוקים לשעה במילוי.** מחושב אחורה מהיעד, לא נמדד. כל לוח הזמנים תלוי בו.
   נבדק במילוי ניסיון, שני 13:00.
2. **אדם שני למילוי מ-11.08.** מהשלישי עד השישי הבישול והמילוי רצים באותן שעות; דניס
   לבדו 06:00–17:00 לא יכול לכסות את שניהם. 12 באצ׳ים בארבעה ימים = 3 ליום, מול קצב
   היסטורי מאומת של 1 ליום.
3. **קוטר צוואר הבקבוק.** PO-2026-00307 הזמין `PKG-CAP-PLASTIC-28-500ML` (28 מ״מ),
   בעוד ה-BOM של 300ML קורא ל-`PKG-CAP-300ML`. טרם אומת שהם מתאימים.
4. **פורמט האריזה החיצונית.** ה-BOM אומר קרטון של 12 (1,534 יח׳); התכנון הקודם דיבר על
   ארגז 54 (341 יח׳). שתי אפשרויות שונות, החלטה פתוחה.

## עדכון הדשבורד

בסוף השבוע (או בכל נקודת בקרה) — כפתור **"העתק לסשן"** מייצר בלוק טקסט אחד עם כל
ההערות והסימונים. הדבקה שלו בסשן חדש מספיקה כדי לייצר גרסה מעודכנת.

---
נוצר 09.08.2026 · לצד `../blueprint/` (שרטוט המפעל, חלאייב 15 חולון).
