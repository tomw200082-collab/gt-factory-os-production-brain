# UX Release Gate — Procurement Corridor

**Date:** 2026-07-16 · **Run:** wf_8a93229b-763 (53 agents, render-grade) · **Invoked by:** Tom via `/ux-release-gate` in chat

## Scope

Corridor: procurement-planning skill (chat) → purchase session → approval → `APPROVED_TO_ORDER` → placement queue → place.
Routes: `/planning/procurement` (+ FocusMode, `[session_po_id]/sheet`), `/planning/purchase-session`, `/purchase-orders/placement-queue`, `/purchase-orders`.

## Visual evidence

8 screenshots (desktop 1440×900 + mobile 390×844) via committed `tests/e2e/ux-shot.spec.ts`, dev-shim auth, fixture-driven — zero portal files edited. Paths + fixtures in the run journal. Note: viewer-role shot of the placement queue renders the RoleGate block — faithful committed behavior (see FLOW-8).

## Verdict

**HOLD**

Threshold rule is locked: any P0 forces HOLD regardless of the P1/P2 tail. FLOW-8 is a true P0 — the office manager cannot open the surface her daily task lives on, and the code even carries a comment ('office manager signs in as planner') that directly contradicts the described 'viewer' role assignment, so this is an unresolved role-lattice contradiction, not a UI bug. Shipping the corridor without resolving this would ship a broken production flow on day one for the exact operator the placement queue was built for. Compliance is FAIL: portal_ux_standard.md §1 is violated on the two English surfaces (raw enums in filter chips, raw UUID fragments in FocusCard placed-state link, supplier_id field-name tooltip on /purchase-orders) and §3 error-template + §4 status-vocabulary coherence are also breached. The Hebrew/RTL usage on the two authorized surfaces is not a violation — it is Tom-authorized per CLAUDE.md exceptions.

## Blockers (P0)

- FLOW-8 (P0, /purchase-orders/placement-queue): Office manager (task-described role 'viewer') is categorically blocked from her placement queue — page wraps in <RoleGate minimum='planning:execute'> and nav manifest sets min_role:'planner' + required_capability:'planning:execute', while viewer role holds only viewer:read; the page comment even says 'office manager signs in as planner', a direct contradiction with the assigned role. Doreen cannot perform any placement action; her daily job is blocked at the portal entry point. FIX (ARCH_REQUIRED, Tom-only): issue a written role-lattice decision — either (A) reclassify Doreen's Supabase role from viewer to planner, or (B) direct backend to split a new capability (e.g. purchasing:place_order) grantable to viewer and update the RoleGate. Do NOT lower the gate silently. Location: src/app/(po)/purchase-orders/placement-queue/page.tsx:181 + src/lib/nav/manifest.ts:352-356.

**Production-reality note (session verification):** Doreen's live `app_users.role` is `planner` (verified 2026-07-16 in this session), so the daily flow is NOT blocked in production today. The P0 stands as an unresolved role-lattice contradiction: tranche 090's cockpit hard-codes bookkeeper=viewer with a placement-queue tile that the viewer gate makes permanently invisible (dead Hebrew strings), while the queue and its APIs require planner. Tom must still issue the written decision; option (A) formalize-planner is near-zero-effort given the live role.

### Conditions (P1 — next-sprint, named)

- FLOW-5 (P1) — no cancel action on APPROVED_TO_ORDER PO in placement queue
- FLOW-6 (P1) — 'סמן כבוצע — צור הזמנה' button label contradicts post-action 'ממתינה לביצוע' copy; Tom ends session believing order is complete
- FLOW-9 (P1) — session supersede warning does not distinguish placed vs approved vs proposed POs; approved-not-placed work lost silently
- FLOW-1 (P1) — ActionList has no filter/search/sort controls (Tom's explicit requirement unmet)
- FLOW-2 (P1) — placement queue has no filter/search/sort controls
- FLOW-3 (P1) — no delete/remove-from-session action for a session PO
- FLOW-4 (P1) — no defer/postpone flow anywhere in the corridor (Tom's explicit requirement unmet)
- INTERACTION-1 (P1) — defer action missing across corridor
- INTERACTION-2 (P1) — Skip fires with no confirmation dialog, no undo skip
- INTERACTION-3 (P1) — no delete/remove for a session PO
- INTERACTION-4 (P1) — ActionList missing filter/sort
- INTERACTION-5 (P1) — placement queue missing filter/sort
- INTERACTION-6 (P1) — no cancel/reject on APPROVED_TO_ORDER in queue; cancel API only supports OPEN/DRAFT (ARCH_REQUIRED backend extension)
- VISUAL-9 (P1) — no filter/sort/defer affordance across corridor pages
- VISUAL-12 (P1) — raw enum names in /purchase-orders status filter chips (portal_ux_standard.md §1 violation)
- COPY-5 (P1) — 'בוצע' status label contradicts placement queue title 'הזמנות לביצוע' — false completion signal on money action
- A11Y-2 (P1) — FocusMode inner close-confirm alertdialog does not move focus in; outer Tab trap leaks to occluded FocusCard buttons

## Top-ranked actions (single cross-dimension list)

| # | Sev | Effort | Dimension | Route | Finding | Evidence |
|---|-----|--------|-----------|-------|---------|----------|
| 1 | P0 | S | flow | /purchase-orders/placement-queue | Doreen (described in the task as role `viewer`) is categorically blocked from the placement queue. The page wraps in `<RoleGate minimum="planning:execute">` and the nav manifest sets `min_role: "planner"` with `required_… | /home/user/gt-factory-os-portal/src/app/(po)/purchase-orders/placement-queue/page.tsx:181 (`<RoleGate minimum="planning:execute">`); /home/user/gt-factory-os-po… |
| 2 | P1 | S | flow | /purchase-orders/placement-queue | Doreen has no way to cancel or remove an APPROVED_TO_ORDER PO from the placement queue. The only available action is "בצע הזמנה" (place the order). If a PO was approved in error, the supplier is unavailable, or the item … | /home/user/gt-factory-os-portal/src/app/(po)/purchase-orders/placement-queue/_components/PlacementRow.tsx:388-404 (action bar: only "בצע הזמנה" — no cancel, no … |
| 3 | P1 | S | flow | /planning/procurement | The FocusCard's primary action for an `approved` session PO is labelled "סמן כבוצע — צור הזמנה" (Mark as done — create order). After clicking, the post-action success text says "ההזמנה נוצרה וממתינה לביצוע מול הספק" (ord… | /home/user/gt-factory-os-portal/src/app/(planning)/planning/procurement/_components/FocusCard.tsx:748-750 (button label "סמן כבוצע — צור הזמנה"); FocusCard.tsx:… |
| 4 | P1 | S | flow | /planning/procurement | The session supersede confirmation warning says "קיים מושב רכש פתוח עם N הזמנות. מושב חדש יחליף אותו, ואישורים שלא נשמרו יאבדו." The phrase "אישורים שלא נשמרו" (unsaved approvals) is ambiguous: it does not distinguish be… | /home/user/gt-factory-os-portal/src/app/(planning)/planning/procurement/page.tsx:127-130 (warning copy "ואישורים שלא נשמרו יאבדו" — no per-status breakdown); pa… |
| 5 | P1 | S | interaction | /planning/procurement (FocusCard) | Skip ('דלג') fires immediately with no confirmation dialog; there is no undo skip in the UI | /home/user/gt-factory-os-portal/src/app/(planning)/planning/procurement/_components/FocusCard.tsx:694-702 — skipMut.mutate({poId: po.session_po_id}, {onSuccess:… |
| 6 | P1 | S | interaction | /purchase-orders/placement-queue | Placement queue rows have no cancel/reject action; an APPROVED_TO_ORDER PO that should not be placed has no corrective path from this surface — the existing cancel API only supports OPEN/DRAFT status, so even navigating … | /home/user/gt-factory-os-portal/src/app/(po)/purchase-orders/placement-queue/_components/PlacementRow.tsx:388-404 — action area contains only 'בצע הזמנה'; /home… |
| 7 | P1 | S | visual | /purchase-orders | The status filter chips in /purchase-orders display raw database enum values: APPROVED_TO_ORDER, OPEN, PARTIAL, RECEIVED, CANCELLED — in uppercase with tracking-sops letter-spacing. The UX standard explicitly forbids raw… | purchase-orders/page.tsx:98-104 (STATUS_OPTIONS array), purchase-orders/page.tsx:790-817 (filter chip renders `{s}` raw enum string). Screenshot: /tmp/ux-shots/… |
| 8 | P1 | M | flow | /planning/procurement | The ActionList groups session POs into three fixed decision buckets (must_today / can_wait / handled) sorted by order_by_date, but exposes no user-facing filter, search, or sort controls. There is no way to filter by sup… | /home/user/gt-factory-os-portal/src/app/(planning)/planning/procurement/_components/ActionList.tsx:315 (`groupByDecision(pos, today)` call — no filter/search st… |
| 9 | P1 | M | flow | /purchase-orders/placement-queue | The placement queue sorts APPROVED_TO_ORDER POs by order_by_date and surfaces only a page-level overdue count banner (`overdueCount > 0`). There is no search input (by supplier name or PO number), no filter chip (e.g., o… | /home/user/gt-factory-os-portal/src/app/(po)/purchase-orders/placement-queue/page.tsx:162-174 (list rendered directly from `rows` with no filter UI); /home/user… |
| 10 | P1 | M | flow | /planning/procurement | The only way for Tom to disposition a session PO he does not want to order is "דלג" (skip). There is no "remove from session" or "delete" action for an entire session PO. The drop-line checkbox in FocusCard edit mode rem… | /home/user/gt-factory-os-portal/src/app/(planning)/planning/procurement/_components/FocusCard.tsx:661-754 (action bar shows only skip / approve / place — no del… |
| 11 | P1 | M | interaction | /planning/procurement (ActionList) | ActionList has no filter or sort controls; with a multi-supplier session the planner cannot narrow to a single supplier, tier, or date range to batch-process related orders | /home/user/gt-factory-os-portal/src/app/(planning)/planning/procurement/_components/ActionList.tsx:306-314 (ActionListProps: pos, onOpen, today — no filter prop… |
| 12 | P1 | M | interaction | /purchase-orders/placement-queue | Placement queue has no filter or sort controls; with multiple APPROVED_TO_ORDER POs the office manager cannot filter by supplier or sort by urgency to process in batches | /home/user/gt-factory-os-portal/src/app/(po)/purchase-orders/placement-queue/page.tsx:162 — <ul className='space-y-3'> rendered immediately after overdue banner… |
| 13 | P1 | M | copy | /planning/procurement → /purchase-orders/placement-queue (co | The session-PO status label for `placed` is 'בוצע' (done/completed) in both ActionList.tsx and FocusCard.tsx. However, at this point the order has NOT been placed with the supplier; it has only moved to the placement que… | ActionList.tsx:64 `placed: 'בוצע'`; FocusCard.tsx:63 same. PlacementQueue page.tsx:49 title 'הזמנות לביצוע'. FocusMode DoneSummary line 484: `{placed} בוצעו`. C… |
| 14 | P1 | M | a11y | /planning/procurement | FocusMode inner close-confirm alertdialog: focus is not moved into it when it opens, and the outer Tab focus trap continues to cycle through visually-occluded FocusCard buttons behind the overlay | /home/user/gt-factory-os-portal/src/app/(planning)/planning/procurement/_components/FocusMode.tsx:94-97 (requestClose sets confirmingClose=true with no focus ca… |
| 15 | P1 | L | flow | /planning/procurement | Tom's complaint explicitly requests the ability to "DEFER/POSTPONE orders for later procurement execution." No defer or postpone flow state exists anywhere in the corridor. "דלג" (skip) is the only non-placement exit for… | /home/user/gt-factory-os-portal/src/app/(planning)/planning/procurement/_components/FocusCard.tsx:693-704 (skip button — only `skipMut.mutate({poId})`, no date … |
| 16 | P1 | L | interaction | /planning/procurement, /purchase-orders/placement-queue | No defer/postpone action exists anywhere in the procurement corridor — Tom's explicit requirement is unmet | /home/user/gt-factory-os-portal/src/app/(planning)/planning/procurement/_components/ActionList.tsx:306-314 (ActionListProps: pos, onOpen, today only — no defer … |
| 17 | P1 | L | interaction | /planning/procurement (ActionList, FocusCard) | No delete/remove action for a session PO exists in the portal or the backend API — a wrongly-generated PO cannot be removed from the session, only skipped (which leaves it visible in the 'handled' bucket) | /home/user/gt-factory-os-portal/src/app/(planning)/planning/procurement/_components/ActionList.tsx:255-274 (row actions: only 'פתח במיקוד →', no delete/remove);… |
| 18 | P1 | L | visual | /purchase-orders/placement-queue + /planning/procurement | Tom's stated requirement: 'EVERY page needs the option to FILTER, SORT/CLASSIFY, DELETE orders, and DEFER/POSTPONE orders for later procurement execution.' Neither /purchase-orders/placement-queue nor /planning/procureme… | placement-queue/page.tsx (no filter UI of any kind). ActionList.tsx (no filter props or UI). /purchase-orders/page.tsx:783-863 (reference filter bar pattern tha… |
| 19 | P2 | S | flow | /planning/procurement | After skipping a session PO ("דלג"), the row moves to the "handled" (טופל) bucket with the status badge "דולג" (skipped) and the `whyNow` text "דולג." There is no explanation of whether or when the skipped item will resu… | /home/user/gt-factory-os-portal/src/app/(planning)/planning/procurement/_lib/decision.ts:87-89 (`bucket === "handled"` returns only "הוזמן" or "דולג" — no tempo… |
| 20 | P2 | S | flow | /planning/procurement | The link to the placement queue ("N הזמנות ממתינות לביצוע ←") in the SessionView summary card only renders when `placedCount > 0`. While Tom is mid-session reviewing proposed/approved POs and has not yet clicked "place" … | /home/user/gt-factory-os-portal/src/app/(planning)/planning/procurement/page.tsx:293-338 (`placedCount > 0` conditional renders the link; no static handoff cont… |
| 21 | P2 | S | interaction | /purchase-orders/placement-queue | PlacementRow has no form wrapper; pressing Enter on the last input field (payment terms custom text or date) does not submit the placement — expert operator must Tab to or click the button, adding friction to Doreen's re… | /home/user/gt-factory-os-portal/src/app/(po)/purchase-orders/placement-queue/_components/PlacementRow.tsx:231 (expanded content opens with <div className='space… |
| 22 | P2 | S | interaction | /planning/procurement (ActionList) | No quick approve or skip available directly from the ActionList row — every action requires entering the full-screen FocusMode overlay, even for an experienced planner who already knows the decision | /home/user/gt-factory-os-portal/src/app/(planning)/planning/procurement/_components/ActionList.tsx:255-274 — the actionable branch renders only the 'פתח במיקוד … |
| 23 | P2 | S | interaction | /purchase-orders | The /purchase-orders list has no user-controlled sort — the sort is hardcoded (late first → expected_receive_date → po_number); planners cannot re-order by supplier or total net | /home/user/gt-factory-os-portal/src/app/(po)/purchase-orders/page.tsx:604-613 — sort logic is a fixed comparator with no user override; page.tsx:1046-1056 — tab… |
| 24 | P2 | S | interaction | /purchase-orders/placement-queue | The placement confirm dialog title shows only the PO number ('לבצע את ההזמנה PO-001?') with no supplier name — when processing multiple rows the office manager cannot visually confirm which supplier she is committing to … | /home/user/gt-factory-os-portal/src/app/(po)/purchase-orders/placement-queue/_components/PlacementRow.tsx:121 — title: `לבצע את ההזמנה ${po.po_number}?` (suppli… |
| 25 | P2 | S | interaction | /planning/procurement/[session_po_id]/sheet | The order sheet back link always navigates to /planning/procurement unconditionally, dropping any active FocusMode position — a planner who opened the sheet mid-focus-walk returns to the list view and loses their place | /home/user/gt-factory-os-portal/src/app/(planning)/planning/procurement/[session_po_id]/sheet/page.tsx:59 — <Link href='/planning/procurement'> with no position… |
| 26 | P2 | S | visual | /planning/procurement | The expand/collapse toggle icon in ActionList's ProcurementRow uses ChevronRight (pointing visually rightward) for the collapsed state inside a dir='rtl' page. In RTL, the natural reading direction starts on the right. A… | ActionList.tsx:210 (`<ChevronRight className="h-4 w-4" aria-hidden />`), inside page-level `dir="rtl"` set at procurement/page.tsx:103. Screenshot: /tmp/ux-shot… |
| 27 | P2 | S | visual | /planning/procurement | ActionList uses `text-left` (absolute left-alignment) on the section count-badge and section total div inside a dir='rtl' page. FocusCard uses `text-left` on multiple table column headers in a dir='rtl' table. The Tailwi… | ActionList.tsx:363 (`className="text-left shrink-0"`), FocusCard.tsx:455 (`className="px-3 py-1.5 text-left font-semibold"`) repeated for מומלץ, כמות, יחידה, על… |
| 28 | P2 | S | visual | /purchase-orders/placement-queue | PlacementRow collapsed headers carry no status badge or urgency icon. The overdue state — where order_by_date has already passed — is signaled exclusively by a danger-red text color on the date string ('להזמין עד 2026-07… | PlacementRow.tsx:177-198 (no badge, only `className={po.order_by_date < todayIso ? 'font-semibold text-danger-fg' : 'font-medium text-fg'}`). ActionList.tsx:227… |
| 29 | P2 | S | visual | /planning/procurement + /purchase-orders/placement-queue | The order total field is rendered at wildly different typographic weights across the corridor, with no system rule governing it. In ActionList's per-row summary line, the total is 'text-3xs text-fg-faint tabular-nums' — … | ActionList.tsx:242-245 (`text-3xs text-fg-faint tabular-nums`), FocusCard.tsx:354 (`text-base font-semibold text-fg`), PlacementRow.tsx:174 (`font-mono tabular-… |
| 30 | P2 | S | visual | /planning/procurement | The EmptyNoSession state at /planning/procurement has no icon — just two short paragraphs of Hebrew text in a centered card. The portal UX standard defines the empty-state pattern as: icon/illustration + heading + body. … | procurement/page.tsx:493-504 (EmptyNoSession returns a div with text only, no icon). placement-queue/page.tsx:130-135 (`<ClipboardCheck className="h-6 w-6 text-… |
| 31 | P2 | S | visual | /planning/procurement | The cross-surface bridge link from /planning/procurement to /purchase-orders/placement-queue ('N הזמנות ממתינות לביצוע ←') is styled as a plain text link at text-xs font-medium text-accent, visually equal in weight to 'ה… | procurement/page.tsx:329-338 (`className="text-xs font-medium text-accent underline-offset-2 hover:underline"`). Screenshot: /tmp/ux-shots/planning-procurement-… |
| 32 | P2 | S | visual | /planning/procurement | FocusCard renders a link to the created PO in its placed-state confirmation using a truncated UUID: 'PO {po.po_id.slice(0, 8)}…'. The po_id is a UUID (e.g. 'a3f8e2c1-…'), not the human-readable PO number. The po_number f… | FocusCard.tsx:648 (`PO {po.po_id.slice(0, 8)}…`). portal_ux_standard.md §1 Forbidden in primary UI: 'Raw item_id, bom_version_id, recommendation_id, or any UUID… |
| 33 | P2 | S | copy | /planning/procurement (FocusCard placed-success state) | Raw UUID fragment surfaced to operator in placed-success link. After placing a PO, FocusCard renders `PO {po.po_id.slice(0, 8)}…` as the visible link text. `po_id` is a Postgres UUID; `po.po_id.slice(0, 8)` is a UUID pre… | src/app/(planning)/planning/procurement/_components/FocusCard.tsx:649 — `PO {po.po_id.slice(0, 8)}…` as the visible Link text. PurchaseSessionPo type at src/app… |
| 34 | P2 | S | copy | /purchase-orders (status filter bar) | Raw backend enum name `APPROVED_TO_ORDER` rendered verbatim as a clickable filter chip label in the purchase orders list. The filter bar iterates `STATUS_OPTIONS` and renders `{s}` directly, so operators see the enum str… | src/app/(po)/purchase-orders/page.tsx:98–103 (STATUS_OPTIONS array containing 'APPROVED_TO_ORDER'), line 813 (`{s}` as button child — raw enum label). Compare w… |
| 35 | P2 | S | copy | /purchase-orders (mobile cards and desktop table) | When `supplier_name` is null, both the mobile-card list and the desktop table render a UUID fragment and the internal field name as a tooltip: visible text shows `({r.supplier_id.slice(0, 8)}…)` and the `title` attribute… | src/app/(po)/purchase-orders/page.tsx:993–999 (mobile card) and 1098–1106 (desktop table) — `<span title={\`supplier_id ${r.supplier_id}\`}>Unknown supplier<spa… |
| 36 | P2 | S | copy | /planning/procurement (FocusCard — approved state, place but | The in-progress label on the primary 'place' button reads 'יוצר PO…', mixing the English acronym 'PO' into a Hebrew operator surface. /planning/procurement is an authorized Hebrew+RTL surface; all button and status copy … | src/app/(planning)/planning/procurement/_components/FocusCard.tsx:749 — `{placeMut.isPending ? "יוצר PO…" : "סמן כבוצע — צור הזמנה"}` |
| 37 | P2 | S | copy | /planning/procurement (RecommendationsToConvert error state) | The retry button in RecommendationsToConvert's error state reads 'Try again' in English. This component renders inside /planning/procurement, which is an authorized Hebrew+RTL operator surface. All button labels on this … | src/app/(planning)/planning/procurement/_components/RecommendationsToConvert.tsx:93 — `Try again` as button text. CLAUDE.md portal exceptions list confirms /pla… |
| 38 | P2 | S | copy | /planning/procurement/[session_po_id]/sheet (order sheet err | The order sheet page error state renders raw API error messages with no retry mechanism: `<p className="text-sm text-danger">שגיאה בטעינת הנתונים: {(error as Error)?.message ?? "לא ידוע"}</p>`. Two violations: (1) `error… | src/app/(planning)/planning/procurement/[session_po_id]/sheet/page.tsx:74–76 — `<p className="text-sm text-danger">שגיאה בטעינת הנתונים: {(error as Error)?.mess… |
| 39 | P2 | S | copy | /purchase-orders (POStatusBadge fallback) | The POStatusBadge component has a catch-all fallback `return <Badge tone="neutral">{status}</Badge>` that renders any unrecognized status string verbatim. If the backend introduces a new status enum value before the fron… | src/app/(po)/purchase-orders/page.tsx:175 — `return <Badge tone="neutral">{status}</Badge>;` as the fallback branch in POStatusBadge. |
| 40 | P2 | S | copy | /planning/procurement (FocusMode completion screen) | The completion headline for a fully resolved procurement session includes an emoji: 'סיימת את מושב הרכש 🎉'. Emoji are inconsistent with the factory OS's operational tone and may not render uniformly across the browser/OS… | src/app/(planning)/planning/procurement/_components/FocusMode.tsx:477 — `"סיימת את מושב הרכש 🎉"` as the done-summary headline. |
| 41 | P2 | S | copy | /planning/procurement (ActionList, FocusCard) and /purchase- | Per-PO date fields (order_by_date, earliest_need_date, covered_through_date) are rendered as raw ISO strings (YYYY-MM-DD) in Hebrew operator text on RTL surfaces. Examples: 'להזמין עד 2026-07-25', 'נדרש 2026-07-18'. The … | ActionList.tsx:251 — `להזמין עד {po.order_by_date}`; FocusCard.tsx:364 — `להזמין עד {po.order_by_date}`, :367 — `נדרש {po.earliest_need_date}`, :369 — `מכוסה עד… |
| 42 | P2 | S | copy | /purchase-orders/placement-queue (PlacementRow confirm dialo | The confirm dialog description for the 'בצע הזמנה' action is a single long conditional run-on sentence with four clauses and an appended conditional sub-sentence about missing ETA. Placing a real supplier order is a mone… | src/app/(po)/purchase-orders/placement-queue/_components/PlacementRow.tsx:122–128 — single paragraph with payment_terms, totalPreview, confirmedDate, cancellati… |
| 43 | P2 | S | copy | /planning/procurement (FocusCard inline action errors) | Inline mutation errors from approve, place, skip, and edit operations in FocusCard are rendered directly as `{actionError}` with no human-friendly lead-in and no guidance on next steps. The error message string comes fro… | src/app/(planning)/planning/procurement/_components/FocusCard.tsx:624–633 — `{actionError}` rendered without preamble or next-step guidance. actionError is sour… |
| 44 | P2 | S | a11y | /planning/procurement | AddLineForm: inline validation error messages for the orderable field and quantity field are plain sibling divs with no aria-describedby linking them to their triggering input — screen reader users tab to a field that ap… | /home/user/gt-factory-os-portal/src/app/(planning)/planning/procurement/_components/AddLineForm.tsx:90-104 (SearchableSelect has ariaLabel='פריט או רכיב' but no… |
| 45 | P2 | S | a11y | /purchase-orders | Purchase orders list page search input: placeholder text is the sole accessible label — no aria-label, no associated label element, and the Search icon is aria-hidden; placeholder disappears on input leaving the field un… | /home/user/gt-factory-os-portal/src/app/(po)/purchase-orders/page.tsx:847-862 — input element has only placeholder='Search PO number, supplier, notes…'; no id, … |
| 46 | P2 | S | a11y | /planning/procurement | ActionList ProcurementRow expand/collapse button: has aria-expanded but no aria-controls referencing the expandable lines panel — screen readers cannot navigate directly to the controlled region | /home/user/gt-factory-os-portal/src/app/(planning)/planning/procurement/_components/ActionList.tsx:200-211 — button has aria-label and aria-expanded={expanded} … |
| 47 | P2 | S | a11y | /purchase-orders/placement-queue | PlacementRow header toggle button: has aria-expanded but no aria-controls referencing the expanded content panel — screen readers cannot associate the button with the panel it controls | /home/user/gt-factory-os-portal/src/app/(po)/purchase-orders/placement-queue/_components/PlacementRow.tsx:161-165 — button has aria-expanded={open} but no aria-… |
| 48 | P2 | S | a11y | /planning/procurement | SessionView tab panels (ActionList, CalendarView) are not wrapped in role='tabpanel' elements with aria-labelledby pointing to their controlling tab — screen reader users switching tabs receive no semantic context that a… | /home/user/gt-factory-os-portal/src/app/(planning)/planning/procurement/page.tsx:392-396 — '{view === 'list' ? <ActionList .../> : <CalendarView .../>}' with no… |
| 49 | P2 | S | a11y | /purchase-orders/placement-queue | PlacementRow 'בצע הזמנה' disabled-state explanation is conveyed via the title attribute only — title is not reliably announced by screen readers on focus of a disabled button, leaving keyboard and AT users without the re… | /home/user/gt-factory-os-portal/src/app/(po)/purchase-orders/placement-queue/_components/PlacementRow.tsx:389-404 — disabled={!canPlace \|\| placeMut.isPending}… |
| 50 | P2 | S | a11y | /purchase-orders/placement-queue | PlacementRow expanded lines loading skeleton: has aria-busy='true' but no aria-live region — when loading completes and the skeleton is replaced by the real line content, there is no announcement to screen reader users t… | /home/user/gt-factory-os-portal/src/app/(po)/purchase-orders/placement-queue/_components/PlacementRow.tsx:203-210 — '<div className="space-y-2" aria-busy="true"… |
| 51 | P2 | S | a11y | /purchase-orders | Purchase orders desktop table rows have cursor-pointer and a full-row onClick handler on a non-interactive tr element — keyboard-only users cannot trigger row navigation by pressing Enter on the row; they must find and a… | /home/user/gt-factory-os-portal/src/app/(po)/purchase-orders/page.tsx:1072-1087 — '<tr ... className={cn('cursor-pointer',...)} onClick={() => router.push(...)}… |
| 52 | P2 | S | a11y | /planning/procurement | Inline supersede-session confirmation zone uses role='alertdialog' incorrectly — alertdialog implies a modal that restricts focus, but this zone renders inline without aria-modal and without focus management; role='alert… | /home/user/gt-factory-os-portal/src/app/(planning)/planning/procurement/page.tsx:114-149 — div with role='alertdialog' aria-label='אישור החלפת מושב' renders inl… |
| 53 | P2 | M | flow | /planning/procurement | When Tom runs the procurement-planning skill in chat, a purchase_session with proposed POs is created in Postgres. There is no portal notification — no inbox item, no dashboard widget, no home-page tile — telling Tom tha… | /home/user/gt-factory-os-portal/src/app/(planning)/planning/procurement/page.tsx:50-52 (`useCurrentSession()` — passive React Query poll, no push; no inbox noti… |
| 54 | P2 | M | interaction | /planning/procurement, /purchase-orders/placement-queue | No bulk select-and-act controls exist on either the ActionList or the placement queue — a planner with 10 'can_wait' orders cannot skip them all at once; Doreen with a uniform-payment-terms batch cannot apply the same te… | /home/user/gt-factory-os-portal/src/app/(planning)/planning/procurement/_components/ActionList.tsx:374-395 — row map has no checkbox or bulk state; /home/user/g… |
| 55 | P2 | M | visual | /planning/procurement + /purchase-orders | The 'ready to place' concept has two different badge colors across the corridor. In /planning/procurement ActionList, a session PO with status 'approved' renders an INFO (blue) badge labeled 'אושר — מוכן לשליחה'. In /pur… | ActionList.tsx:70 (`approved: 'info'`) vs purchase-orders/page.tsx:168 (`tone="warning"` for APPROVED_TO_ORDER). Screenshot: /tmp/ux-shots/planning-procurement-… |
| 56 | P2 | M | a11y | /planning/procurement | CalendarView desktop grid: tier urgency conveyed by color and dot only — no text label in chip or in button title attribute | /home/user/gt-factory-os-portal/src/app/(planning)/planning/procurement/_components/CalendarView.tsx:233-262 — desktop button only contains a colored dot (tierD… |
| 57 | P2 | L | visual | /planning/procurement + /purchase-orders/placement-queue | The corridor has no visual pipeline indicator showing where an order is in the 4-step execution flow (proposed → approved → placement queue → goods receipt). The only cross-surface signal is a de-emphasized text link in … | procurement/page.tsx:329-338 (bridge link styled as plain text). placement-queue/page.tsx (no pipeline context, no forward link). Screenshot: /tmp/ux-shots/plan… |

## Per-dimension status

| Dimension | P0 | P1 | P2 | Status |
|---|---|---|---|---|
| flow | 1 | 7 | 3 | RED |
| interaction | 0 | 6 | 6 | AMBER |
| visual | 0 | 2 | 9 | AMBER |
| copy | 0 | 1 | 11 | AMBER |
| a11y | 0 | 1 | 10 | AMBER |

## portal_ux_standard.md compliance

FAIL — portal_ux_standard.md violations: §1 Forbidden in primary UI (raw enum names in /purchase-orders filter chips — VISUAL-12/COPY-2; POStatusBadge fallback raw enum — COPY-8; raw UUID fragments in FocusCard placed-state link — VISUAL-11/COPY-1; raw UUID + supplier_id field-name tooltip in /purchase-orders — COPY-3); §3 Error template (raw error.message forwarded with no retry on the order-sheet error state — COPY-7; unwrapped inline mutation errors in FocusCard — COPY-12); §4 Status semantics coherence (same 'ready to place' state renders info/blue in ActionList and warning/amber in POStatusBadge — VISUAL-1); Hebrew-surface exception contract (English 'PO' acronym and 'Try again' leaking into authorized Hebrew surfaces — COPY-4/COPY-6). Hebrew + dir='rtl' on /planning/procurement and /purchase-orders/placement-queue are NOT violations — both are on the Tom-authorized exception list (CLAUDE.md 2026-06-17, 2026-06-20).

## Adversarial verification

All P0/P1 findings verified (P0: dual-lens, P1: single refuter). 3 findings refuted and dropped:

- **FLOW-7**: No surface in the corridor shows a PO's full status trajectory: proposed → approved → APPROVED_TO_ORDER → OPEN → PARTIAL… — REFUTED: The core claim ("no surface in the corridor shows the PO status trajectory; Tom has no persistent view of reached-queue vs placed vs receiving") is refuted by /purchase-orders (in the audited corridor…
- **VISUAL-2**: Two different Badge implementations are used across the corridor. ActionList.tsx, FocusCard.tsx, and PlacementRow.tsx (i… — REFUTED: The finding's premise is false: '@/components/badges/StatusBadge' is not a second Badge implementation. StatusBadge.tsx:23 is a documented Tranche 0A (2026-05-15) re-export shim — `export { Badge, BAD…
- **A11Y-3**: FocusCard action error uses conditionally-rendered role='alert' div — a second distinct mutation failure that replaces a… — REFUTED: Citations verify (FocusCard.tsx:625-634 conditional role="alert"; PlacementRow.tsx:368-386 always-mounted A11Y-010 pattern), but the finding's failure mechanism is inverted and its scenario unreachabl…

## Tom approval required?

yes — FLOW-8 is an ARCH_REQUIRED role-lattice decision that only Tom can arbitrate (portal-production-executor and backend-db-executor cannot resolve which side of the role/capability boundary should move without a written directive). Additionally, once FLOW-8 is unblocked, moving from HOLD to CONDITIONAL_SHIP with 17 named P1 conditions requires Tom's explicit written approval per locked gate thresholds.

## Next action for Tom

Tom: issue a written role-lattice decision on FLOW-8 — pick (A) reclassify the office manager (Doreen) from 'viewer' to 'planner' in Supabase and update the nav-manifest justification comment, OR (B) direct backend-db-executor to split a new capability (e.g. purchasing:place_order) grantable to the viewer role and update the RoleGate on /purchase-orders/placement-queue accordingly. Do not change the gate or role assignment silently; record the decision before any executor picks it up. Once FLOW-8 is resolved, re-run the UX release gate to consider CONDITIONAL_SHIP with the 17 P1s as named next-sprint conditions.

---
*Read-only gate run. Full per-finding detail (proposed fixes, verifier reasoning, screenshots) in workflow journal `wf_8a93229b-763`. Design directives from Tom (2026-07-16 chat) to be folded into the fix tranche: rename primary CTA to 'העבר לביצוע רכש'; recount-request→inbox; dynamic defaults-with-override; cancel-with-reason in placement queue; queue-age badges; mandatory expected_receive_date at placement; spoken-Hebrew supplier messages; no auto-actions; approval-is-the-money-gate; desktop-first.*